# v19 tracker

The one checklist for the v19 programme
(`2026-09-15-v19-programme.md`). Each chat flips its boxes, fills its
ledger row and leaves a hand-off note on merge. Read the previous row's
note before starting the next sub-cycle.

## Order

1. v19a — the week's clock and health
2. v19b — the loop closed
3. v19c — the phone
4. v19d — This Week, readable
5. v19e — compare and act
6. v19f — the question box (droppable, ruling 1)
7. v19g — the model's free half
8. v19h — code health and the docs

Sequential. Start each from `main` at the previous row's merge hash.

## Ledger

Fill on merge. Pins at the start of the programme: routes 51, job kinds
12, `Config` fields 62; suites 4509 Python (+ the golden's 11 inside that
total), 1098 frontend (+1 skipped); `main` at `b3e2bf2` plus the commit
that adds these programme docs.

| Sub-cycle | Branch | Merged | Date | Python | Frontend | Routes | Kinds | Config | Gate |
|---|---|---|---|---|---|---|---|---|---|
| v19a | `v19a-clock` | `8354a21` | 2026-09-16 | 4531 (4383 inner + 148 slow/golden) | 1114 (+1 skipped) | 51 | 12 | 62 | pass, seven lines; ruling 5 corrected (spec §4) |
| v19b | `v19b-loop` | 4927a15 | 2026-09-16 | 4535 (4387 inner + 148) | 1135 (+1 skipped) | 51 | 12 | 62 | pass, six lines; the real-run line waits on Thursday (spec §4) |
| v19c | `v19c-phone` | dc7cd6f | 2026-09-16 | 4535 | 1149 (+1 skipped) | 51 | 12 | 62 | pass, six lines; §2.1 corrected to StackedRows (spec §4) |
| v19d | `v19d-readable` | f2d61d3 | 2026-09-16 | 4538 (4390 inner + 148) | 1172 (+1 skipped) | 51 | 12 | 62 | pass, five lines; the what-if tab shares the ladder card (spec §4) |
| v19e | `v19e-compare` | d9881d9 | 2026-09-16 | 4542 (4394 inner + 148) | 1184 (+1 skipped) | 51 | 12 | 62 | pass, six lines; ruling 2 amended, pin unmoved (spec §4) |
| v19f | `v19f-ask` | `58f5960` | 2026-09-16 | 4554 (4406 inner + 148) | 1189 (+1 skipped) | 52 | 12 | 62 | pass, seven lines; three real answers, 0 offences (spec §4) |
| v19g | `v19g-model-free` | | | | | 52 | 12 | 62 | |
| v19h | `v19h-health` | | | | | 52 | 12 | 61 | |

## Checklist

Every sub-cycle has the same eleven boxes. Flip them in the chat that does
the work.

### v19a — the week's clock and health
- [x] spec written and reviewed by the user (the programme design's rulings; ruling 5 corrected in the spec)
- [x] plan written (programme §3 v19a; two briefs)
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec (§4)
- [x] screenshots approved (the user's "carry on" on the three named after-shots, 2026-09-16)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run (both greps, `git show main:config.toml` fails)
- [x] ROADMAP block + GUIDE §11 line
- [x] ledger row + hand-off note below
- [x] memory line

### v19b — the loop closed
- [x] spec written and reviewed by the user
- [x] plan written
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (eleven identical, one render flake settled by a same-code control; nothing new reachable on the served data)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run (both greps, `git show main:config.toml` fails)
- [x] ROADMAP block + GUIDE §11 line
- [x] ledger row + hand-off note below
- [x] memory line

### v19c — the phone
- [x] spec written and reviewed by the user
- [x] plan written
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (the user's "approved" on four phone/tablet shots, 2026-09-16; desktop pairs identical)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run (both greps, `git show main:config.toml` fails)
- [x] ROADMAP block + GUIDE §11 line
- [x] ledger row + hand-off note below
- [x] memory line

### v19d — This Week, readable
- [x] spec written and reviewed by the user
- [x] plan written
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (the user's "approved" on three shots, 2026-09-16)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run (both greps, `git show main:config.toml` fails)
- [x] ROADMAP block + GUIDE §11 line
- [x] ledger row + hand-off note below
- [x] memory line

### v19e — compare and act
- [x] spec written and reviewed by the user
- [x] plan written
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (the user's "approved with the 2 shots above", 2026-09-16)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run (both greps, `git show main:config.toml` fails)
- [x] ROADMAP block + GUIDE §11 line
- [x] ledger row + hand-off note below
- [x] memory line

### v19f — the question box
- [x] spec written and reviewed by the user
- [x] plan written
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (the user's "approving the merge", 2026-09-16)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run (both greps, `git show main:config.toml` fails)
- [x] ROADMAP block + GUIDE §11 line
- [x] ledger row + hand-off note below
- [x] memory line

### v19g — the model's free half
- [ ] spec written and reviewed by the user
- [ ] plan written
- [ ] implemented on the branch, suites green
- [ ] gate run by the orchestrator, verdict recorded in the spec
- [ ] screenshots approved
- [ ] ff-merged to main
- [ ] pushed
- [ ] security ritual run (both greps, `git show main:config.toml` fails)
- [ ] ROADMAP block + GUIDE §11 line
- [ ] ledger row + hand-off note below
- [ ] memory line

### v19h — code health and the docs
- [ ] spec written and reviewed by the user
- [ ] plan written
- [ ] implemented on the branch, suites green
- [ ] gate run by the orchestrator, verdict recorded in the spec
- [ ] screenshots approved
- [ ] ff-merged to main
- [ ] pushed
- [ ] security ritual run (both greps, `git show main:config.toml` fails)
- [ ] ROADMAP block + GUIDE §11 line
- [ ] ledger row + hand-off note below
- [ ] memory line

## Hand-off notes

### v19f (merged 58f5960 2026-09-16)
`POST /api/ask` is an anonymous job (`JOB_KINDS` 12); the route pin is 52
and no further bump is planned in v19. `answer_question` shares the
brief's command, facts and check; the question is fenced after the facts.
`useJob.start` takes a body for path-started jobs. The last three answers
live in `QuestionBox` state. v19g's spec is drafted at
`specs/2026-09-16-v19g-model-free-design.md` and its backend brief is
written; the pre-blend goals column it lists is already banked (v18c).

### v19e (merged d9881d9 2026-09-16)
`/api/advice/diff` takes `a` and `b` (week against week) beside `gw`;
`gw_from`/`gw_to` say which. The route pin is still 51, so v19f's bump
is 51 → 52 and the ledger's later rows read 52. *Must sell* from the
squad is `force_out`. `Planning` reads `location.state.whatif` once on
mount. One flake seen once in `test_advice_fixture.py`, passed twice
after. v19f's spec is drafted at `specs/2026-09-16-v19f-ask-design.md`;
its backend brief is written.

### v19d (merged f2d61d3 2026-09-16)
`reports/` is mounted at `/reports` (a mount, not an OpenAPI path).
`kit/Disclosure` keeps its state under a `storageKey`; `kit/Countdown`
has a `short` form; `Card` takes `id`. The What-If tab shares `LadderCard`
with This Week, so a change there names both pages. v19e's spec is
drafted at `specs/2026-09-16-v19e-compare-design.md`; it amends ruling 2:
the existing `/api/advice/diff` takes `a` and `b`, so the route pin stays
51 in v19e and v19f's bump is 51 → 52.

### v19c (merged dc7cd6f 2026-09-16)
`shots.sh` takes `WIDTHS="1400 820 375"` and names multi-width files
`<hub>-<theme>-<width>.png`; run it from the repo (a copy elsewhere writes
its shots relative to itself). `StackedRows` in `kit/` is the phone
rendering for hand tables; `useIsCompact`/`useIsWide` join `useIsMobile`.
The skip link's classes compile from `index.html`. Next: v19d, whose spec
is drafted at `specs/2026-09-16-v19d-readable-design.md`.

### v19b (merged 4927a15 2026-09-16)
The price step is one name, `gaffer.pipeline.bank_price_reading`, stubbed
for the whole suite in `conftest.py` except `test_pipeline.py`; a new test
that runs the pipeline outside that file needs no stub, and one inside it
goes through `_wire`. Gate line 3 (the trace's price line on a real run)
is pasted into the v19b spec after Thursday's 18:00 job. `PinDialog` is
in `kit/` now. Next: v19c, whose spec is drafted at
`specs/2026-09-16-v19c-phone-design.md`.

### v19a (merged `8354a21` 2026-09-16)
All gate lines pass; the merge waits on the user's screenshot approval
(`.superpowers/shots/v19a-after/{this-week,health,live}-dark.png` and
their light twins). Two things the next chat must know: the freshness
strip is on every page, so an unnamed hub's pair is compared *below the
strip* (rows 27–38 are the strip at 1400 wide); and `shots.sh`'s default
`SHELL_BIN` names build 1234 while 1243 is installed, so export
`CHROME_HEADLESS_SHELL` to the 1243 path before shooting. After the
merge the user re-runs `scripts/install_automation.sh` (the field plist
moved to 18:30) and checks `launchctl list | grep com.gaffer` prints
nine: on 2026-09-15 at 23:33 it printed two and the prices job had not
fired. v19b's spec is drafted at
`specs/2026-09-15-v19b-loop-design.md`, uncommitted until its branch.

### programme start (2026-09-15)
The design was approved with all eleven rulings as proposed. Automation was
reinstalled on 2026-09-15 (nine plists written 00:37; the snapshot job ran
for GW5 the same day). The Tuesday digest of 2026-09-15 re-emitted GW3 and
`logs/field.log` shows the scrape banking GW3 twice on 09-12, both by
design of the current code and both v19a's to change. Nothing else is in
flight.
