# v18 tracker

The one checklist for the polish programme
(`2026-09-12-v18-polish-programme.md`). Each chat flips its boxes, fills
its ledger row and leaves a hand-off note on merge. Read the previous row's
note before starting the next sub-cycle.

## Order

1. v18a — the gate, back on
2. v18b — the advice path tells the truth
3. v18c — measurement that has never run
4. v18d — the core out of the web layer
5. v18e — the loader, finished
6. v18f — the surface, correct to the hand
7. v18g — the suite, one home per rule
8. v18h — the docs, true

Sequential. Start each from `main` at the previous row's merge hash.

## Ledger

Fill on merge. Pins at the start of the programme: routes 51, job kinds
12, `Config` fields 62; suites 4420 passed + 5 skipped Python (the five are
the stale golden, closed by v18a), 986 frontend (+1 skipped); `main` at
`5a01f29` plus the commit that adds these programme docs.

| Sub-cycle | Branch | Merged | Date | Python | Frontend | Routes | Kinds | Config | Gate |
|---|---|---|---|---|---|---|---|---|---|
| v18a | `v18a-gate` | `20185aa` | 2026-09-12 | 4427 | 986 (+1 skipped) | 51 | 12 | 62 | pass, first run (spec §3) |
| v18b | `v18b-advice-path` | `f662d6d` | 2026-09-12 | 4445 | 986 (+1 skipped) | 51 | 12 | 62 | pass, first full run (spec §3) |
| v18c | `v18c-measurement` | `dfb0984` | 2026-09-12 | 4458 | 988 (+1 skipped) | 51 | 12 | 62 | pass, two gate lines corrected (spec §3) |
| v18d | `v18d-layering` | `b94fbc4` | 2026-09-14 | 4496 | 988 (+1 skipped) | 51 | 12 | 62 | pass, three golden runs, two rulings (spec §3) |
| v18e | `v18e-loader` | `b1867b9` | 2026-09-14 | 4496 | 1058 (+1 skipped) | 51 | 12 | 62 | pass, four parts; 422 ruling (spec §3) |
| v18f | `v18f-surface` | `bf84d7c` | 2026-09-14 | 4496 | 1098 (+1 skipped) | 51 | 12 | 62 | pass, five parts; lint ruling (spec §3) |
| v18g | `v18g-suite` | `bc02faf` | 2026-09-14 | 4509 | 1098 (+1 skipped) | 51 | 12 | 62 | pass, six lines; seventeen pin homes not fourteen (spec §3) |
| v18h | `v18h-docs` | `1f5afaa` | 2026-09-14 | 4509 | 1098 (+1 skipped) | 51 | 12 | 62 | pass, two lines; G4 the review was wrong (spec §3) |

## Checklist

Every sub-cycle has the same eleven boxes. Flip them in the chat that does
the work.

### v18a — the gate, back on
- [x] spec written and reviewed by the user
- [x] plan written
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (n/a)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run (both greps, `git show main:config.toml` fails)
- [x] ROADMAP block + GUIDE §11 line
- [x] ledger row + hand-off note below
- [x] memory line

### v18b — the advice path tells the truth
- [x] spec written and reviewed by the user
- [x] plan written
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (n/a)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run
- [x] ROADMAP block + GUIDE §11 line
- [x] ledger row + hand-off note below
- [x] memory line

### v18c — measurement that has never run
- [x] spec written and reviewed by the user
- [x] plan written
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (Model → Review, Model → Journal; by the orchestrator in the user's absence, images kept)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run
- [x] ROADMAP block + GUIDE §11 line + GUIDE §3 blend sentence
- [x] ledger row + hand-off note below
- [x] memory line

### v18d — the core out of the web layer
- [x] spec written and reviewed (by the orchestrator in the user's absence; two rulings it did not foresee are in its §3)
- [x] plan written (the spec's three groups, run as three implementer briefs)
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (n/a)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run
- [x] ROADMAP block + GUIDE §11 line
- [x] ledger row + hand-off note below
- [x] memory line

### v18e — the loader, finished
- [x] spec written and reviewed (by the orchestrator in the user's absence; the 422 ruling is in its §2.2/§3)
- [x] plan written (the spec's five tasks, run as four implementer briefs plus the orchestrator's rails)
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (twelve pairs byte-identical against a same-time control; by the orchestrator in the user's absence, images kept)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run
- [x] ROADMAP block + GUIDE §11 line
- [x] ledger row + hand-off note below
- [x] memory line

### v18f — the surface, correct to the hand
- [x] spec written and reviewed (by the orchestrator in the user's absence; the lint ruling is in its §3)
- [x] plan written (the spec's four tasks, run as four implementer briefs)
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (twelve pairs byte-identical against a same-time control; by the orchestrator in the user's absence, images kept)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run
- [x] ROADMAP block + GUIDE §11 line
- [x] ledger row + hand-off note below
- [x] memory line

### v18g — the suite, one home per rule
- [x] spec written and reviewed by the user
- [x] plan written (the spec's §2 is the plan, six tasks)
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (n/a)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run
- [x] ROADMAP block + GUIDE §11 line + CLAUDE.md gate command
- [x] ledger row + hand-off note below
- [x] memory line

### v18h — the docs, true
- [x] spec written and reviewed by the user
- [x] plan written (the spec's §2 checklist)
- [x] implemented on the branch, suites green
- [x] gate run by the orchestrator, verdict recorded in the spec
- [x] screenshots approved (n/a, no UI change) (n/a)
- [x] ff-merged to main
- [x] pushed
- [x] security ritual run
- [x] ROADMAP v18 block with the closing ledger + GUIDE §11/§12 as of v18h
- [x] ledger row + hand-off note below
- [x] memory line

## Hand-off notes

One short paragraph per sub-cycle, written by the chat that merged it, for
the chat that starts the next one: new names (fields, functions, files),
anything the next sub-cycle must know, anything left open. Never a value
from `config.toml`.

### v18a
Merged `20185aa`. New names: `tests.golden_client.golden_header_or_skip(repo,
golden=GOLDEN_DIR)` — the one gate for "may the live-model golden run":
skips when `models/` is absent, when the header is missing, or when
`stale_inputs` is non-empty, in that order, with the sentence `golden board
recorded under a different <first file> (<n> input(s) differ); re-record
with python -m tests.golden_client --write`; `_HushedTime` in the same
file (every `time` name forwarded but `sleep`). `write_expected` gathers
the Inputs in its own fresh scratch tree, so `--write` and `--inputs` now
record the same `prior_advice` (`null`). The fixture was re-recorded at
`a0bd45c`: expected files unchanged, header hashes moved. **The gate for
v18b–v18d is** `.venv/bin/pytest -q -rs tests/test_golden_board.py
tests/test_pipeline.py` → 56 passed, 0 skipped, about fifteen minutes;
`-rs` is part of the command now, and a skip line naming a file means the
board moved, not that the refactor is fine. Things the next chats must
know: the golden's five live-model tests need `models/` to match the
header, so **do not retrain on the branch** (v17d's lesson) and do not
re-record unless the board is meant to move; `tests/test_pipeline.py` runs
alone now, but any new test that patches `gaffer.models.train.*` by dotted
path must import `gaffer.advise` first for the same reason. Left open:
nothing from this sub-cycle; the v18b spec is already drafted with its gate
(`specs/2026-09-12-v18b-advice-path-design.md`).

### v18b
Merged `f662d6d`. New names: `Plan.solver` (`"highs"` | `"cbc"` | `None`),
`optimize.milp._solve` returns the name; `build_advice(..., now=)`,
`ladder_payload(..., now=)`; `predict_components(..., *, cfg)` and
`Predictions.components(..., cfg)` (the recorded adapter accepts and
ignores it); keywords `price_falls(state, *, price_timing=)`,
`owned_price_falls(owned, *, price_timing=)`,
`_owned_price_falls(day, owned, price_timing)`, `apply_availability(...,
overrides=, start_floor=, llm_serving=, current_season=)`,
`save_availability(avail, gw, *, overrides=)`; `run_golden` returns a
3-tuple `(advice, state, ladder)`, `strip_volatile` drops `wall_s`,
`expected/ladder.json` is the fourth golden file; `render_report` passes
`hit_cost` to the template; `tests/test_advice_path.py` holds the v18b
rails and `tests/test_golden_board.py::test_gather_inputs_and_build_advice_read_no_view`
is the sealed-view rail (golden-marked, ~2.5 min: it builds the board).
**The gate for v18c–v18d is** `.venv/bin/pytest -q -rs
tests/test_golden_board.py tests/test_pipeline.py` → **58 passed, 0
skipped**, about eighteen minutes. Things the next chats must know: the
`_Read` sentinel is a `BaseException` and is patched at `gaffer.config`
*and* at `gaffer.price_timing` (which binds the name at import) — a new
module that binds `config_in_force` at import needs a third `patch_view`
line in that rail or the seal has a hole; the ladder's three view reads
are on the router's `build_ladder` path and were left by design; the
first sealed rail compared a board built from a fresh `RecordedComponents`
gather and was ten points off because that adapter has no calibration —
compare gathered *frames* to the recorded ones, build from the recorded
Inputs. Left open: one unattributed flake in one of three full-suite runs
(name not captured); the fake FPL clients in `test_web_smoke.py` and
`test_web_league_sim.py` now answer `get_entry`/`get_entry_history`, and
the v8c fixture patches `routers.league.fpl_client` and clears
`_OVERVIEW` — v18g's cache fixture should own those clears.

### v18c
Merged `dfb0984`. New names: `e_goals_model` in `artifacts.COMPONENT_COLS`
(banked before the odds blend; backfilled from `e_goals` on a no-odds
run); `cli.evaluate(season=None)` and `cli.backtest(season=None)` resolve
to the configured season and to `train_seasons[-1]` respectively;
`tracking.update_health` reads `review.load_ledger()` lazily; the v18c
stage in `frontend/scripts/shots.sh`. Things the next chats must know:
`update_health` runs from `gather_inputs` (`advise.py:627`), not from
`review` — a gate that wants the health file refreshed must run advise or
call the function; the golden's `inputs/components.parquet` is the file
that moves when a component column is added, and `--inputs` re-records it
in a fresh tree in about a minute; the control screenshots were shot from a
`main` worktree served with `PYTHONPATH=<worktree>/src` from the repo's
cwd (data and reports are the machine's), which is the cheap way to get a
same-machine control without checking out `main` under an implementer.
Left open: the calibration block's `p_cs` stays cumulative-only until a
gameweek has 30 club-fixtures, which none will; a per-two-gameweek window
is a candidate for the model cycle.

### v18d
Merged `b94fbc4`. New names: `gaffer/difficulty.py`
(`rate_fixtures(weeks)`, `difficulty_by_team(gws)`; the ticker route is a
shape adapter over it and `identity._difficulty_by_team` a one-line
delegate kept for the tests that stub it), `gaffer/refresh.py`
(`run_data_refresh`, the job body; `job_kinds` imports it),
`gaffer/clock.py` (`snap_date`, imported back into `snapshot`),
`gaffer/models/predict.py` (`MODEL_NAMES`, `predict_components`,
`news_availability` — `advise` re-exports nothing, so tests patch
`gaffer.models.predict.*` or the module that binds the name),
`gaffer/models/modes.py`, `gaffer/optimize/formation.py`
(`formation_legal`, `XI_BOUNDS`), `gaffer/web/coerce.py` (`fail`,
`opt_float`, `opt_int`, `finite`), `config.cap`, `config.on_invalidate`,
`match_odds.code_for`, `chip_policy.{WILDCARD_RECOMMEND_THRESHOLD,
CHIP_PLAY_THRESHOLD, PAIR_CHIP}` (patch the sentinels there), the override
store's read half in `artifacts` (`load_overrides`, `attach_overrides`,
`overrides_path`; patch `gaffer.artifacts.load_overrides`),
`fit_dnp_calibrator(..., *, inner=factory)`, `tracking.HEALTH_PATH`,
`tests/test_layering.py` (three rails: `web_imports_outside_web`,
`top_level_cycles`, `absent_lazy_imports`/`private_reaches`). Things the
next chats must know: the golden gate is unchanged (`58 passed, 0
skipped`, ~18 min) and ran clean three times, so the layering moved no
served number; the what-if validators stayed in the router as public
`validate`/`summary` (ruling in the spec §3: a core copy would import the
schemas); rail 1 exempts `cli.py` and `mcp_server.py` whole; a new module
that imports the web layer anywhere else fails the suite. For v18e:
`web/coerce.py` is the write routers' one 422 and the frontend's error
shape is unchanged. For v18g: four private reaches the spec did not name
(`artifacts._history_stamp` from `journal`, `journal._code_of_element`
from `review`, `config._source_of` from `routers/settings`,
`data.cups._cached_get` from `core_insights`) are recorded in the spec's
"left open"; `routers/meta.py` still spells the health path. For v18h:
GUIDE §2/§5 sentences that name `routers/meta.ticker` as the rating's home
or `advise.predict_components` are now wrong (grep list in the v18d spec's
outcome is not exhaustive — grep the GUIDE for `predict_components`,
`_difficulty_by_team`, `run_data_refresh`).

### v18e
Merged `b1867b9`. New names: `PageData.status` (the `ApiError`
status behind `error`, else `null`); `kit/Loaded` (`page`, `empty` as a
node or `(message) => node`, `loading`, `isEmpty`, render-prop children;
404 **or 422** is the empty branch — every `GafferError` is a 422, the
ruling of this cycle); `kit/ErrorBoundary` (`resetKey`, around `<Routes>`
in `App.tsx`); the five fetch rails `hubs/*.fetches.test.tsx` (the
control commit is `8db1259`; the walk's `/api/review` is 1 now);
`kit/Loaded.status.test.tsx` (nine rows, 500 vs the cold-clone status); the
invalidation rows for the watchlist, drafts, `invalidatePrefix('/api/league/')`
and the Model hub's job buttons (the remount nonces are gone). Things the
next chats must know: a test double of `../api/client` must export
`errorText` and a real-shaped `ApiError` (`status` on the instance,
`instanceof` must hold — `vi.importActual` is the safe way), or every
rejection through `usePageData` throws instead of rendering; the only
`apiGet` left in `hubs/`+`kit/` is Live's poll and a new raw read fails
nothing yet — v18g may want that as a rail; the control screenshots must
be shot minutes before the branch's (`v18e-before` differed from
`v18e-before2` on all twelve by the clock); the `main` worktree method for
a same-machine control is in the v18c note and works unchanged. For
v18f: `FixtureTicker` has no cold-clone sentence (it shows a callout for
the 422, as before); the Players matrix tab fetches twice before the
gameweek is known (`null` path until `gw` would fix it); `ExplainModal`
and `PinDialog` are the two hand-rolled modals v18f replaces with Radix
`Dialog` — `ExplainModal` now reads through `usePageData(open ? path :
null)`, keep that when swapping the shell.

### v18f
Merged `bf84d7c`. New names: `kit/Th` (`scope="col"`, `sort` → `aria-sort`),
`kit/useOpener` (focus return for a dialog with no Radix `Trigger`),
`api/useSettingWrite` (`{ also, alsoPrefix, onError }` → `(key, value,
what?) => Promise<boolean>`; the invalidation table has a row for the hook
and each caller's row names `useSettingWrite(` plus its literal extras),
`hubs/planning/useWhatIfSubmit`, `boardRequest`, `TraceMoves`,
`ChipOutlook`, `chips.ts`, `hubs/model/quality/*Section.tsx` +
`quality/shared.tsx`, `hubs/league/MarginFan`, `hubs/players/compareRows`,
`hubs/this-week/RungRow`; `--color-scrim`, `--color-on-accent`;
`eslint.config.js`, `npm run check` / `npm run lint`; `POLL_MS` exported
from `api/useJob`. Things the next chats must know: `npm run check` is the
frontend gate now (tsc, vitest, types drift, eslint) and takes ~10 s;
`set-state-in-effect` is `warn` by ruling, eight sites, do not silence them
per-line; the token rail's mono-face allowance names `TraceMoves.tsx` as
well as `PlannerBoard.tsx`; the dialogs set `aria-label` explicitly so
tests that find them by name keep working — keep that when touching them;
Tailwind's scanner reads class names out of `tokens.test.ts`'s own regexes
(v7d), so the compiled CSS still defines `.text-white` — a rule written as
a regex with the class name spelled out regenerates the class it forbids;
a same-time control from a `main` worktree is the only screenshot control
that holds (v18e's and v18f's lesson, twice). For v18g: the frontend has
no per-rule single home yet beyond `tokens.test.ts`; `types.test.ts`'s
`BEFORE_THE_SPLIT` list dropped two retired names on purpose; the eight
`react-hooks` warnings are the residual. Left open: `ExplainModal`'s long
lines; `FixtureTicker`'s cold-clone sentence.

### v18g
Merged `bc02faf`. New names: `[tool.ruff]` in `pyproject.toml` (`uvx ruff check
src tests` is a gate line now; `extend-immutable-calls` lists typer's
builders), `tests/test_advise_order.py`, `test_only_one_file_pins_the_job_kind_count`
in the v12 W1 meta-rail, `_module_caches_are_never_shared_between_tests`
in `tests/conftest.py`, the `slow` marker and `filterwarnings` in
`pyproject.toml`, `tests/data/gw2-advice.json`, `tests/test_cli_smoke.py`,
`scripts/archive/`. Things the next chats must know: the inner loop is
`.venv/bin/pytest -q -m "not slow and not golden"` (~70 s, 4 warnings) and
the full suite is unchanged in meaning; a new job-kind count assertion
anywhere but the meta-rail fails the suite — assert membership; the seam
order is asserted through `tests/gather_harness.py`, so a new seam in
`gather_inputs` needs a stub there and a name in `SEAMS`; the conftest
fixture clears caches only for modules already imported, so a new
process-lifetime cache gets a row in its tuple, not a clear in a test;
`wired` is imported into `test_v16_plan_objective` with a `noqa` because a
fixture named as a parameter is what F811 sees; the four PuLP filters are
deferred debt for PuLP 4. For v18h: GUIDE sentences naming
`routers/meta.ticker`, `advise.predict_components`, `run_data_refresh` and
`_difficulty_by_team` are stale since v18d; §11's closing paragraph still
says 4,425 + 986; §12 is "as of 2026-09-03". Left open: the four singleton
warnings; `test_advise.py` at 3.4 s.

### v18h
Merged `1f5afaa`. No code. Things the next chats must know: `CLAUDE.md`'s
orchestrator-only list now names the five pin-carrying test files
(`test_served_plan`, `test_golden_board`, `test_pipeline`,
`test_advise_order`, `test_layering`); the README is 413 lines and the
front door — the history is GUIDE §11, the open list GUIDE §12 and the
ROADMAP's Open index; this machine's `launchctl` shows 2 of 9 gaffer jobs
loaded (only `backup` and `core-insights`) and the availability log stops
on 09-12 — `scripts/install_automation.sh` is the user's to run before the
model cycle's replays lean on fresh snapshots; the 2026-09-12 review's
horizon claim was wrong (the default and this machine are both 3) and its
"seventeen became fourteen" claim was wrong (fourteen is right); `npm run
check`'s `SettingsTab > saves one key at a time` loses a typing race under
heavy load (read `35` for `5`) and passes alone — do not read one such
failure as a regression, re-run it idle. The v18 programme is closed; the
model cycle (ROADMAP candidates 11–13, the role replay, the C1 ablation)
follows by the user's ruling, each arm replay-gated; the security incident
is still the user's, undecided.
