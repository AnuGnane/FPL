{
 "definitions": {
  "AdviceDiff": {
   "description": "What changed between the two newest runs of one gameweek.\n\n``available`` is false on a first run of the week \u2014 the ordinary case, not\nan error \u2014 and everything else is then empty, so the client renders\nnothing without having to special-case a status code.",
   "properties": {
    "available": {
     "description": "Whether two runs exist to compare \u2014 false on a first run of the week.",
     "type": "boolean"
    },
    "buys_added": {
     "description": "Players in ``buys`` now that were not in the earlier run.",
     "items": {
      "$ref": "#/definitions/AdvicePlayer"
     },
     "type": "array"
    },
    "buys_dropped": {
     "description": "Players in the earlier run's ``buys`` that dropped out.",
     "items": {
      "$ref": "#/definitions/AdvicePlayer"
     },
     "type": "array"
    },
    "captain_from": {
     "anyOf": [
      {
       "$ref": "#/definitions/AdvicePlayer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The earlier run's captain, set only when the captain changed."
    },
    "captain_to": {
     "anyOf": [
      {
       "$ref": "#/definitions/AdvicePlayer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The later run's captain, set only when the captain changed."
    },
    "changed": {
     "default": false,
     "description": "Whether anything in the plan moved, from ``diff_advice`` in\n`artifacts.py`: any buy, sell, captain or chip change, or a nonzero\nexpected-points delta.",
     "type": "boolean"
    },
    "chip_from": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The earlier run's recommended chip, set only when it changed."
    },
    "chip_to": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The later run's recommended chip, set only when it changed."
    },
    "current_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Timestamp stem of the later of the two compared files."
    },
    "ep_movers": {
     "description": "Players whose expected points moved between the two newest component\nbreakdowns. Independent of ``available``: a first run of the week has no\nplan to diff and may still have a retrain to report (plan A10).",
     "items": {
      "$ref": "#/definitions/EpMover"
     },
     "type": "array"
    },
    "ep_movers_count": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "How many moved, or ``None`` when there is no predecessor breakdown to\ncompare against. ``None`` and ``0`` are different claims \u2014 \"we have not\nretrained since you looked\" against \"the retrain changed nothing\" \u2014 and\nthe strip renders only the second."
    },
    "expected_pts_delta": {
     "default": 0.0,
     "description": "Later run's ``expected_pts`` minus the earlier run's, rounded to 2dp.",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek being diffed, or the ``b`` week-against-week path's target.",
     "type": "integer"
    },
    "gw_from": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "v19e \u00a72.1: set only on the week-against-week path (``?a=&b=``), so a\nclient can tell \"last run of this gameweek\" from \"last gameweek\"."
    },
    "gw_to": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The ``b`` gameweek on the week-against-week path, paired with ``gw_from``."
    },
    "previous_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Timestamp stem of the earlier of the two compared files."
    },
    "sells_added": {
     "description": "Players in ``sells`` now that were not in the earlier run.",
     "items": {
      "$ref": "#/definitions/AdvicePlayer"
     },
     "type": "array"
    },
    "sells_dropped": {
     "description": "Players in the earlier run's ``sells`` that dropped out.",
     "items": {
      "$ref": "#/definitions/AdvicePlayer"
     },
     "type": "array"
    }
   },
   "required": [
    "available",
    "buys_added",
    "buys_dropped",
    "captain_from",
    "captain_to",
    "changed",
    "chip_from",
    "chip_to",
    "current_at",
    "ep_movers",
    "ep_movers_count",
    "expected_pts_delta",
    "gw",
    "gw_from",
    "gw_to",
    "previous_at",
    "sells_added",
    "sells_dropped"
   ],
   "title": "AdviceDiff",
   "type": "object"
  },
  "AdvicePlayer": {
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "name": {
     "description": "Player name, as stored on the advice payload being diffed.",
     "type": "string"
    }
   },
   "required": [
    "code",
    "name"
   ],
   "title": "AdvicePlayer",
   "type": "object"
  },
  "ArtifactItem": {
   "properties": {
    "bytes": {
     "description": "File size in bytes, from ``path.stat().st_size``.",
     "type": "integer"
    },
    "name": {
     "description": "The file's path under ``reports/``, e.g. ``reports/health.json``.",
     "type": "string"
    }
   },
   "required": [
    "bytes",
    "name"
   ],
   "title": "ArtifactItem",
   "type": "object"
  },
  "AskAnswer": {
   "description": "The job record's ``result`` for ``POST /api/ask`` (v19f \u00a72.2).\n\nThe answer is never banked, so this shape exists only as the job's\nresult: the frontend reads it through ``useJob`` and forgets it.",
   "properties": {
    "answer": {
     "description": "The prose, or ``\"\"`` when the command did not answer.",
     "type": "string"
    },
    "at": {
     "description": "When the answer was made, UTC.",
     "type": "string"
    },
    "gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The gameweek whose facts the answer was drawn from."
    },
    "model_command": {
     "description": "The command's first word, as the brief records it.",
     "type": "string"
    },
    "offences": {
     "description": "``check_brief``'s verdict; non-empty means the answer is not trusted.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "question": {
     "description": "The question as asked, stripped.",
     "type": "string"
    }
   },
   "required": [
    "answer",
    "at",
    "gw",
    "model_command",
    "offences",
    "question"
   ],
   "title": "AskAnswer",
   "type": "object"
  },
  "AskRequest": {
   "description": "The body of ``POST /api/ask`` (v19f \u00a72.2).",
   "properties": {
    "gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The gameweek to answer about; ``None`` is the newest on disk."
    },
    "question": {
     "description": "The manager's own words, refused over 500 characters.",
     "type": "string"
    }
   },
   "required": [
    "gw",
    "question"
   ],
   "title": "AskRequest",
   "type": "object"
  },
  "BackupHealth": {
   "properties": {
    "bytes": {
     "description": "Its size on disk.",
     "type": "integer"
    },
    "modified_at": {
     "description": "When it was written.",
     "type": "string"
    },
    "path": {
     "description": "The newest backup archive's path, from ``gaffer.backup.latest_backup``.",
     "type": "string"
    }
   },
   "required": [
    "bytes",
    "modified_at",
    "path"
   ],
   "title": "BackupHealth",
   "type": "object"
  },
  "BenchmarkEvaluation": {
   "properties": {
    "caveat": {
     "description": "Why the comparison to ``references`` is not exact.",
     "type": "string"
    },
    "git_sha": {
     "description": "The commit the benchmarked model was built from.",
     "type": "string"
    },
    "references": {
     "additionalProperties": {
      "additionalProperties": {
       "$ref": "#/definitions/ReferenceMetrics"
      },
      "type": "object"
     },
     "description": "Published metrics for the same cuts, from outside sources.",
     "type": "object"
    },
    "run_at": {
     "description": "When this benchmark was run.",
     "type": "string"
    },
    "stratified": {
     "additionalProperties": {
      "additionalProperties": {
       "$ref": "#/definitions/CategoryMetrics"
      },
      "type": "object"
     },
     "description": "cut (\"all\" / \"starters\") -> return category -> metrics.",
     "type": "object"
    },
    "test_season": {
     "description": "The held-out season the benchmark was scored against.",
     "type": "string"
    }
   },
   "required": [
    "caveat",
    "git_sha",
    "references",
    "run_at",
    "stratified",
    "test_season"
   ],
   "title": "BenchmarkEvaluation",
   "type": "object"
  },
  "BriefPanel": {
   "description": "The newest brief, or the digest to fall back on (v16 \u00a76.5).",
   "properties": {
    "checked_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "ISO timestamp the brief was checked and banked, from ``run_brief``."
    },
    "fallback": {
     "anyOf": [
      {
       "$ref": "#/definitions/DigestPanel"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The newest digest, offered in place of a brief that was never\nwritten or did not pass its truth check."
    },
    "gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The gameweek the brief is about."
    },
    "model_command": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The command name of the LLM the brief was generated with."
    },
    "note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why there is no brief for the newest gameweek, when there is none."
    },
    "prose": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The LLM's written brief, once it has passed the truth check."
    },
    "run_stamp": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The advice run this brief was written against, for staleness checks."
    }
   },
   "required": [
    "checked_at",
    "fallback",
    "gw",
    "model_command",
    "note",
    "prose",
    "run_stamp"
   ],
   "title": "BriefPanel",
   "type": "object"
  },
  "CalibrationGw": {
   "properties": {
    "gw": {
     "description": "The gameweek this row scores.",
     "type": "integer"
    },
    "heads": {
     "additionalProperties": {
      "$ref": "#/definitions/CalibrationHead"
     },
     "default": {},
     "description": "Per-head calibration for this gameweek alone.",
     "type": "object"
    },
    "n": {
     "description": "How many rows this gameweek contributed.",
     "type": "integer"
    }
   },
   "required": [
    "gw",
    "heads",
    "n"
   ],
   "title": "CalibrationGw",
   "type": "object"
  },
  "CalibrationHead": {
   "description": "One probability head's calibration for one gameweek, or a refusal.\n\n``status`` rather than a missing key: a head under\n``evaluation.MIN_CALIBRATION_SAMPLES`` has the same shape as a scored one\nwith nulls in it, so the card renders \"not enough data\" from a field\ninstead of branching on absence.",
   "properties": {
    "brier": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Brier score, when scored."
    },
    "log_loss": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Log loss, when scored."
    },
    "n": {
     "description": "How many rows the head was scored over.",
     "type": "integer"
    },
    "reliability": {
     "default": [],
     "description": "Predicted-vs-observed calibration curve, when scored.",
     "items": {
      "$ref": "#/definitions/ReliabilityBin"
     },
     "type": "array"
    },
    "status": {
     "description": "``\"ok\"`` when scored, else why not (e.g. below the sample floor).",
     "type": "string"
    }
   },
   "required": [
    "brier",
    "log_loss",
    "n",
    "reliability",
    "status"
   ],
   "title": "CalibrationHead",
   "type": "object"
  },
  "CalibrationHealth": {
   "description": "The fitted EP calibration, one delta per position group (v19g \u00a72.1).\n\nA position is fitted only once it has ``min_rows`` 60-minute appearances\n(``models/calibrate.py``, ``MIN_ROWS``), so the GKP row arrived by accrual\npart-way through a season and nothing anywhere said so. Nothing said when\none dropped back out either, which is the failure this reports: an\nunfitted position is the identity, and identity is indistinguishable from\n\"calibrated to zero\" unless the absence is named.",
   "properties": {
    "by_pos": {
     "additionalProperties": {
      "type": "number"
     },
     "description": "Position group -> the additive correction a nailed starter takes.",
     "type": "object"
    },
    "fitted_positions": {
     "description": "The keys of ``by_pos``, in ``GKP DEF MID FWD`` order.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "min_rows": {
     "description": "The floor a group must clear to be fitted at all \u2014 the number the tab\nquotes when it says why a position is absent.",
     "type": "integer"
    },
    "missing": {
     "description": "Which of the four groups carry no fitted delta, in the same order.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "saved_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "description": "From the model's own ``.meta.json`` sidecar, or ``None`` when there is\nnone: an artifact old enough to predate the sidecar still renders."
    }
   },
   "required": [
    "by_pos",
    "fitted_positions",
    "min_rows",
    "missing",
    "saved_at"
   ],
   "title": "CalibrationHealth",
   "type": "object"
  },
  "CategoryMetrics": {
   "properties": {
    "mae": {
     "description": "Mean absolute error on this category's held-out rows.",
     "type": "number"
    },
    "n": {
     "description": "How many held-out rows the category was scored over.",
     "type": "integer"
    },
    "rmse": {
     "description": "Root-mean-square error on this category's held-out rows.",
     "type": "number"
    }
   },
   "required": [
    "mae",
    "n",
    "rmse"
   ],
   "title": "CategoryMetrics",
   "type": "object"
  },
  "ChipPlan": {
   "properties": {
    "chips": {
     "description": "One row per chip, best gain first.",
     "items": {
      "$ref": "#/definitions/ChipPlanRow"
     },
     "type": "array"
    },
    "gw": {
     "description": "The gameweek the plan was solved from.",
     "type": "integer"
    }
   },
   "required": [
    "chips",
    "gw"
   ],
   "title": "ChipPlan",
   "type": "object"
  },
  "ChipPlanRow": {
   "properties": {
    "best_gain": {
     "description": "The total gain at ``best_gw``.",
     "type": "number"
    },
    "best_gain_per_week": {
     "description": "The per-week gain at ``best_gw`` \u2014 the figure ``best_gw`` is chosen on.",
     "type": "number"
    },
    "best_gw": {
     "description": "The gameweek with the best ``per_week`` gain \u2014 not necessarily the\nbest total (``optimize.chips.chip_plan``).",
     "type": "integer"
    },
    "chip": {
     "description": "The chip this row is about: wildcard, bench boost, free hit or triple\ncaptain.",
     "type": "string"
    },
    "now_gain": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "description": "The gain if played this gameweek, or ``None`` when it cannot be\nplayed now."
    },
    "play_now": {
     "anyOf": [
      {
       "type": "boolean"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Whether ``now_gain`` clears ``threshold_now`` \u2014 the chip policy's own\nverdict on playing this week rather than waiting."
    },
    "play_now_delta": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "description": "``now_gain`` minus ``best_gain`` \u2014 the total-points price of playing\nnow rather than waiting, or ``None`` when it cannot be played now."
    },
    "thetas": {
     "default": [],
     "description": "\u03b8 per week, aligned by index with ``weeks``. Built at the router by\nlooping the same ``(chip, gw) -> float`` callable, because putting it in\n``chip_plan``'s week rows would be an ``optimize/**`` edit for a display\nfield (plan A9).",
     "items": {
      "type": "number"
     },
     "type": "array"
    },
    "threshold_now": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "\u03b8 for this chip in the current gameweek: the surplus the best remaining\nweek is expected to offer. ``chip_plan`` has always computed it and this\nmodel has never declared it, so until v10b it was computed and dropped \u2014\nthe ``odds_blend_weight`` failure, repeated. An undeclared field never\nreaches the page and nothing fails while it doesn't."
    },
    "threshold_source": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "See ``ChipWorkbenchRow.threshold_source``. Filled at the router from the\nsame lookup ``thetas`` is built from (v12 W3 \u00a74.2)."
    },
    "weeks": {
     "description": "Every horizon gameweek this chip could be played in, with its gain.",
     "items": {
      "$ref": "#/definitions/ChipWeek"
     },
     "type": "array"
    },
    "weeks_scored": {
     "description": "How many gameweeks were looked at, so the UI can say how far ahead\n\"best\" reaches rather than implying the whole season.",
     "type": "integer"
    },
    "window": {
     "default": [],
     "description": "``[from_gw, last_gw]`` from ``chip_policy.chip_windows``. Note the first\nelement is the gameweek asked about, not the window's opening \u2014 the UI says\n\"expires after GW19\" and never \"window starts at\".",
     "items": {
      "type": "integer"
     },
     "type": "array"
    }
   },
   "required": [
    "best_gain",
    "best_gain_per_week",
    "best_gw",
    "chip",
    "now_gain",
    "play_now",
    "play_now_delta",
    "thetas",
    "threshold_now",
    "threshold_source",
    "weeks",
    "weeks_scored",
    "window"
   ],
   "title": "ChipPlanRow",
   "type": "object"
  },
  "ChipWeek": {
   "properties": {
    "gain": {
     "description": "Total expected-points gain from playing the chip in ``gw``, from\n``optimize/chips.py``'s ``evaluate_chips``.",
     "type": "number"
    },
    "gw": {
     "description": "A gameweek the chip could be played in.",
     "type": "integer"
    },
    "per_week": {
     "description": "``gain`` divided by the horizon weeks the chip is credited with \u2014 the\nweeks from ``gw`` onwards for a wildcard, one for every other chip.",
     "type": "number"
    }
   },
   "required": [
    "gain",
    "gw",
    "per_week"
   ],
   "title": "ChipWeek",
   "type": "object"
  },
  "ChipWorkbenchRow": {
   "description": "One (chip, gameweek) cell of the advice run's own chip table.\n\n``threshold`` is the \u03b8 bar that week \u2014 the surplus the best remaining week\nis expected to offer \u2014 so the workbench can draw the gain against the bar\nrather than against an arbitrary axis. Both it and ``play_now`` are\noptional because an advice payload written before the chip policy landed\ncarries neither.",
   "properties": {
    "chip": {
     "description": "The chip this cell prices: wildcard, bench boost, free hit or triple\ncaptain.",
     "type": "string"
    },
    "gain": {
     "description": "Expected-points gain from playing the chip in this cell, as the\nadvice run's own chip table priced it.",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek this cell is for.",
     "type": "integer"
    },
    "gw2": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The second week of a chip *pair* \u2014 the bench boost's, where ``gw`` is\nthe wildcard's. ``None`` on every single-chip row, which is every row on\nevery payload written before v12 and every row until the fixture list\ncarries a double."
    },
    "note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "A caveat on this cell, when the advice run's chip table wrote one."
    },
    "per_week": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``gain`` divided by the weeks it is credited with."
    },
    "play_now": {
     "default": false,
     "description": "Whether ``gain`` clears ``threshold`` this cell.",
     "type": "boolean"
    },
    "threshold": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The \u03b8 bar this gain is judged against \u2014 see the class docstring."
    },
    "threshold_source": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Where ``threshold`` came from: ``\"theta\"``, or ``\"flat: <reason>\"``.\n\nThree distinct fallbacks produce a flat bar and they are not the same\nnews \u2014 no asset, no surplus for this chip, a gameweek outside the\ncalibrated window \u2014 so the reason travels with the number rather than\nbeing guessed at from it. ``None`` on a payload written before v12\n(v12 W3 \u00a74.2)."
    }
   },
   "required": [
    "chip",
    "gain",
    "gw",
    "gw2",
    "note",
    "per_week",
    "play_now",
    "threshold",
    "threshold_source"
   ],
   "title": "ChipWorkbenchRow",
   "type": "object"
  },
  "ChipsWorkbench": {
   "properties": {
    "chips": {
     "description": "Every (chip, gameweek) cell in the advice run's own chip table.",
     "items": {
      "$ref": "#/definitions/ChipWorkbenchRow"
     },
     "type": "array"
    },
    "gw": {
     "description": "The gameweek the workbench is showing.",
     "type": "integer"
    },
    "wildcard": {
     "anyOf": [
      {
       "$ref": "#/definitions/SquadDiff"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The wildcard candidate squad against the owned one, or ``None`` when\nthe advice payload carries no ``wildcard_now``."
    }
   },
   "required": [
    "chips",
    "gw",
    "wildcard"
   ],
   "title": "ChipsWorkbench",
   "type": "object"
  },
  "Component": {
   "properties": {
    "label": {
     "description": "The scoring category this term covers (e.g. minutes, goals, saves).",
     "type": "string"
    },
    "points": {
     "description": "Its contribution to ``ep`` for this fixture, summed over the\ncategory's columns.",
     "type": "number"
    }
   },
   "required": [
    "label",
    "points"
   ],
   "title": "Component",
   "type": "object"
  },
  "ComponentFixture": {
   "description": "One player-fixture's additive terms.\n\nDeliberately shaped like :class:`FixtureExplain` (the explain modal's\nper-fixture row) without being it: this one is read from the saved\ncomponents parquet with no model loading at all, and carries only what a\nwhy-panel renders.",
   "properties": {
    "components": {
     "description": "The scoring categories that summed to ``ep``, from the saved\ncomponents parquet.",
     "items": {
      "$ref": "#/definitions/Component"
     },
     "type": "array"
    },
    "ep": {
     "description": "Expected points for this fixture, the sum ``components`` explains.",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek of this fixture.",
     "type": "integer"
    },
    "home": {
     "description": "Whether the fixture is at home.",
     "type": "boolean"
    },
    "kickoff_time": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "description": "ISO kickoff time, or ``None`` when not yet known."
    },
    "minutes": {
     "$ref": "#/definitions/MinutesOutput",
     "description": "The minutes model's own prediction for this fixture."
    },
    "opponent": {
     "description": "The opponent's name.",
     "type": "string"
    },
    "pen_taker": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "How much of the Goals term is penalty duty, when any of it is.\n\nNot a component: the increment was folded into ``e_goals`` before\n``assemble_ep`` ran, so it is already inside ``components``' Goals row and\nlisting it beside them would stop them summing to ``ep``. It rides along\nas an annotation the panel prints under Goals, and is ``None`` \u2014 not 0.0 \u2014\nfor the great majority of rows that have no penalty duty at all, so the\npanel can tell \"no term\" from \"a term that rounded to zero\"."
    }
   },
   "required": [
    "components",
    "ep",
    "gw",
    "home",
    "kickoff_time",
    "minutes",
    "opponent",
    "pen_taker"
   ],
   "title": "ComponentFixture",
   "type": "object"
  },
  "ComponentPlayer": {
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "ep": {
     "description": "Summed over every fixture in this payload \u2014 a horizon total, not a\ngameweek's, because the components parquet carries the whole solve\nhorizon.",
     "type": "number"
    },
    "ep_gw": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Expected points for the *requested* gameweek alone.\n\n``ep`` above is a horizon sum \u2014 the components parquet carries every\ngameweek in the solve horizon \u2014 so it is not a number the \u03c3 table has ever\nseen. The band brackets this one instead (plan A2)."
    },
    "ep_hi": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "p75 of ``ep_gw``'s distribution, same contract as ``ep_lo``."
    },
    "ep_lo": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "p25 / p75 of the distribution ``noise_ep`` draws from. ``None``, never\nzero, when the frame carries no minutes model for him."
    },
    "fixtures": {
     "description": "Every horizon fixture's breakdown from the saved components parquet.",
     "items": {
      "$ref": "#/definitions/ComponentFixture"
     },
     "type": "array"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "p_blank": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``P(points <= 2)`` under the same distribution."
    },
    "p_haul": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``uncertainty.Band.p_haul``: P(total points >= 10) in the tail of the\nwhole forecast. The advice payload's attacking quantity is a different\nnumber on a different scale and is served as ``p_attacking_haul``\n(spec D3)."
    },
    "position": {
     "description": "GKP, DEF, MID or FWD.",
     "type": "string"
    },
    "sigma": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The scenario sweep's own \u03c3 for this player-gameweek, in points."
    },
    "team_name": {
     "description": "His club's name.",
     "type": "string"
    }
   },
   "required": [
    "code",
    "ep",
    "ep_gw",
    "ep_hi",
    "ep_lo",
    "fixtures",
    "name",
    "p_blank",
    "p_haul",
    "position",
    "sigma",
    "team_name"
   ],
   "title": "ComponentPlayer",
   "type": "object"
  },
  "ComponentsBreakdown": {
   "properties": {
    "gw": {
     "description": "The gameweek this breakdown was requested for.",
     "type": "integer"
    },
    "players": {
     "description": "Every candidate's EP decomposition for this gameweek.",
     "items": {
      "$ref": "#/definitions/ComponentPlayer"
     },
     "type": "array"
    }
   },
   "required": [
    "gw",
    "players"
   ],
   "title": "ComponentsBreakdown",
   "type": "object"
  },
  "ConfidenceData": {
   "properties": {
    "captain": {
     "$ref": "#/definitions/ConfidenceTier",
     "description": "The captaincy record's own tier and counts \u2014 the only lane the card\nreports today."
    }
   },
   "required": [
    "captain"
   ],
   "title": "ConfidenceData",
   "type": "object"
  },
  "ConfidenceTier": {
   "description": "One record-derived claim, with the counts that back it.\n\n``text`` is the whole product \u2014 a sentence quoting counts. The counts are\ncarried beside it so a caller can style the tier without re-parsing prose,\nnever so it can compute a rate: the absence of a percentage anywhere in\nthis model is the point of it (spec D3).",
   "properties": {
    "aligned": {
     "default": 0,
     "description": "Weeks I took the model's own captain \u2014 reviewed, but not graded, since\nthere is nothing to compare.",
     "type": "integer"
    },
    "graded": {
     "default": 0,
     "description": "Reviewed gameweeks where the lane was actually comparable. The gap\nbetween this and ``reviewed`` is the weeks the model's captain was not in\nthe eleven, which is not evidence either way.",
     "type": "integer"
    },
    "losses": {
     "default": 0,
     "description": "Graded weeks mine beat the model's.",
     "type": "integer"
    },
    "reviewed": {
     "default": 0,
     "description": "Gameweeks with a captaincy lane worth the name \u2014 excludes weeks the\nadvice was pruned to nothing.",
     "type": "integer"
    },
    "text": {
     "default": "",
     "description": "The one sentence quoting these counts, composed by\n``captain_confidence``.",
     "type": "string"
    },
    "tier": {
     "default": "early",
     "description": "How strong the record is, from ``gaffer.confidence.captain_confidence``:\n``\"early\"`` below the graded floor, else split on the win rate.",
     "enum": [
      "early",
      "mixed",
      "backed"
     ],
     "type": "string"
    },
    "wins": {
     "default": 0,
     "description": "Graded weeks the model's captain beat mine.",
     "type": "integer"
    }
   },
   "required": [
    "aligned",
    "graded",
    "losses",
    "reviewed",
    "text",
    "tier",
    "wins"
   ],
   "title": "ConfidenceTier",
   "type": "object"
  },
  "CoreInsightsHealth": {
   "properties": {
    "collected": {
     "description": "Whether any of the collector's tables exist for ``season``.",
     "type": "boolean"
    },
    "season": {
     "description": "The season the collector fetches for, from config or its default.",
     "type": "string"
    },
    "tables": {
     "description": "Row counts and latest date per table, when ``collected`` is true;\nempty otherwise.",
     "items": {
      "$ref": "#/definitions/CoreInsightsTable"
     },
     "type": "array"
    },
    "waiting_for": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What has to happen before these numbers mean anything, or ``None`` when\nthey already do. Spec \u00a71: a view whose data does not exist yet says what\nit is waiting for and never renders zeros as if they were measurements."
    }
   },
   "required": [
    "collected",
    "season",
    "tables",
    "waiting_for"
   ],
   "title": "CoreInsightsHealth",
   "type": "object"
  },
  "CoreInsightsTable": {
   "properties": {
    "latest": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Newest kickoff date in the table, ``YYYY-MM-DD``, or ``None`` when the\ntable has no dated rows. A table with rows and no date is possible \u2014 the\nplayer table is keyed on gameweek, not on a timestamp \u2014 and reads as its\nhighest gameweek instead."
    },
    "rows": {
     "description": "How many rows the collector has banked for it.",
     "type": "integer"
    },
    "table": {
     "description": "The core-insights table's name.",
     "type": "string"
    }
   },
   "required": [
    "latest",
    "rows",
    "table"
   ],
   "title": "CoreInsightsTable",
   "type": "object"
  },
  "CurrentEvaluation": {
   "properties": {
    "baselines": {
     "additionalProperties": {
      "additionalProperties": {
       "$ref": "#/definitions/CategoryMetrics"
      },
      "type": "object"
     },
     "description": "The same stratified metrics for each naive baseline, for comparison.",
     "type": "object"
    },
    "git_sha": {
     "description": "The commit the evaluated model was built from.",
     "type": "string"
    },
    "heads": {
     "additionalProperties": {
      "$ref": "#/definitions/HeadMetrics"
     },
     "description": "Per-head calibration: ``p_play``, ``p60`` and the rest.",
     "type": "object"
    },
    "holdout_slots": {
     "description": "How many of the newest gameweek slots were held out and scored.",
     "type": "integer"
    },
    "run_at": {
     "description": "When this evaluation was run.",
     "type": "string"
    },
    "stratified": {
     "additionalProperties": {
      "additionalProperties": {
       "$ref": "#/definitions/CategoryMetrics"
      },
      "type": "object"
     },
     "description": "cut (\"all\" / \"starters\") -> return category -> metrics.",
     "type": "object"
    }
   },
   "required": [
    "baselines",
    "git_sha",
    "heads",
    "holdout_slots",
    "run_at",
    "stratified"
   ],
   "title": "CurrentEvaluation",
   "type": "object"
  },
  "DecisionGrade": {
   "properties": {
    "delta_pts": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "My points minus the model's on the transfers lane, from the review\nledger."
    },
    "label": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The review ledger's own label for how the lane graded."
    },
    "lane": {
     "default": "transfers",
     "description": "Always ``\"transfers\"``: the one review lane a deviation note grades\nagainst.",
     "type": "string"
    }
   },
   "required": [
    "delta_pts",
    "label",
    "lane"
   ],
   "title": "DecisionGrade",
   "type": "object"
  },
  "DecisionNote": {
   "description": "v16 \u00a75: one gameweek's deviation note and whether it may be edited.",
   "properties": {
    "at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "ISO timestamp the note was saved."
    },
    "deadline": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The gameweek's deadline, from the advice payload or the events\nsnapshot."
    },
    "grade": {
     "anyOf": [
      {
       "$ref": "#/definitions/DecisionGrade"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The transfers lane's grade, once the gameweek has been reviewed."
    },
    "gw": {
     "description": "The gameweek the note is for.",
     "type": "integer"
    },
    "reason": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "One of ``decisions.REASONS`` \u2014 why the manager deviated, or ``None``\nwhen no note has been saved."
    },
    "state": {
     "default": "open",
     "description": "Whether the note may still be written: not yet open, open, or closed\nbecause the gameweek has been graded (``decisions.note_state``).",
     "enum": [
      "before_deadline",
      "open",
      "graded"
     ],
     "type": "string"
    },
    "text": {
     "default": "",
     "description": "The manager's own words, up to ``decisions.TEXT_MAX`` characters.",
     "type": "string"
    }
   },
   "required": [
    "at",
    "deadline",
    "grade",
    "gw",
    "reason",
    "state",
    "text"
   ],
   "title": "DecisionNote",
   "type": "object"
  },
  "DecisionRef": {
   "description": "The deviation note beside a grade (v16 \u00a75).",
   "properties": {
    "at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "ISO timestamp the note was saved."
    },
    "reason": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The reason code the manager gave for deviating."
    },
    "text": {
     "default": "",
     "description": "The manager's own words.",
     "type": "string"
    }
   },
   "required": [
    "at",
    "reason",
    "text"
   ],
   "title": "DecisionRef",
   "type": "object"
  },
  "DecisionWrite": {
   "properties": {
    "reason": {
     "description": "One of ``decisions.REASONS``; refused with ``unknown_reason`` otherwise.",
     "type": "string"
    },
    "text": {
     "default": "",
     "description": "The manager's own words, up to ``decisions.TEXT_MAX`` characters.",
     "type": "string"
    }
   },
   "required": [
    "reason",
    "text"
   ],
   "title": "DecisionWrite",
   "type": "object"
  },
  "DecompositionCell": {
   "properties": {
    "hits": {
     "description": "Hits taken over the replay window.",
     "type": "integer"
    },
    "per_gw": {
     "description": "``total`` divided by the number of gameweeks replayed.",
     "type": "number"
    },
    "total": {
     "description": "Total points over the replay window.",
     "type": "integer"
    }
   },
   "required": [
    "hits",
    "per_gw",
    "total"
   ],
   "title": "DecompositionCell",
   "type": "object"
  },
  "DecompositionData": {
   "properties": {
    "cells": {
     "additionalProperties": {
      "$ref": "#/definitions/DecompositionCell"
     },
     "description": "``{model,oracle}_h{1,3}`` -> that replay's outcome.",
     "type": "object"
    },
    "forecast_gap_h3": {
     "description": "oracle_h3 - model_h3: what better forecasting could still win.",
     "type": "number"
    },
    "git_sha": {
     "description": "The commit the replayed model was built from.",
     "type": "string"
    },
    "planning_ceiling": {
     "description": "oracle_h3 - oracle_h1: the ceiling on multi-week planning.",
     "type": "number"
    },
    "run_at": {
     "description": "When this decomposition was run.",
     "type": "string"
    },
    "season": {
     "description": "The season replayed.",
     "type": "string"
    },
    "start_gw": {
     "description": "The first gameweek of the replay window.",
     "type": "integer"
    }
   },
   "required": [
    "cells",
    "forecast_gap_h3",
    "git_sha",
    "planning_ceiling",
    "run_at",
    "season",
    "start_gw"
   ],
   "title": "DecompositionData",
   "type": "object"
  },
  "Digest": {
   "properties": {
    "error": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Set only on a digest that failed to build. A run that crashes still\nbanks an artifact so the card can say \"Friday's briefing did not build\"\nrather than falling back to the never-run empty state."
    },
    "generated_at": {
     "default": "",
     "description": "ISO timestamp the digest was built.",
     "type": "string"
    },
    "gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The gameweek the digest is about."
    },
    "headline": {
     "description": "The digest's one-line summary \u2014 the brief's first sentence when a\nbrief exists, else a fact composed in `digest.py`.",
     "type": "string"
    },
    "kind": {
     "description": "``\"friday\"`` or ``\"tuesday\"`` \u2014 which of the two banked digests this\nis, from ``DIGEST_KINDS``.",
     "type": "string"
    },
    "sections": {
     "description": "The digest's body, one entry per non-empty section.",
     "items": {
      "$ref": "#/definitions/DigestSection"
     },
     "type": "array"
    }
   },
   "required": [
    "error",
    "generated_at",
    "gw",
    "headline",
    "kind",
    "sections"
   ],
   "title": "Digest",
   "type": "object"
  },
  "DigestPanel": {
   "description": "The newest digest, or a stated absence.\n\n``available`` false covers all three ways there is nothing to show \u2014 never\nrun, deleted, unparseable \u2014 because the card's empty state says the same\nsentence for each of them: press the button, or wait for Friday.",
   "properties": {
    "available": {
     "description": "Whether a digest was found \u2014 the requested kind, or the newer of\nboth when no kind was asked for.",
     "type": "boolean"
    },
    "digest": {
     "anyOf": [
      {
       "$ref": "#/definitions/Digest"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The digest, when ``available`` is true."
    }
   },
   "required": [
    "available",
    "digest"
   ],
   "title": "DigestPanel",
   "type": "object"
  },
  "DigestSection": {
   "description": "One block of a digest. ``bits`` is prose the client joins.\n\nThe DiffStrip idiom: clauses assembled server-side, rendered by joining\nthem, so there is no markdown dependency anywhere in the client. A section\nwith no bits never reaches here \u2014 the builder drops it (plan A5).",
   "properties": {
    "bits": {
     "description": "The section's clauses, joined by the client into prose.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "key": {
     "description": "The section's identifier, distinguishing it for the client's layout.",
     "type": "string"
    },
    "title": {
     "type": "string"
    }
   },
   "required": [
    "bits",
    "key",
    "title"
   ],
   "title": "DigestSection",
   "type": "object"
  },
  "DraftCompare": {
   "properties": {
    "gw": {
     "description": "The gameweek the comparison was solved from.",
     "type": "integer"
    },
    "rows": {
     "description": "The reference row and every requested draft's re-solve.",
     "items": {
      "$ref": "#/definitions/DraftCompareRow"
     },
     "type": "array"
    },
    "weeks": {
     "description": "The shared window every row was scored over \u2014 the shortest plan's\nhorizon.",
     "type": "integer"
    }
   },
   "required": [
    "gw",
    "rows",
    "weeks"
   ],
   "title": "DraftCompare",
   "type": "object"
  },
  "DraftCompareRequest": {
   "properties": {
    "names": {
     "description": "Which saved drafts to compare, up to :data:`MAX_COMPARE`.",
     "items": {
      "type": "string"
     },
     "type": "array"
    }
   },
   "required": [
    "names"
   ],
   "title": "DraftCompareRequest",
   "type": "object"
  },
  "DraftCompareRow": {
   "properties": {
    "buys": {
     "description": "Players this row's plan buys.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "captain": {
     "anyOf": [
      {
       "$ref": "#/definitions/WirePlayerRef"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "chip": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The chip this row's plan plays, if any."
    },
    "delta_xpts": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "This row's ``horizon_pts`` minus the reference row's."
    },
    "error": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why this row is empty. An infeasible draft is a row with a reason, not\na failed comparison."
    },
    "expected_pts": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Expected points for the first week alone, net of hits."
    },
    "hits": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Hits this row's plan takes."
    },
    "horizon": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Gameweeks this row's plan actually covers, which is not always the\ncomparison's. A free hit is a one-week squad; ``DraftCompare.weeks`` is\nthe shorter shared window every row was then *scored* over."
    },
    "horizon_pts": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Expected points summed over the comparison's shared window."
    },
    "is_reference": {
     "default": false,
     "description": "The unconstrained optimum, so every other row has a \"worse than what\".",
     "type": "boolean"
    },
    "name": {
     "description": "The draft's name, or the reference row's own label.",
     "type": "string"
    },
    "sells": {
     "description": "Players this row's plan sells.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "solved_at": {
     "default": "",
     "description": "ISO timestamp this row's re-solve completed.",
     "type": "string"
    }
   },
   "required": [
    "buys",
    "captain",
    "chip",
    "delta_xpts",
    "error",
    "expected_pts",
    "hits",
    "horizon",
    "horizon_pts",
    "is_reference",
    "name",
    "sells",
    "solved_at"
   ],
   "title": "DraftCompareRow",
   "type": "object"
  },
  "DraftList": {
   "properties": {
    "drafts": {
     "description": "Every saved draft.",
     "items": {
      "$ref": "#/definitions/DraftRow"
     },
     "type": "array"
    }
   },
   "required": [
    "drafts"
   ],
   "title": "DraftList",
   "type": "object"
  },
  "DraftRow": {
   "properties": {
    "constraints": {
     "$ref": "#/definitions/WhatIfRequest",
     "description": "The what-if lab request this draft pins."
    },
    "created_at": {
     "default": "",
     "description": "ISO timestamp the draft was saved.",
     "type": "string"
    },
    "name": {
     "description": "The draft's name, as the manager saved it.",
     "type": "string"
    }
   },
   "required": [
    "constraints",
    "created_at",
    "name"
   ],
   "title": "DraftRow",
   "type": "object"
  },
  "DraftSaveRequest": {
   "properties": {
    "constraints": {
     "$ref": "#/definitions/WhatIfRequest",
     "description": "The what-if lab request to pin."
    },
    "name": {
     "description": "The name to save this draft under.",
     "type": "string"
    }
   },
   "required": [
    "constraints",
    "name"
   ],
   "title": "DraftSaveRequest",
   "type": "object"
  },
  "EpMover": {
   "description": "One player the newest retrain moved, in the gameweek being decided.",
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "delta": {
     "description": "``ep_now - ep_prev``, points.",
     "type": "number"
    },
    "ep_now": {
     "description": "Expected points after the newest retrain.",
     "type": "number"
    },
    "ep_prev": {
     "description": "Expected points before the newest retrain.",
     "type": "number"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    }
   },
   "required": [
    "code",
    "delta",
    "ep_now",
    "ep_prev",
    "name"
   ],
   "title": "EpMover",
   "type": "object"
  },
  "FieldRank": {
   "description": "v12 W4 \u00a75.3. One gameweek against a synthetic field drawn from EO.\n\nThree headline numbers and two of them are ``None`` today. Each null has\nits own sentence rather than a shared one, because they are waiting for\ndifferent things: ``p_green`` for a banked field sample, ``p_top10k`` for\na score series that does not exist anywhere, ``rank_slope`` for graded\ngameweeks. Spec \u00a71: a view whose data does not exist says what it is\nwaiting for and never renders a zero as a measurement.",
   "properties": {
    "eo_gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Which gameweek's sample the EO came from, or ``None`` when none did.\n\nNot :attr:`gw`. The field sample for plan gameweek N is banked under\nN-1 \u2014 picks 404 before a deadline, so the scrape reads the last scored\nweek \u2014 and \u00a73.3's ``deadline_eo`` extrapolates it one gameweek forward.\nTwo different gameweek numbers in one payload is exactly the kind of\nthing a reader has to be told rather than left to infer."
    },
    "eo_source": {
     "description": "``\"deadline-trend\"`` (\u00a73.3's extrapolation), ``\"last-sample\"`` (the\nnewest scrape), or ``\"none\"``. A trend EO and a last-sample EO are\ndifferent numbers and the panel says which it used.",
     "type": "string"
    },
    "field_draws": {
     "default": 1,
     "description": "Independent field populations the headline was averaged over\n(:data:`gaffer.league_sim.FIELD_DRAWS`). Provenance, like ``n`` and\n``seed``: which three hundred managers were drawn is a source of noise in\nits own right.",
     "type": "integer"
    },
    "field_median_ep": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The synthetic field's median expected points this gameweek, when\navailable."
    },
    "gw": {
     "description": "The gameweek this field ranking is for.",
     "type": "integer"
    },
    "managers": {
     "description": "How many sampled entries the effective-ownership table was built\nfrom.",
     "type": "integer"
    },
    "my_ep": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The manager's own expected points this gameweek, when available."
    },
    "n": {
     "description": "How many synthetic field entries were simulated.",
     "type": "integer"
    },
    "p_green": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "P(the manager's squad beats the median of the synthetic field), when\na field sample is banked."
    },
    "p_top10k": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "P(finishes in the overall top 10k), when a score series exists to\ncompute it from."
    },
    "rank_slope": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Overall-rank places per point, from the graded ledger. Negative: more\npoints is a better (smaller) rank."
    },
    "rank_slope_rows": {
     "default": 0,
     "description": "How many graded gameweeks ``rank_slope`` was fitted over.",
     "type": "integer"
    },
    "rank_waiting_for": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What has to happen before ``rank_slope`` can be computed, when it\ncannot be."
    },
    "seed": {
     "description": "The RNG seed the field sample was drawn with.",
     "type": "integer"
    },
    "top10k_waiting_for": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What has to happen before ``p_top10k`` can be computed, when it\ncannot be."
    },
    "unsampled_picks": {
     "default": 0,
     "description": "Players in my squad the field sample never saw.\n\n``eo_from_picks`` omits anyone no sampled entry started, so a genuine\ndifferential is routinely absent from the EO table. He is simulated at\nownership 0.0 \u2014 nobody in the field has him \u2014 and counted here so the\npanel can say how much of my week the sample cannot speak to.",
     "type": "integer"
    },
    "waiting_for": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What has to happen before ``p_green`` can be computed, when it\ncannot be."
    }
   },
   "required": [
    "eo_gw",
    "eo_source",
    "field_draws",
    "field_median_ep",
    "gw",
    "managers",
    "my_ep",
    "n",
    "p_green",
    "p_top10k",
    "rank_slope",
    "rank_slope_rows",
    "rank_waiting_for",
    "seed",
    "top10k_waiting_for",
    "unsampled_picks",
    "waiting_for"
   ],
   "title": "FieldRank",
   "type": "object"
  },
  "FixtureExplain": {
   "properties": {
    "calibration_delta": {
     "description": "The fitted EP calibration's adjustment for this position, 0.0 when\nthe frame carries no calibration column.",
     "type": "number"
    },
    "components": {
     "description": "The scoring categories that summed to ``ep``, each with nonzero\ncolumns.",
     "items": {
      "$ref": "#/definitions/Component"
     },
     "type": "array"
    },
    "ep": {
     "description": "Expected points for this fixture, the sum ``components`` explains.",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek this fixture falls in.",
     "type": "integer"
    },
    "home": {
     "description": "Whether the fixture is at home.",
     "type": "boolean"
    },
    "kickoff_time": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "description": "ISO kickoff time, or ``None`` when not yet known."
    },
    "minutes": {
     "$ref": "#/definitions/MinutesOutput",
     "description": "The minutes model's own prediction for this fixture."
    },
    "odds": {
     "$ref": "#/definitions/OddsInfluence",
     "description": "How the bookmaker odds influenced this fixture's team numbers."
    },
    "opponent": {
     "description": "The opponent's name.",
     "type": "string"
    }
   },
   "required": [
    "calibration_delta",
    "components",
    "ep",
    "gw",
    "home",
    "kickoff_time",
    "minutes",
    "odds",
    "opponent"
   ],
   "title": "FixtureExplain",
   "type": "object"
  },
  "FixtureMatrixData": {
   "properties": {
    "gws": {
     "description": "Every gameweek rated.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "source": {
     "description": "Whether the ratings came from the Dixon-Coles model or there was\nnothing to rate.",
     "enum": [
      "dixon_coles",
      "none"
     ],
     "type": "string"
    },
    "teams": {
     "description": "Every club's attack and defence ratings.",
     "items": {
      "$ref": "#/definitions/MatrixTeam"
     },
     "type": "array"
    }
   },
   "required": [
    "gws",
    "source",
    "teams"
   ],
   "title": "FixtureMatrixData",
   "type": "object"
  },
  "FixtureOutlook": {
   "description": "Doubles and blanks in the season ahead (v10b \u00a7F2a).\n\nEvery failure is a 200 with a ``note`` rather than an error: this renders\nas one card beside populated cards, and a 422 there is indistinguishable\nfrom a broken endpoint.",
   "properties": {
    "from_gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The first gameweek in ``weeks``, or ``None`` when there is nothing to\nshow."
    },
    "has_blanks": {
     "default": false,
     "description": "A claim about the **served slice**, not the season: both flags are\ncomputed over the same ``weeks`` this response carries, so a ``from_gw``\nnarrows them together with the rows.\n\nDeclared rather than derived on the client, for the reason v9d's\n``available`` exists: the empty state is the common case for months, and a\nclient branching on ``weeks.every(w => !w.doubles.length)`` is a client\nthat will one day branch on ``weeks.length`` by mistake.",
     "type": "boolean"
    },
    "has_doubles": {
     "default": false,
     "description": "Whether any served week has a double gameweek.",
     "type": "boolean"
    },
    "note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why the card is empty, when it is \u2014 a missing fixture list, an\nunreadable one, or nothing to report."
    },
    "teams_known": {
     "default": false,
     "description": "False when the teams snapshot was unreadable and the codes above are\nraw team ids. The counts hold; the names do not.",
     "type": "boolean"
    },
    "weeks": {
     "description": "The season's remaining gameweeks, doubles and blanks per week.",
     "items": {
      "$ref": "#/definitions/OutlookWeek"
     },
     "type": "array"
    }
   },
   "required": [
    "from_gw",
    "has_blanks",
    "has_doubles",
    "note",
    "teams_known",
    "weeks"
   ],
   "title": "FixtureOutlook",
   "type": "object"
  },
  "FlagChange": {
   "description": "One (gameweek, player) whose status moved before the deadline.",
   "properties": {
    "chance_of_playing": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "FPL's own chance-of-playing percentage at the final status."
    },
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "final_status": {
     "description": "The last status recorded **before** the deadline. A snapshot taken\nafterwards told nobody anything and is not in this window.",
     "type": "string"
    },
    "first_change": {
     "description": "The snapshot day the status first differed from what it had been \u2014\nthe first day a manager could have acted, not the last.",
     "type": "string"
    },
    "from_status": {
     "description": "His status immediately before it changed.",
     "type": "string"
    },
    "gw": {
     "description": "The gameweek the deadline belongs to.",
     "type": "integer"
    },
    "lead_days": {
     "description": "Days between ``first_change`` and the deadline.",
     "type": "number"
    },
    "started": {
     "description": "Whether he actually started the fixture.",
     "type": "boolean"
    }
   },
   "required": [
    "chance_of_playing",
    "code",
    "final_status",
    "first_change",
    "from_status",
    "gw",
    "lead_days",
    "started"
   ],
   "title": "FlagChange",
   "type": "object"
  },
  "FlagLatencyData": {
   "description": "v12 \u00a73.1's readout, or its refusal.\n\n``available`` is what the card branches on, and ``note`` is the sentence\nit prints when the answer is no. Both are on the payload rather than in\nthe page because the CLI prints the same sentence, and two copies of an\nempty state drift.\n\nThe scorer's ``changes`` \u2014 every status move it found, one row each \u2014 is\n**deliberately not declared here**, so pydantic drops it on the way out.\nIt is the evidence behind the histogram and it stays in the artifact for\nanyone who wants their own bands over it, but nothing on the page reads\nit and it is the only field on this payload that grows without bound: one\nrow per player per move per gameweek, all season. ``rows`` carries the\ncount that the page does show.",
   "properties": {
    "available": {
     "default": false,
     "description": "Whether the gate is open: enough snapshot days banked and at least\none covered gameweek graded.",
     "type": "boolean"
    },
    "checked_covered_gws": {
     "default": [],
     "description": "Of those, the ones that have also been graded.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "covered_gws": {
     "default": [],
     "description": "Gameweeks with at least one pre-deadline snapshot.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "git_sha": {
     "description": "The commit the readout was built from.",
     "type": "string"
    },
    "histogram": {
     "default": [],
     "description": "Lead-time distribution of status changes, split by outcome.",
     "items": {
      "$ref": "#/definitions/LeadBucket"
     },
     "type": "array"
    },
    "late_flags": {
     "default": [],
     "description": "Every status change scored, one row per (gameweek, player).",
     "items": {
      "$ref": "#/definitions/FlagChange"
     },
     "type": "array"
    },
    "min_snap_dates": {
     "default": 14,
     "description": "The floor ``snap_dates`` must clear for the gate to open\n(``availability_eval.MIN_SNAP_DATES``).",
     "type": "integer"
    },
    "note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why the report is empty, when the gate is shut."
    },
    "rows": {
     "default": 0,
     "description": "How many status changes the histogram and table are built from.",
     "type": "integer"
    },
    "run_at": {
     "description": "When this readout was built.",
     "type": "string"
    },
    "snap_dates": {
     "default": 0,
     "description": "Distinct snapshot days banked in the log.",
     "type": "integer"
    }
   },
   "required": [
    "available",
    "checked_covered_gws",
    "covered_gws",
    "git_sha",
    "histogram",
    "late_flags",
    "min_snap_dates",
    "note",
    "rows",
    "run_at",
    "snap_dates"
   ],
   "title": "FlagLatencyData",
   "type": "object"
  },
  "Freshness": {
   "properties": {
    "rows": {
     "description": "One row per standing job (v12 W1 \u00a72.9, v19a \u00a72.2).",
     "items": {
      "$ref": "#/definitions/FreshnessRow"
     },
     "type": "array"
    }
   },
   "required": [
    "rows"
   ],
   "title": "Freshness",
   "type": "object"
  },
  "FreshnessRow": {
   "properties": {
    "age_hours": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Hours since the file was written, or ``None`` for \"never\".\n\nNever 0.0 for an absent file. Zero is \"just now\", which is the strongest\nclaim this row can make and the exact opposite of what an absent file\nmeans. The client colours on ``None`` first and on the number second."
    },
    "cadence_hours": {
     "description": "How often the job that writes this file is meant to run.\n\nv19a \u00a72.2: the strip used to colour on absolute hours, which called a\n90-hour-old nightly price bank fresh and a 90-hour-old weekly advice\nstale \u2014 both backwards. Served rather than hard-coded on the client so\nthe rule lives beside the schedule it describes.",
     "type": "number"
    },
    "modified_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "When the file was last written, or ``None`` when it never was."
    },
    "path": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What was actually stat'd, so a surprising age is diagnosable."
    },
    "source": {
     "description": "Which of the seven standing jobs this row reports on.",
     "enum": [
      "refresh",
      "odds",
      "field",
      "advise",
      "backup",
      "prices",
      "snapshot"
     ],
     "type": "string"
    }
   },
   "required": [
    "age_hours",
    "cadence_hours",
    "modified_at",
    "path",
    "source"
   ],
   "title": "FreshnessRow",
   "type": "object"
  },
  "GapPoint": {
   "properties": {
    "gap": {
     "description": "Your total minus the leader's, negative when you are behind.",
     "type": "integer"
    },
    "gw": {
     "description": "The gameweek this gap is measured at.",
     "type": "integer"
    }
   },
   "required": [
    "gap",
    "gw"
   ],
   "title": "GapPoint",
   "type": "object"
  },
  "GwPoint": {
   "properties": {
    "gw": {
     "description": "The gameweek this point is for.",
     "type": "integer"
    },
    "points": {
     "description": "Points scored that gameweek.",
     "type": "integer"
    },
    "total": {
     "description": "Running total through that gameweek.",
     "type": "integer"
    }
   },
   "required": [
    "gw",
    "points",
    "total"
   ],
   "title": "GwPoint",
   "type": "object"
  },
  "HeadMetrics": {
   "properties": {
    "log_loss": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "description": "``None`` for a head with nothing to score \u2014 see\n:func:`gaffer.evaluation.head_metrics`. Nullable rather than NaN because\nNaN is not JSON."
    },
    "reliability": {
     "description": "The head's predicted-vs-observed calibration curve, binned.",
     "items": {
      "$ref": "#/definitions/ReliabilityBin"
     },
     "type": "array"
    }
   },
   "required": [
    "log_loss",
    "reliability"
   ],
   "title": "HeadMetrics",
   "type": "object"
  },
  "HistoryRun": {
   "properties": {
    "actual_pts": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "description": "XI points as actually scored, captain doubled, no autosubs, once the\ngameweek has results; ``None`` before it does."
    },
    "buys": {
     "description": "Names of players bought.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "captain": {
     "description": "The captain's name, as advised.",
     "type": "string"
    },
    "deadline": {
     "description": "That gameweek's deadline.",
     "type": "string"
    },
    "expected_pts": {
     "description": "Expected points at the time of the run.",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek this run advised for.",
     "type": "integer"
    },
    "hits": {
     "description": "Hits taken.",
     "type": "integer"
    },
    "sells": {
     "description": "Names of players sold.",
     "items": {
      "type": "string"
     },
     "type": "array"
    }
   },
   "required": [
    "actual_pts",
    "buys",
    "captain",
    "deadline",
    "expected_pts",
    "gw",
    "hits",
    "sells"
   ],
   "title": "HistoryRun",
   "type": "object"
  },
  "JobAccepted": {
   "properties": {
    "job_id": {
     "description": "The v6 ``JobRegistry`` id the caller polls at ``GET /api/jobs/{job_id}``.",
     "type": "string"
    }
   },
   "required": [
    "job_id"
   ],
   "title": "JobAccepted",
   "type": "object"
  },
  "JobHealth": {
   "description": "One ``scripts/com.gaffer.*.plist``, read off disk.\n\nv19a \u00a72.3: the Health tab knew when the *advise* log last moved and\nnothing at all about the other eight jobs, so a launchd agent that had\nstopped being loaded was invisible until a number on another page went\nquietly stale.",
   "properties": {
    "age_hours": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "description": "Hours since the last run, or ``None`` for \"never\" \u2014 never 0.0 for an\nabsent log, for ``FreshnessRow.age_hours``' reason."
    },
    "interval_hours": {
     "description": "Hours between scheduled runs, from the calendar entries.",
     "type": "number"
    },
    "label": {
     "description": "The plist's ``Label`` minus the ``com.gaffer.`` prefix.",
     "type": "string"
    },
    "log": {
     "description": "The ``logs/<x>.log`` the plist redirects into, or \"\" when it names\nnone. This is the file whose mtime the age below is measured from.",
     "type": "string"
    },
    "modified_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "description": "When that log was last written, or ``None`` when it never was."
    },
    "overdue": {
     "description": "Has it missed a run? Half an interval of grace, because a job that\ntakes a minute to start should not alarm the page that watches it.",
     "type": "boolean"
    },
    "schedule": {
     "description": "When it is meant to run, as a sentence \u2014 \"Thu 18:00\", \"daily 23:15\".",
     "type": "string"
    }
   },
   "required": [
    "age_hours",
    "interval_hours",
    "label",
    "log",
    "modified_at",
    "overdue",
    "schedule"
   ],
   "title": "JobHealth",
   "type": "object"
  },
  "JobRunView": {
   "properties": {
    "error": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The failure reason, set only when ``status`` is ``\"failed\"``."
    },
    "finished_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "ISO timestamp the run ended, or ``None`` while it is still going."
    },
    "id": {
     "description": "The run's id, as minted by ``JobRunner.start``.",
     "type": "string"
    },
    "kind": {
     "description": "Which of the runner's four named kinds this run is.",
     "type": "string"
    },
    "line_count": {
     "description": "Total stdout lines captured so far, including any dropped from the\n500-line ring buffer (``JobRun.first_line_index + len(lines)``).",
     "type": "integer"
    },
    "started_at": {
     "description": "ISO timestamp the run began, from ``JobRun.started_at``.",
     "type": "string"
    },
    "status": {
     "description": "The run's lifecycle state, from ``JobRun.status`` in `web/jobs.py`.",
     "enum": [
      "queued",
      "running",
      "done",
      "failed"
     ],
     "type": "string"
    },
    "summary": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The one-line result the job wrote for itself on success."
    }
   },
   "required": [
    "error",
    "finished_at",
    "id",
    "kind",
    "line_count",
    "started_at",
    "status",
    "summary"
   ],
   "title": "JobRunView",
   "type": "object"
  },
  "JobStarted": {
   "description": "The v7 runner's accept body. ``JobAccepted`` above still serves the v6\nqueue endpoints, whose clients read only ``job_id``.",
   "properties": {
    "job_id": {
     "description": "The v7 ``JobRunner`` id, minted by ``JobRunner.start`` in `web/jobs.py`.",
     "type": "string"
    },
    "kind": {
     "description": "The job kind the caller posted to, echoed back from the path parameter.",
     "type": "string"
    }
   },
   "required": [
    "job_id",
    "kind"
   ],
   "title": "JobStarted",
   "type": "object"
  },
  "JournalData": {
   "properties": {
    "built_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "ISO timestamp the journal was built."
    },
    "cumulative": {
     "description": "The running totals behind the journal's chart.",
     "items": {
      "$ref": "#/definitions/JournalPoint"
     },
     "type": "array"
    },
    "rows": {
     "description": "One row per gameweek compared, newest first.",
     "items": {
      "$ref": "#/definitions/JournalRow"
     },
     "type": "array"
    }
   },
   "required": [
    "built_at",
    "cumulative",
    "rows"
   ],
   "title": "JournalData",
   "type": "object"
  },
  "JournalPoint": {
   "properties": {
    "actual": {
     "description": "Running total of the manager's actual points through this gameweek.",
     "type": "integer"
    },
    "delta": {
     "description": "``actual`` minus ``model``, running.",
     "type": "integer"
    },
    "gw": {
     "description": "The gameweek this cumulative point is through.",
     "type": "integer"
    },
    "model": {
     "description": "Running total of the model's own points through this gameweek.",
     "type": "integer"
    }
   },
   "required": [
    "actual",
    "delta",
    "gw",
    "model"
   ],
   "title": "JournalPoint",
   "type": "object"
  },
  "JournalRow": {
   "properties": {
    "actual_captain": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The manager's own captain choice."
    },
    "actual_pts": {
     "description": "Points the manager's own team actually scored.",
     "type": "integer"
    },
    "delta": {
     "description": "``actual_pts`` minus ``model_pts``.",
     "type": "integer"
    },
    "gw": {
     "description": "The gameweek this row compares.",
     "type": "integer"
    },
    "model_buys": {
     "description": "Names the model would have bought that gameweek.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "model_captain": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The model's captain choice, when a plan was banked."
    },
    "model_pts": {
     "description": "Points the model's own plan would have scored.",
     "type": "integer"
    },
    "model_sells": {
     "description": "Names the model would have sold that gameweek.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "post_deadline": {
     "default": false,
     "description": "Every banked run of this gameweek was written after its deadline, so\nthe model's side of the comparison had the team news the user did not.",
     "type": "boolean"
    }
   },
   "required": [
    "actual_captain",
    "actual_pts",
    "delta",
    "gw",
    "model_buys",
    "model_captain",
    "model_pts",
    "model_sells",
    "post_deadline"
   ],
   "title": "JournalRow",
   "type": "object"
  },
  "LadderCap": {
   "properties": {
    "max_hits": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The hit cap the ladder solved every rung under, or ``None`` for none."
    },
    "max_transfers": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The transfer cap the ladder solved every rung under, or ``None`` for\nnone; 0 means bank."
    }
   },
   "required": [
    "max_hits",
    "max_transfers"
   ],
   "title": "LadderCap",
   "type": "object"
  },
  "LadderPayload": {
   "properties": {
    "bar": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The hit bar the walk used (v16 \u00a73.2)."
    },
    "cap": {
     "$ref": "#/definitions/LadderCap",
     "description": "The hit and transfer caps every rung solved under."
    },
    "cap_note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Set when the saved ``max_transfers`` has no rung of its own."
    },
    "cap_rung": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The highlighted row, resolved through ``same_as`` to a row that\ncarries numbers."
    },
    "cap_rung_requested": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The row the saved cap literally names, before that resolution."
    },
    "cap_source": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Where ``cap`` came from: ``\"config\"``, the live settings the card\nwrites, or ``\"state\"`` when that could not be read and the caps the saved\nsolve ran under stood in."
    },
    "chosen": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The rung the walk stopped on \u2014 the served advice's plan."
    },
    "free_transfers": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Free transfers available going into ``gw``, from the saved state."
    },
    "generated_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "ISO timestamp the ladder was built."
    },
    "gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The gameweek the ladder was built for."
    },
    "gws": {
     "description": "Every gameweek in the solve horizon, first entry ``gw``.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "n_draws": {
     "default": 0,
     "description": "How many Monte Carlo draws the rungs were scored on.",
     "type": "integer"
    },
    "note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why ``rungs`` is empty, when it is: no state, or no ladder banked."
    },
    "notes": {
     "description": "Rungs dropped because they would not solve.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "recommended": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The rung key the served advice on disk matches, when it matches one."
    },
    "recommended_note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why ``recommended`` is ``None``, when it is."
    },
    "rungs": {
     "description": "Every rung the ladder solved, in ``RUNG_ORDER``.",
     "items": {
      "$ref": "#/definitions/LadderRung"
     },
     "type": "array"
    },
    "seed": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The RNG seed the draws were built with."
    },
    "served_note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Set by the router when a rebuild's choice differs from the advice\non disk."
    },
    "sigma_fallbacks": {
     "default": 0,
     "description": "Player-weeks that fell back to the outcome \u03c3 for want of a band.",
     "type": "integer"
    },
    "sigma_source": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``\"bands\"``, ``\"outcome\"`` or ``\"bands+outcome\"`` \u2014 where the scoring\n\u03c3 came from, the mixed value set when some player-week fell back."
    },
    "steps": {
     "description": "The restraint walk's steps, from the lowest rung up to ``chosen``.",
     "items": {
      "$ref": "#/definitions/LadderStep"
     },
     "type": "array"
    },
    "wall_s": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Seconds the solve took, timed from inside ``ladder.ladder_payload``,\nexcluding whatever loaded the state beforehand."
    }
   },
   "required": [
    "bar",
    "cap",
    "cap_note",
    "cap_rung",
    "cap_rung_requested",
    "cap_source",
    "chosen",
    "free_transfers",
    "generated_at",
    "gw",
    "gws",
    "n_draws",
    "note",
    "notes",
    "recommended",
    "recommended_note",
    "rungs",
    "seed",
    "served_note",
    "sigma_fallbacks",
    "sigma_source",
    "steps",
    "wall_s"
   ],
   "title": "LadderPayload",
   "type": "object"
  },
  "LadderRung": {
   "description": "One row. Every number is ``None`` on a ``same_as`` row, which repeats\nthe rung below rather than re-solving it.",
   "properties": {
    "cost": {
     "description": "The first week's hits, in points.",
     "type": "integer"
    },
    "hits": {
     "description": "Hits taken in the **first** week \u2014 the decision on the table now.",
     "type": "integer"
    },
    "horizon_cost": {
     "default": 0,
     "description": "``horizon_hits`` in points.",
     "type": "integer"
    },
    "horizon_hits": {
     "default": 0,
     "description": "Hits over the whole horizon, which is what ``horizon_pts`` and\n``mean_pts`` are already net of.",
     "type": "integer"
    },
    "horizon_pts": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Expected points summed over the whole horizon, net of hits."
    },
    "key": {
     "description": "The rung's identifier: ``\"bank\"``, ``\"open\"``, or ``\"hits{n}\"``.",
     "type": "string"
    },
    "label": {
     "default": "",
     "description": "The rung's name in prose (v17b \u00a73.1): ``bank``, ``free transfers\nonly``, ``1 hit``, ``no cap``. Every surface renders this; none composes\nit.",
     "type": "string"
    },
    "mean_pts": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Mean simulated points across the ladder's shared draws."
    },
    "objective": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The MILP's own objective value for this rung's solve."
    },
    "p10_pts": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "10th percentile of the rung's simulated points."
    },
    "p90_pts": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "90th percentile of the rung's simulated points."
    },
    "p_beats_bank": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Share of shared draws this rung outscores the bank rung, or ``None``\nfor the bank rung itself or when the bank rung did not solve."
    },
    "p_beats_top": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Share of shared draws this rung outscores the top (highest-hit)\nrung, or ``None`` for the top rung itself."
    },
    "p_best": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Share of shared draws this rung scores the maximum among all rungs,\nties split evenly (``ladder.p_best``)."
    },
    "plan_by_gw": {
     "description": "This rung's plan, one entry per gameweek in the horizon.",
     "items": {
      "$ref": "#/definitions/LadderWeek"
     },
     "type": "array"
    },
    "same_as": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The distinct rung's key this row repeats, when the solve collapsed\nonto an earlier rung's first-week decision (``ladder.collapse``)."
    },
    "transfers": {
     "description": "Number of buys in the first week's plan.",
     "type": "integer"
    },
    "vs_below": {
     "anyOf": [
      {
       "$ref": "#/definitions/LadderVsBelow"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What this rung buys over the previous distinct rung, or ``None`` for\nthe lowest rung."
    },
    "week_pts": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Expected points for the first week alone, net of its hits."
    }
   },
   "required": [
    "cost",
    "hits",
    "horizon_cost",
    "horizon_hits",
    "horizon_pts",
    "key",
    "label",
    "mean_pts",
    "objective",
    "p10_pts",
    "p90_pts",
    "p_beats_bank",
    "p_beats_top",
    "p_best",
    "plan_by_gw",
    "same_as",
    "transfers",
    "vs_below",
    "week_pts"
   ],
   "title": "LadderRung",
   "type": "object"
  },
  "LadderStep": {
   "description": "One step of the restraint walk (v16 \u00a73.1). ``share`` is the share of\nthe shared draws in which ``above`` outscored ``below``.",
   "properties": {
    "above": {
     "description": "The rung key it considered stepping to.",
     "type": "string"
    },
    "below": {
     "description": "The rung key the walk was standing on.",
     "type": "string"
    },
    "line": {
     "default": "",
     "description": "The step as one sentence (v17b \u00a73.1), composed once by\n``ladder._step_line``.",
     "type": "string"
    },
    "reason": {
     "default": "",
     "description": "Why the step was or was not taken, in prose (``ladder.explain_step``\nor the cap sentence in ``ladder.walk``).",
     "type": "string"
    },
    "reason_kind": {
     "default": "points",
     "description": "``\"cap\"`` when a hit or transfer cap refused the step; otherwise\nwhich kind of evidence ``explain_step`` picked.",
     "type": "string"
    },
    "share": {
     "description": "Share of the shared draws in which ``above`` outscored ``below``.",
     "type": "number"
    },
    "taken": {
     "description": "Whether the walk took this step \u2014 ``share >= hit_bar``, unless a\ncap refused it first.",
     "type": "boolean"
    }
   },
   "required": [
    "above",
    "below",
    "line",
    "reason",
    "reason_kind",
    "share",
    "taken"
   ],
   "title": "LadderStep",
   "type": "object"
  },
  "LadderVsBelow": {
   "description": "What the extra hit bought, against the previous distinct rung.",
   "properties": {
    "delta_cost": {
     "description": "The **horizon** hit cost this rung carries over the rung below.\n``max_hits`` is a per-gameweek cap, so a rung can pay it every week, and\nit is the horizon figure that ``delta_mean_pts`` is net of.",
     "type": "integer"
    },
    "delta_cost_now": {
     "default": 0,
     "description": "The first week's difference alone.",
     "type": "integer"
    },
    "delta_mean_pts": {
     "description": "This rung's mean simulated points minus the rung below's, from\n``ladder.vs_below``.",
     "type": "number"
    },
    "dropped_buys": {
     "description": "The rung below's first-week buys this rung does not make.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "dropped_sells": {
     "description": "The rung below's first-week sells this rung does not make.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "extra_buys": {
     "description": "First-week buys this rung makes that the rung below did not.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "extra_sells": {
     "description": "First-week sells this rung makes that the rung below did not.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    }
   },
   "required": [
    "delta_cost",
    "delta_cost_now",
    "delta_mean_pts",
    "dropped_buys",
    "dropped_sells",
    "extra_buys",
    "extra_sells"
   ],
   "title": "LadderVsBelow",
   "type": "object"
  },
  "LadderWeek": {
   "properties": {
    "bench": {
     "description": "The bench this week of the plan.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "buys": {
     "description": "Players bought in this week of the plan.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "captain": {
     "$ref": "#/definitions/WirePlayerRef"
    },
    "expected_pts": {
     "description": "Expected points for this week alone, net of its hits\n(``ladder.plan_points`` over one week).",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek this week of the rung's plan is for.",
     "type": "integer"
    },
    "hits": {
     "description": "Hits taken in this week alone.",
     "type": "integer"
    },
    "sells": {
     "description": "Players sold in this week of the plan.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "vice": {
     "$ref": "#/definitions/WirePlayerRef"
    },
    "xi": {
     "description": "The starting eleven this week of the plan.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    }
   },
   "required": [
    "bench",
    "buys",
    "captain",
    "expected_pts",
    "gw",
    "hits",
    "sells",
    "vice",
    "xi"
   ],
   "title": "LadderWeek",
   "type": "object"
  },
  "LaunchdHealth": {
   "properties": {
    "last_line": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "description": "The log's last non-blank line \u2014 the advise job's own report of what\nit did."
    },
    "log": {
     "description": "Path to the advise job's redirect log.",
     "type": "string"
    },
    "modified_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "description": "When it was last written, or ``None`` if it never was."
    },
    "present": {
     "description": "Whether that log file exists.",
     "type": "boolean"
    }
   },
   "required": [
    "last_line",
    "log",
    "modified_at",
    "present"
   ],
   "title": "LaunchdHealth",
   "type": "object"
  },
  "LeadBucket": {
   "description": "One band of the lead-time histogram, split by what happened.",
   "properties": {
    "bucket": {
     "description": "The lead-time band this row covers, in prose.",
     "type": "string"
    },
    "missed": {
     "description": "How many flags in this band belonged to a player who then did not\nstart.",
     "type": "integer"
    },
    "started": {
     "description": "How many flags in this band belonged to a player who then started.",
     "type": "integer"
    }
   },
   "required": [
    "bucket",
    "missed",
    "started"
   ],
   "title": "LeadBucket",
   "type": "object"
  },
  "LeagueRaceData": {
   "properties": {
    "entry_id": {
     "description": "The configured manager's entry id.",
     "type": "integer"
    },
    "focus": {
     "default": true,
     "description": "False when this is another private league opened for display: its\n\u03bb is computed from its own standings and tilts nothing (v15 \u00a73.3).",
     "type": "boolean"
    },
    "gap": {
     "description": "The manager's running gap to the leader, one point per scored week.",
     "items": {
      "$ref": "#/definitions/GapPoint"
     },
     "type": "array"
    },
    "lam": {
     "description": "The chase/defend tilt strength the next advise would see.",
     "type": "number"
    },
    "lam_explained": {
     "description": "The stance and its \u03bb, in one sentence, from ``explain_lam``.",
     "type": "string"
    },
    "league_id": {
     "description": "The league this race is for.",
     "type": "integer"
    },
    "league_name": {
     "default": "",
     "description": "The league's name.",
     "type": "string"
    },
    "stance": {
     "description": "``\"chase\"``, ``\"defend\"`` or ``\"neutral\"``, the stance ``lam`` implies.",
     "type": "string"
    },
    "stance_source": {
     "default": "auto",
     "description": "Whether ``stance`` came from the solver's own read or a manual\noverride in config.",
     "enum": [
      "auto",
      "manual"
     ],
     "type": "string"
    },
    "standings": {
     "description": "Every entry's current standing.",
     "items": {
      "$ref": "#/definitions/StandingRow"
     },
     "type": "array"
    },
    "trajectory": {
     "description": "Every entry's points history across the season.",
     "items": {
      "$ref": "#/definitions/Trajectory"
     },
     "type": "array"
    },
    "win_probability": {
     "description": "Every rival's modelled chance of finishing above the manager.",
     "items": {
      "$ref": "#/definitions/WinProb"
     },
     "type": "array"
    }
   },
   "required": [
    "entry_id",
    "focus",
    "gap",
    "lam",
    "lam_explained",
    "league_id",
    "league_name",
    "stance",
    "stance_source",
    "standings",
    "trajectory",
    "win_probability"
   ],
   "title": "LeagueRaceData",
   "type": "object"
  },
  "LeagueSimData": {
   "properties": {
    "entries": {
     "description": "How many entries in the league were simulated.",
     "type": "integer"
    },
    "exp_finish": {
     "description": "Mean simulated final rank for the manager.",
     "type": "number"
    },
    "field": {
     "anyOf": [
      {
       "$ref": "#/definitions/FieldRank"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "v12 W4 \u00a75.3's panel. ``None`` only when the simulation itself could not\nbe built; an unanswerable question is a ``FieldRank`` full of nulls with\ntheir reasons, not an absent object."
    },
    "field_rate": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The sampled field's weekly rate, or ``None`` when nothing is banked \u2014\nin which case rivals do not drift however ``rival_drift`` is set."
    },
    "gw": {
     "description": "The gameweek the simulation was run from.",
     "type": "integer"
    },
    "history": {
     "description": "Banked headline numbers from earlier gameweeks, for the sparkline.",
     "items": {
      "$ref": "#/definitions/SimPoint"
     },
     "type": "array"
    },
    "legacy_win_probability": {
     "description": "``league_mode.win_probability``'s parametric answer, kept beside the\nsimulated one until the UI has fully switched (spec \u00a73).",
     "items": {
      "$ref": "#/definitions/WinProb"
     },
     "type": "array"
    },
    "margin_quantiles": {
     "additionalProperties": {
      "type": "number"
     },
     "description": "Quantiles of the simulated final points margin over the field.",
     "type": "object"
    },
    "n": {
     "description": "How many Monte Carlo draws the simulation ran.",
     "type": "integer"
    },
    "notice": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "A caveat on the simulation, when one applies."
    },
    "p_top3": {
     "description": "Modelled P(the manager finishes top 3).",
     "type": "number"
    },
    "p_win": {
     "description": "Modelled P(the manager finishes first).",
     "type": "number"
    },
    "per_rival": {
     "description": "Every rival's simulated chance of being beaten.",
     "items": {
      "$ref": "#/definitions/RivalBeat"
     },
     "type": "array"
    },
    "rival_drift": {
     "description": "``[league] rival_drift`` as configured \u2014 the per-week noise applied\nto a rival's future scoring.",
     "type": "number"
    },
    "seed": {
     "description": "The RNG seed the simulation was run with.",
     "type": "integer"
    },
    "weeks_left": {
     "description": "Gameweeks remaining in the season.",
     "type": "integer"
    }
   },
   "required": [
    "entries",
    "exp_finish",
    "field",
    "field_rate",
    "gw",
    "history",
    "legacy_win_probability",
    "margin_quantiles",
    "n",
    "notice",
    "p_top3",
    "p_win",
    "per_rival",
    "rival_drift",
    "seed",
    "weeks_left"
   ],
   "title": "LeagueSimData",
   "type": "object"
  },
  "LeagueWhatIfPin": {
   "properties": {
    "code": {
     "description": "A gaffer player *code*, not a season element id \u2014 the explorer, the\nsquad table and the compare panel all speak codes, and the router maps to\nelements against the same snapshot they were rendered from.",
     "type": "integer"
    },
    "event": {
     "default": "blank",
     "description": "The score outcome to pin this player to for the simulation.",
     "type": "string"
    }
   },
   "required": [
    "code",
    "event"
   ],
   "title": "LeagueWhatIfPin",
   "type": "object"
  },
  "LeagueWhatIfRequest": {
   "properties": {
    "cached_only": {
     "default": false,
     "description": "Answer from the cache or not at all (204).\n\nThis Week's captaincy chip sets it. That page is the one opened on a\nThursday evening, the chip is decoration, and a cold cache means fifty\nentry-picks requests at the FPL API fired by a page load \u2014 at the hour\nevery FPL manager in the country is loading pages. The League What-if tab\nleaves it false: there the simulation *is* the page.",
     "type": "boolean"
    },
    "captain_override": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "A player code to captain instead of the manager's own pick."
    },
    "league_id": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Which private league to re-count (v15 \u00a75.2); ``None`` is the focus."
    },
    "pins": {
     "description": "Player outcomes to pin before re-running the simulation.",
     "items": {
      "$ref": "#/definitions/LeagueWhatIfPin"
     },
     "type": "array"
    },
    "rival_captain_blanks": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "A rival entry id whose captain is pinned to blank for the run."
    }
   },
   "required": [
    "captain_override",
    "league_id",
    "pins",
    "rival_captain_blanks"
   ],
   "title": "LeagueWhatIfRequest",
   "type": "object"
  },
  "LeagueWhatIfResult": {
   "properties": {
    "baseline_exp_finish": {
     "description": "The manager's mean simulated finish before the pins were applied.",
     "type": "number"
    },
    "baseline_p_win": {
     "description": "The manager's P(win) before the pins were applied.",
     "type": "number"
    },
    "delta_p_win": {
     "description": "``p_win`` minus ``baseline_p_win``.",
     "type": "number"
    },
    "delta_rank": {
     "description": "``exp_finish`` minus ``baseline_exp_finish``.",
     "type": "number"
    },
    "exp_finish": {
     "description": "The manager's mean simulated finish with the pins applied.",
     "type": "number"
    },
    "p_win": {
     "description": "The manager's P(win) with the pins applied.",
     "type": "number"
    },
    "table": {
     "description": "Every entry's simulated result under the pins.",
     "items": {
      "$ref": "#/definitions/LeagueWhatIfRow"
     },
     "type": "array"
    },
    "unknown_codes": {
     "description": "Pinned codes that could not be resolved against the squad snapshot.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    }
   },
   "required": [
    "baseline_exp_finish",
    "baseline_p_win",
    "delta_p_win",
    "delta_rank",
    "exp_finish",
    "p_win",
    "table",
    "unknown_codes"
   ],
   "title": "LeagueWhatIfResult",
   "type": "object"
  },
  "LeagueWhatIfRow": {
   "properties": {
    "entry": {
     "description": "FPL entry id.",
     "type": "integer"
    },
    "exp_finish": {
     "description": "Mean simulated final rank under this what-if.",
     "type": "number"
    },
    "is_you": {
     "description": "Whether this row is the configured entry.",
     "type": "boolean"
    },
    "name": {
     "description": "The entry's team name.",
     "type": "string"
    },
    "p_win": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "This entry's win frequency in the same run as the headline, or ``None``\nwhen its squad could not be read (``league_sim.is_readable``)."
    },
    "total": {
     "description": "Total points to date.",
     "type": "integer"
    }
   },
   "required": [
    "entry",
    "exp_finish",
    "is_you",
    "name",
    "p_win",
    "total"
   ],
   "title": "LeagueWhatIfRow",
   "type": "object"
  },
  "LeaguesOverview": {
   "properties": {
    "focus_lam": {
     "description": "The focus league's tilt as the next advise will see it: the solve\nstate's \u03bb with a manual stance applied (plan R2).",
     "type": "number"
    },
    "focus_league_id": {
     "description": "``[league] league_id`` as configured.",
     "type": "integer"
    },
    "focus_name": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``None`` when the focus is not one of the private leagues, in which\ncase ``focus_warning`` says so."
    },
    "focus_stance": {
     "description": "The focus league's resolved stance, auto or manual.",
     "enum": [
      "chase",
      "defend",
      "neutral"
     ],
     "type": "string"
    },
    "focus_warning": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why there is no focus league to report on, when there is none."
    },
    "gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The entry's current gameweek, from FPL's own ``current_event``."
    },
    "private": {
     "description": "Every private mini-league the entry is in.",
     "items": {
      "$ref": "#/definitions/PrivateLeagueRow"
     },
     "type": "array"
    },
    "public": {
     "description": "Every public or system league the entry is in.",
     "items": {
      "$ref": "#/definitions/PublicLeagueRow"
     },
     "type": "array"
    },
    "stance": {
     "description": "``[league] stance`` as configured.",
     "enum": [
      "auto",
      "chase",
      "defend",
      "neutral"
     ],
     "type": "string"
    }
   },
   "required": [
    "focus_lam",
    "focus_league_id",
    "focus_name",
    "focus_stance",
    "focus_warning",
    "gw",
    "private",
    "public",
    "stance"
   ],
   "title": "LeaguesOverview",
   "type": "object"
  },
  "LivePlayer": {
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "element": {
     "description": "FPL's own element id.",
     "type": "integer"
    },
    "minutes": {
     "description": "Minutes played so far this gameweek.",
     "type": "integer"
    },
    "multiplier": {
     "description": "FPL's own points multiplier: 0 unused, 1 normal, 2 captain,\n3 triple captain.",
     "type": "integer"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "points": {
     "description": "Live points so far this gameweek, multiplier applied.",
     "type": "integer"
    },
    "position": {
     "description": "GKP, DEF, MID or FWD.",
     "type": "string"
    },
    "projected_in": {
     "default": false,
     "description": "Whether he is projected to be substituted in by an autosub.",
     "type": "boolean"
    },
    "projected_out": {
     "default": false,
     "description": "Whether he is projected to be substituted out by an autosub.",
     "type": "boolean"
    },
    "provisional_bonus": {
     "description": "Bonus points projected from the live BPS, before FPL confirms them.",
     "type": "integer"
    },
    "remaining_ep": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Expected points still to come this gameweek, for a player yet to\nfinish his fixture."
    },
    "selected_by_percent": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "FPL's own overall ownership percentage."
    },
    "status": {
     "description": "Whether his fixture has finished, is live, or has not kicked off.",
     "enum": [
      "played",
      "playing",
      "yet to play"
     ],
     "type": "string"
    },
    "sub_partner": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The other half of a projected substitution, so a chip can name him."
    },
    "sub_reason": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``\"played\"`` or ``\"yet to play\"``: how certain the incoming man is."
    },
    "tier_eo": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Effective ownership among the sampled top-tier managers, when a\nsample was taken."
    },
    "tier_eo_se": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Standard error on ``tier_eo``, when a sample was taken."
    }
   },
   "required": [
    "code",
    "element",
    "minutes",
    "multiplier",
    "name",
    "points",
    "position",
    "projected_in",
    "projected_out",
    "provisional_bonus",
    "remaining_ep",
    "selected_by_percent",
    "status",
    "sub_partner",
    "sub_reason",
    "tier_eo",
    "tier_eo_se"
   ],
   "title": "LivePlayer",
   "type": "object"
  },
  "LiveRacePoint": {
   "description": "One poll's snapshot of the race, held in memory for this session only.",
   "properties": {
    "at": {
     "description": "ISO timestamp of this poll.",
     "type": "string"
    },
    "rival": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The tracked rival's race value \u2014 the entry pinned in ``rival_name``,\nwhich is the top entry in the league that is not me. He is the leader\nonly when I am not; when I am leading he is the man in second."
    },
    "you": {
     "description": "The manager's own race value at this poll.",
     "type": "number"
    }
   },
   "required": [
    "at",
    "rival",
    "you"
   ],
   "title": "LiveRacePoint",
   "type": "object"
  },
  "LiveSafety": {
   "description": "One league place worth watching, priced in points.",
   "properties": {
    "entry": {
     "description": "FPL entry id.",
     "type": "integer"
    },
    "margin": {
     "description": "Their projected total minus mine. Positive means they are ahead.",
     "type": "integer"
    },
    "name": {
     "description": "The entry's team name.",
     "type": "string"
    },
    "need": {
     "description": "What I must add beyond my projection to pass them; 0 when I lead.",
     "type": "integer"
    },
    "role": {
     "description": "Whether this entry sits above, below the manager, or leads the league.",
     "enum": [
      "above",
      "below",
      "leader"
     ],
     "type": "string"
    }
   },
   "required": [
    "entry",
    "margin",
    "name",
    "need",
    "role"
   ],
   "title": "LiveSafety",
   "type": "object"
  },
  "LiveState": {
   "properties": {
    "active": {
     "description": "Whether any fixture in the gameweek is live or about to be.",
     "type": "boolean"
    },
    "gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "description": "The gameweek being tracked, or ``None`` when inactive."
    },
    "matches_in_play": {
     "description": "How many fixtures are currently live.",
     "type": "integer"
    },
    "my_points": {
     "description": "The manager's own live points so far, no autosubs applied.",
     "type": "integer"
    },
    "my_projected_points": {
     "default": 0,
     "description": "The manager's season total projected with autosubs applied.",
     "type": "integer"
    },
    "my_race": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The manager's own race value: projected points plus what remains."
    },
    "notice": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "A caveat on the tier-EO reading, when one applies."
    },
    "players": {
     "description": "The manager's own squad, live.",
     "items": {
      "$ref": "#/definitions/LivePlayer"
     },
     "type": "array"
    },
    "race_notice": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The race's own degradation line. Deliberately not ``notice``, which is\nthe tier-EO line and belongs to a different card."
    },
    "race_reference": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "This gameweek's saved ``advice.expected_pts``, when there is one."
    },
    "race_series": {
     "description": "This session's polled race values, for the sparkline.",
     "items": {
      "$ref": "#/definitions/LiveRacePoint"
     },
     "type": "array"
    },
    "rival_name": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The entry the trajectory follows: the highest-placed entry that is not\nme, picked on the gameweek's first poll and then pinned for the rest of it\nso the line cannot change whose points it is plotting mid-afternoon."
    },
    "safety": {
     "description": "League places worth watching, one row per rival above or below.",
     "items": {
      "$ref": "#/definitions/LiveSafety"
     },
     "type": "array"
    },
    "table": {
     "description": "The league standings, projected live.",
     "items": {
      "$ref": "#/definitions/LiveTableRow"
     },
     "type": "array"
    }
   },
   "required": [
    "active",
    "gw",
    "matches_in_play",
    "my_points",
    "my_projected_points",
    "my_race",
    "notice",
    "players",
    "race_notice",
    "race_reference",
    "race_series",
    "rival_name",
    "safety",
    "table"
   ],
   "title": "LiveState",
   "type": "object"
  },
  "LiveTableRow": {
   "properties": {
    "delta": {
     "description": "``projected`` minus ``pre_total`` \u2014 this gameweek's projected gain.",
     "type": "integer"
    },
    "entry": {
     "description": "FPL entry id.",
     "type": "integer"
    },
    "live": {
     "description": "This gameweek's live points, no autosubs applied.",
     "type": "integer"
    },
    "name": {
     "description": "The entry's team name.",
     "type": "string"
    },
    "pre_total": {
     "description": "Total points before this gameweek.",
     "type": "integer"
    },
    "projected": {
     "description": "Season total projected with autosubs applied.",
     "type": "integer"
    },
    "projected_live": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "This gameweek's points with the projected autosubs applied."
    },
    "race": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``projected_live + remaining_ep``: where this gameweek is heading."
    },
    "remaining_ep": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Expected points still to come this gameweek for this entry's squad."
    }
   },
   "required": [
    "delta",
    "entry",
    "live",
    "name",
    "pre_total",
    "projected",
    "projected_live",
    "race",
    "remaining_ep"
   ],
   "title": "LiveTableRow",
   "type": "object"
  },
  "MatrixCell": {
   "properties": {
    "attack": {
     "description": "Difficulty for your attackers, 0 easiest to 1 hardest.\n\nDriven by the opponent's *defence* strength: a mean defence is a hard\nfixture to score in.",
     "type": "number"
    },
    "defence": {
     "description": "Difficulty of keeping a clean sheet, 0 easiest to 1 hardest.\n\nDriven by the opponent's *attack* strength.",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek this cell rates.",
     "type": "integer"
    },
    "home": {
     "description": "Whether the fixture is at home.",
     "type": "boolean"
    },
    "opponent": {
     "description": "The opponent's short name.",
     "type": "string"
    }
   },
   "required": [
    "attack",
    "defence",
    "gw",
    "home",
    "opponent"
   ],
   "title": "MatrixCell",
   "type": "object"
  },
  "MatrixTeam": {
   "properties": {
    "cells": {
     "description": "One cell per rated gameweek.",
     "items": {
      "$ref": "#/definitions/MatrixCell"
     },
     "type": "array"
    },
    "code": {
     "description": "The club's code.",
     "type": "integer"
    },
    "mean_attack": {
     "description": "Mean of ``cells``' attack difficulty.",
     "type": "number"
    },
    "mean_defence": {
     "description": "Mean of ``cells``' defence difficulty.",
     "type": "number"
    },
    "name": {
     "description": "The club's full name.",
     "type": "string"
    },
    "short_name": {
     "description": "The club's short name, as shown on ``cells``.",
     "type": "string"
    }
   },
   "required": [
    "cells",
    "code",
    "mean_attack",
    "mean_defence",
    "name",
    "short_name"
   ],
   "title": "MatrixTeam",
   "type": "object"
  },
  "MinutesOutput": {
   "properties": {
    "p60": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The same convention, on the probability beside it. 0.0 here is\n\"expected off before the hour\", which is a forecast a frame banked\nwithout a minutes model never made \u2014 and it is the number ``xmins``\nweights the second half by, so a zero propagates into a claim about\nminutes as well."
    },
    "p_play": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``None`` \u2014 never 0.0 \u2014 for a frame banked without a minutes model. Zero\nhere reads as \"expected not to play\", which is the strongest claim this\npayload can make about a player, and the compare radar drew it as a\nzero-length spoke on the minutes axis."
    },
    "xmins": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Expected minutes, ``p_play * (45 + 45 * p60)``. ``None`` when either\nprobability is missing: an un-modelled player is not a player expected to\nplay no minutes."
    }
   },
   "required": [
    "p60",
    "p_play",
    "xmins"
   ],
   "title": "MinutesOutput",
   "type": "object"
  },
  "MissRow": {
   "description": "One player-gameweek the forecast got most wrong.\n\n``miss`` is ``actual - ep``, so it is signed: a positive one is a player\nthe model under-rated and a negative one is a transfer it may have talked\nsomebody into. Both directions are shown, which is why the card sorts on\nthe absolute value and prints the sign.",
   "properties": {
    "actual": {
     "description": "Points actually scored this gameweek.",
     "type": "integer"
    },
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "ep": {
     "description": "Expected points, as forecast for this gameweek.",
     "type": "number"
    },
    "minutes": {
     "default": 0,
     "description": "Minutes actually played this gameweek.",
     "type": "integer"
    },
    "miss": {
     "description": "``actual - ep``, signed: positive is an under-rated player, negative\nan over-rated one.",
     "type": "number"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "position": {
     "default": "",
     "description": "GKP, DEF, MID or FWD, when known.",
     "type": "string"
    },
    "price": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "His price in \u00a3m at the time, when known."
    }
   },
   "required": [
    "actual",
    "code",
    "ep",
    "minutes",
    "miss",
    "name",
    "position",
    "price"
   ],
   "title": "MissRow",
   "type": "object"
  },
  "MissesData": {
   "properties": {
    "gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``None`` when no gameweek has both a banked forecast and a banked\nresult. That is an absent card, not a card of zeros (spec D1)."
    },
    "rows": {
     "description": "The biggest misses that gameweek, by absolute value.",
     "items": {
      "$ref": "#/definitions/MissRow"
     },
     "type": "array"
    }
   },
   "required": [
    "gw",
    "rows"
   ],
   "title": "MissesData",
   "type": "object"
  },
  "MoverRow": {
   "description": "One watched player FPL's predictor has near a threshold tonight.",
   "properties": {
    "calibrating": {
     "description": "FPL is still fitting this player's price model \u2014 an early-season caveat\nthe row carries rather than a reason to hide it.",
     "type": "boolean"
    },
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "direction": {
     "description": "``rise`` or ``drop``. Never ``flat``: this list is only ever rows past\nthe alert threshold, where the price log (which sees everyone) has a third\nvalue.",
     "type": "string"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "now_cost": {
     "description": "In millions, the way the UI shows a price \u2014 not the 0.1m integer the\nbootstrap carries.",
     "type": "number"
    },
    "price_change_percent": {
     "description": "FPL's own predictor reading, how close he is to a change tonight.",
     "type": "number"
    },
    "source": {
     "description": "``squad`` / ``plan`` / ``watchlist``, resolved in that order. The\nanswer to \"why is he on this list?\", on the row itself.",
     "type": "string"
    }
   },
   "required": [
    "calibrating",
    "code",
    "direction",
    "name",
    "now_cost",
    "price_change_percent",
    "source"
   ],
   "title": "MoverRow",
   "type": "object"
  },
  "MoversPanel": {
   "description": "Tonight's likely price changes among players the manager cares about.\n\n``as_of`` is the age of the *reading*, not of the request: this is served\noff ``data/live/players.parquet`` and never off the network, so a panel\nthat did not say how stale it was would be a panel claiming to know\nsomething about tonight when it might be quoting Tuesday.",
   "properties": {
    "as_of": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "When the underlying snapshot was written \u2014 the reading's own age,\nnot the request's."
    },
    "available": {
     "description": "Whether the players snapshot could be read at all.",
     "type": "boolean"
    },
    "rows": {
     "description": "Watched, squad or planned players near a price change tonight.",
     "items": {
      "$ref": "#/definitions/MoverRow"
     },
     "type": "array"
    }
   },
   "required": [
    "as_of",
    "available",
    "rows"
   ],
   "title": "MoversPanel",
   "type": "object"
  },
  "NamedPlayer": {
   "description": "A player a report names but does not price.",
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "position": {
     "default": "",
     "description": "GKP, DEF, MID or FWD, when known.",
     "type": "string"
    }
   },
   "required": [
    "code",
    "name",
    "position"
   ],
   "title": "NamedPlayer",
   "type": "object"
  },
  "NewsPanelData": {
   "properties": {
    "gw": {
     "description": "The gameweek the panel is reporting on.",
     "type": "integer"
    },
    "moved": {
     "description": "How many players cleared ``MOVED_EPSILON`` between the news-aware and\nflag-only readings \u2014 ``len(rows)``.",
     "type": "integer"
    },
    "rows": {
     "description": "The moved players, biggest disagreement first.",
     "items": {
      "$ref": "#/definitions/NewsRow"
     },
     "type": "array"
    }
   },
   "required": [
    "gw",
    "moved",
    "rows"
   ],
   "title": "NewsPanelData",
   "type": "object"
  },
  "NewsRow": {
   "description": "One player the news layer moved, with the evidence that moved him.\n\nBoth sides of every number, because the panel's claim is a *difference*:\n\"we think 5%, the official flag says 75%\" is the sentence, and either half\non its own is not.",
   "properties": {
    "chance_of_playing": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "FPL's own percentage chance of playing, from the live players\nsnapshot."
    },
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "e_min_flags": {
     "description": "Expected minutes under the official-flag-only reading, rounded to 1dp.",
     "type": "number"
    },
    "e_min_news": {
     "description": "Expected minutes under the news-aware reading, rounded to 1dp.",
     "type": "number"
    },
    "expected_return_gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The gameweek the availability frame expects him back, when known."
    },
    "fetched_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "ISO timestamp the availability evidence was fetched."
    },
    "injury_type": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The injury or absence category this run's availability frame recorded\nfor him, when it has one."
    },
    "lineup_hint": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``xi`` / ``doubt`` / ``out`` \u2014 ``p_start_hint`` named, because a\nprobability in a caption reads as a forecast rather than as a listing."
    },
    "name": {
     "description": "Player name, from the live players snapshot.",
     "type": "string"
    },
    "official_note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "FPL's own news text for the player, from the live players snapshot."
    },
    "p_play_flags": {
     "description": "P(starts) as it would read off the official flag alone, rounded to\n3dp \u2014 the comparison figure ``p_play_news`` disagrees with.",
     "type": "number"
    },
    "p_play_news": {
     "description": "This run's P(starts), as predicted from the news-aware availability\nframe, rounded to 3dp.",
     "type": "number"
    },
    "p_start_hint": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The availability frame's own P(starts), separate from the news-model\nfigures above."
    },
    "source": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Where the availability evidence came from (e.g. a news feed name)."
    },
    "status": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "FPL's own status code for the player (e.g. injured, doubtful), from\nthe live players snapshot."
    },
    "team_name": {
     "description": "Player's club name, from the live teams snapshot.",
     "type": "string"
    }
   },
   "required": [
    "chance_of_playing",
    "code",
    "e_min_flags",
    "e_min_news",
    "expected_return_gw",
    "fetched_at",
    "injury_type",
    "lineup_hint",
    "name",
    "official_note",
    "p_play_flags",
    "p_play_news",
    "p_start_hint",
    "source",
    "status",
    "team_name"
   ],
   "title": "NewsRow",
   "type": "object"
  },
  "NewsShadowData": {
   "description": "Gate N2's standing readout.\n\n``rows`` is the field that says whether any of it means anything: the log\nis written every week and scored only once a gameweek has been played, so\na fresh install carries a payload with ``rows: 0``, an empty ``overall``\nand no gameweeks. That is not an error state \u2014 it is \"come back Monday\".",
   "properties": {
    "by_gw": {
     "description": "The same metrics, one row per scored gameweek, with running totals.",
     "items": {
      "$ref": "#/definitions/NewsShadowGw"
     },
     "type": "array"
    },
    "git_sha": {
     "description": "The commit the readout was built from.",
     "type": "string"
    },
    "overall": {
     "anyOf": [
      {
       "$ref": "#/definitions/NewsShadowSummary"
      },
      {
       "additionalProperties": true,
       "type": "object"
      }
     ],
     "description": "The two metrics over the whole log, or ``{}`` before anything is\nscored."
    },
    "rows": {
     "description": "Total scored player-gameweeks across the whole log.",
     "type": "integer"
    },
    "run_at": {
     "description": "When this readout was built.",
     "type": "string"
    }
   },
   "required": [
    "by_gw",
    "git_sha",
    "overall",
    "rows",
    "run_at"
   ],
   "title": "NewsShadowData",
   "type": "object"
  },
  "NewsShadowGw": {
   "properties": {
    "brier_flags": {
     "description": "Brier score of the official-flag-only reading, this gameweek alone.",
     "type": "number"
    },
    "brier_news": {
     "description": "Brier score of the news-aware reading, this gameweek alone.",
     "type": "number"
    },
    "cum_brier_flags": {
     "description": "Brier score of the official-flag-only reading, cumulative to this\ngameweek.",
     "type": "number"
    },
    "cum_brier_news": {
     "description": "Brier score of the news-aware reading, cumulative to this gameweek.",
     "type": "number"
    },
    "cum_mae_flags": {
     "description": "Mean absolute error of the official-flag-only reading, cumulative to\nthis gameweek.",
     "type": "number"
    },
    "cum_mae_news": {
     "description": "Mean absolute error of the news-aware reading, cumulative to this\ngameweek.",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek this row scores.",
     "type": "integer"
    },
    "mae_flags": {
     "description": "Mean absolute error of the official-flag-only reading, this\ngameweek alone.",
     "type": "number"
    },
    "mae_news": {
     "description": "Mean absolute error of the news-aware reading, this gameweek alone.",
     "type": "number"
    },
    "rows": {
     "description": "How many player rows this gameweek's figures cover.",
     "type": "integer"
    }
   },
   "required": [
    "brier_flags",
    "brier_news",
    "cum_brier_flags",
    "cum_brier_news",
    "cum_mae_flags",
    "cum_mae_news",
    "gw",
    "mae_flags",
    "mae_news",
    "rows"
   ],
   "title": "NewsShadowGw",
   "type": "object"
  },
  "NewsShadowSummary": {
   "description": "Both sides of gate N2's two metrics over one slice of the log.",
   "properties": {
    "brier_flags": {
     "description": "Brier score of the official-flag-only P(plays) reading.",
     "type": "number"
    },
    "brier_news": {
     "description": "Brier score of the news-aware P(plays) reading.",
     "type": "number"
    },
    "mae_flags": {
     "description": "Mean absolute error of the official-flag-only expected-minutes\nreading.",
     "type": "number"
    },
    "mae_news": {
     "description": "Mean absolute error of the news-aware expected-minutes reading.",
     "type": "number"
    },
    "rows": {
     "description": "How many scored player-gameweeks this summary covers.",
     "type": "integer"
    }
   },
   "required": [
    "brier_flags",
    "brier_news",
    "mae_flags",
    "mae_news",
    "rows"
   ],
   "title": "NewsShadowSummary",
   "type": "object"
  },
  "NextFixture": {
   "description": "One team's next game in the advised gameweek.\n\nResolved at serve time from the banked fixture list, never solved for.\nTwo of the four fields are independently optional and mean different\nthings when null: ``kickoff_utc`` is null while FPL still has the date as\nTBC, and ``difficulty`` is null when the ticker could rate nothing \u2014 a\nchip in a neutral colour rather than a chip that is not drawn.\n\nA team with *no* game gets ``next_fixture: null`` on the player instead of\nthis model with empty fields, because \"he does not play\" and \"he plays and\nwe know less than usual about it\" are different sentences.",
   "properties": {
    "difficulty": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The ticker's 0-1 rating for the fixture, or ``None`` when it could\nnot be rated."
    },
    "home": {
     "description": "Whether the fixture is at home.",
     "type": "boolean"
    },
    "kickoff_utc": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "ISO kickoff time, or ``None`` while FPL still has it as TBC."
    },
    "opponent_short": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The next opponent's short club name, or ``None`` when he has no game."
    }
   },
   "required": [
    "difficulty",
    "home",
    "kickoff_utc",
    "opponent_short"
   ],
   "title": "NextFixture",
   "type": "object"
  },
  "OddsInfluence": {
   "properties": {
    "e_gc_blended": {
     "description": "Expected goals conceded after blending with the odds market, or the\nmodel's own figure when there is no market weight.",
     "type": "number"
    },
    "e_gc_model": {
     "description": "The team model's own expected goals conceded, before any odds blend.",
     "type": "number"
    },
    "e_goals_against": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "description": "The odds market's own expected goals against, when priced."
    },
    "p_cs_blended": {
     "description": "P(clean sheet) after blending with the odds market, or the model's\nown figure when there is no market weight.",
     "type": "number"
    },
    "p_cs_model": {
     "description": "The team model's own P(clean sheet), before any odds blend.",
     "type": "number"
    },
    "weight": {
     "description": "How much the bookmaker odds blend counts for this fixture, 0-1;\n0.0 for a frame banked before the blend existed.",
     "type": "number"
    }
   },
   "required": [
    "e_gc_blended",
    "e_gc_model",
    "e_goals_against",
    "p_cs_blended",
    "p_cs_model",
    "weight"
   ],
   "title": "OddsInfluence",
   "type": "object"
  },
  "OutlookTeam": {
   "description": "A club in the outlook. ``short_name`` is null when the teams snapshot\ncould not be read \u2014 the counts are still true, only the label is missing,\nand losing the whole answer over a cosmetic join is the wrong trade.",
   "properties": {
    "code": {
     "description": "The club's code, or the raw FPL team id when ``teams_known`` is false.",
     "type": "integer"
    },
    "short_name": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The club's short name, or ``None`` when the teams snapshot could not\nbe read."
    }
   },
   "required": [
    "code",
    "short_name"
   ],
   "title": "OutlookTeam",
   "type": "object"
  },
  "OutlookWeek": {
   "properties": {
    "blanks": {
     "description": "Clubs with no fixture this gameweek.",
     "items": {
      "$ref": "#/definitions/OutlookTeam"
     },
     "type": "array"
    },
    "doubles": {
     "description": "Clubs with more than one fixture this gameweek.",
     "items": {
      "$ref": "#/definitions/OutlookTeam"
     },
     "type": "array"
    },
    "fixtures": {
     "description": "How many fixtures fall in this gameweek.",
     "type": "integer"
    },
    "gw": {
     "description": "The gameweek this row is about.",
     "type": "integer"
    }
   },
   "required": [
    "blanks",
    "doubles",
    "fixtures",
    "gw"
   ],
   "title": "OutlookWeek",
   "type": "object"
  },
  "OverrideRequest": {
   "description": "One pin. At least one of the two values must be present.",
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "e_min": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The manager's own expected minutes, overriding the model's."
    },
    "note": {
     "default": "",
     "description": "Why the pin was made, in the manager's own words.",
     "type": "string"
    },
    "p_play": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The manager's own P(plays), overriding the model's."
    }
   },
   "required": [
    "code",
    "e_min",
    "note",
    "p_play"
   ],
   "title": "OverrideRequest",
   "type": "object"
  },
  "OverrideRow": {
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "e_min": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The manager's own pinned expected minutes."
    },
    "model_e_min": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What the served pipeline had for his expected minutes when the pin\nwas made."
    },
    "model_p_play": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What the served pipeline had for him when the pin was made, so the\nwhy-panel can say \"the model had 0.82\" without re-deriving anything."
    },
    "name": {
     "description": "Player name, from the bootstrap snapshot.",
     "type": "string"
    },
    "note": {
     "default": "",
     "description": "Why the pin was made, in the manager's own words.",
     "type": "string"
    },
    "p_play": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The manager's own pinned P(plays)."
    },
    "set_at": {
     "default": "",
     "description": "ISO timestamp the pin was saved.",
     "type": "string"
    }
   },
   "required": [
    "code",
    "e_min",
    "model_e_min",
    "model_p_play",
    "name",
    "note",
    "p_play",
    "set_at"
   ],
   "title": "OverrideRow",
   "type": "object"
  },
  "OverridesPanel": {
   "properties": {
    "active": {
     "default": true,
     "description": "``[news] overrides``. False means the pins are stored and *not* being\napplied, which the panel says out loud rather than showing nothing.",
     "type": "boolean"
    },
    "rows": {
     "description": "Every pin on record.",
     "items": {
      "$ref": "#/definitions/OverrideRow"
     },
     "type": "array"
    },
    "warning": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Accepted, and worth a second look. Set on a write whose two numbers\ndisagree with each other \u2014 expected minutes implying a player starts,\nbeside a probability of playing that says he probably does not. A refusal\nwould be wrong (the manager is allowed to mean it) and silence would be\nworse, so the dialog shows this and stays open."
    }
   },
   "required": [
    "active",
    "rows",
    "warning"
   ],
   "title": "OverridesPanel",
   "type": "object"
  },
  "PenTrackerData": {
   "description": "``reports/pen_tracker.json``, as written by ``gaffer track-pens``.",
   "properties": {
    "gws": {
     "description": "One block per finished gameweek tracked.",
     "items": {
      "$ref": "#/definitions/PenTrackerGw"
     },
     "type": "array"
    },
    "notes": {
     "description": "Caveats the tracker recorded while building the report.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "season": {
     "default": "",
     "description": "The season the report covers.",
     "type": "string"
    },
    "season_totals": {
     "$ref": "#/definitions/PenTrackerTotals",
     "description": "The season line summed over ``gws``."
    }
   },
   "required": [
    "gws",
    "notes",
    "season",
    "season_totals"
   ],
   "title": "PenTrackerData",
   "type": "object"
  },
  "PenTrackerGw": {
   "description": "One finished gameweek of the penalty tracker.\n\nEvery field but ``gw`` is optional because ``pen_tracker.safe_gw_block``\nwrites one of two shapes: the full block, or ``{\"gw\": N, \"error\": ...}``\nwhen that week would not read. One optional-field model rather than a\nunion \u2014 a union would make the client discriminate before it can render\na row that is a row either way.",
   "properties": {
    "component_rows": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Rows in the banked components frame the taker prediction was built\nfrom."
    },
    "covered_rows": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Of those, how many the chosen instrument could actually read."
    },
    "error": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why this gameweek's block could not be built, when it could not."
    },
    "gw": {
     "description": "The gameweek this block reports on.",
     "type": "integer"
    },
    "instrument": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Which realized-penalty reader scored this week (``pen_tracker\n.realized_pens``'s choice)."
    },
    "pens_by_first_choice": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Of those, how many were taken by the club's first-choice taker."
    },
    "pens_per_team_game": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``pens_taken / team_games``, for comparison against\n:data:`LEAGUE_PENS_PG`."
    },
    "pens_taken": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Penalties actually taken this week, by the chosen instrument's count."
    },
    "predicted_ep_pen_taker": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Expected penalty points the model attributed to first-choice takers\nthis week."
    },
    "predicted_takers": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "How many players the model identified as first-choice takers."
    },
    "realized_pen_points": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Points actually scored from penalty conversions this week."
    },
    "rows": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Player rows this week's live data carried."
    },
    "taker_hit_rate": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``pens_by_first_choice / pens_taken``, or ``None`` when none were\ntaken \u2014 never 0/0 read as a miss."
    },
    "team_games": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Distinct (opponent, kickoff) fixtures this week, for the\npens-per-game rate."
    }
   },
   "required": [
    "component_rows",
    "covered_rows",
    "error",
    "gw",
    "instrument",
    "pens_by_first_choice",
    "pens_per_team_game",
    "pens_taken",
    "predicted_ep_pen_taker",
    "predicted_takers",
    "realized_pen_points",
    "rows",
    "taker_hit_rate",
    "team_games"
   ],
   "title": "PenTrackerGw",
   "type": "object"
  },
  "PenTrackerTotals": {
   "description": "The season line. All optional: a report that degraded before it\nreached a single finished gameweek writes ``{}`` here.",
   "properties": {
    "gws": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "How many gameweeks are summed into this total."
    },
    "instruments": {
     "description": "Every realized-penalty instrument used across the summed gameweeks.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "league_pens_pg_served": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The served league-wide penalties-per-game prior, for comparison."
    },
    "pens_by_first_choice": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Season total taken by first-choice takers."
    },
    "pens_per_team_game": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Season ``pens_taken / team_games``."
    },
    "pens_taken": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Season total of penalties taken."
    },
    "predicted_ep_pen_taker": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Season total of expected penalty points attributed to first-choice\ntakers."
    },
    "realized_pen_points": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Season total of points actually scored from penalty conversions."
    },
    "taker_hit_rate": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Season ``pens_by_first_choice / pens_taken``."
    },
    "team_games": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Total team-games across the season."
    }
   },
   "required": [
    "gws",
    "instruments",
    "league_pens_pg_served",
    "pens_by_first_choice",
    "pens_per_team_game",
    "pens_taken",
    "predicted_ep_pen_taker",
    "realized_pen_points",
    "taker_hit_rate",
    "team_games"
   ],
   "title": "PenTrackerTotals",
   "type": "object"
  },
  "PlanAlternative": {
   "description": "A plan the solver ranked behind the recommended one (v12 W3 \u00a74.3).",
   "properties": {
    "gap": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Objective points behind the recommended plan \u2014 **signed**.\n\nNegative means this plan prices *above* the recommendation, which happens\nbecause the recommendation carries the scenario sweep's moves as\nconstraints and this one does not. ``None`` when the artifact's number\ncould not be read; never 0.0, which is \"exactly level\"."
    },
    "label": {
     "description": "``\"Plan B\"`` / ``\"Plan C\"``, assigned by position at the router. The\nartifact stores the order and not the name, so a payload written by one\nbuild reads correctly on another.",
     "type": "string"
    },
    "weeks": {
     "description": "This alternative's plan, one entry per horizon gameweek.",
     "items": {
      "$ref": "#/definitions/PlanGw"
     },
     "type": "array"
    }
   },
   "required": [
    "gap",
    "label",
    "weeks"
   ],
   "title": "PlanAlternative",
   "type": "object"
  },
  "PlanGw": {
   "properties": {
    "bank": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What is left in the bank after this week's moves, in millions.\n\n``None`` means *unknown*, and it means it for one reason: some move in\nthis week or an earlier one had no price, so the running total is broken\nand stays broken. Never 0.0 \u2014 that is \"fully invested\", which is a real\nand different state a manager can be in."
    },
    "buys": {
     "description": "Players bought this week.",
     "items": {
      "$ref": "#/definitions/PlanMove"
     },
     "type": "array"
    },
    "captain": {
     "anyOf": [
      {
       "$ref": "#/definitions/PlanMove"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The captain this week."
    },
    "chip": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The chip played this week, if any."
    },
    "expected_pts": {
     "description": "Expected points for this week alone, net of hits.",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek this week of the plan is for.",
     "type": "integer"
    },
    "hit_cost": {
     "description": "Points those hits cost.",
     "type": "integer"
    },
    "hits": {
     "description": "Hits taken this week.",
     "type": "integer"
    },
    "sells": {
     "description": "Players sold this week.",
     "items": {
      "$ref": "#/definitions/PlanMove"
     },
     "type": "array"
    },
    "trace": {
     "anyOf": [
      {
       "$ref": "#/definitions/PlanWeekTrace"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why this week's moves, in the objective's own terms (v12 W5 \u00a76.5).\n\n``None`` only when the trace could not be computed at all \u2014 a week that\ndoes nothing carries a trace with no moves, because \"this week does\nnothing\" and \"the trace is broken\" must not look alike on the board.\n\nAlways ``None`` on an alternative plan: the trace is the objective's terms\nat the plan the solver *returned*, and Plan B was returned by a different\nsolve. The board says so under the strip."
    },
    "vice": {
     "anyOf": [
      {
       "$ref": "#/definitions/PlanMove"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The vice-captain this week."
    }
   },
   "required": [
    "bank",
    "buys",
    "captain",
    "chip",
    "expected_pts",
    "gw",
    "hit_cost",
    "hits",
    "sells",
    "trace",
    "vice"
   ],
   "title": "PlanGw",
   "type": "object"
  },
  "PlanMove": {
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "ep": {
     "description": "Expected points for the week this move is made in.",
     "type": "number"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "position": {
     "description": "GKP, DEF, MID or FWD.",
     "type": "string"
    },
    "price": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Buy price for an in, sell value for an out \u2014 in millions."
    }
   },
   "required": [
    "code",
    "ep",
    "name",
    "position",
    "price"
   ],
   "title": "PlanMove",
   "type": "object"
  },
  "PlanMoveTrace": {
   "description": "One transfer, priced against the objective's own terms (v12 W5 \u00a76.5).\n\nNot a counterfactual. ``ep_gain`` is the decayed expected-points\ndifference of a position-matched swap over the rest of the horizon \u2014 the\nobjective's own arithmetic at the plan the solver returned \u2014 and **not**\n\"the plan is this much worse without this move\", which would need a\nre-solve. ``None`` everywhere means unknown, never a measured zero.",
   "properties": {
    "buy_code": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "buy_name": {
     "default": "",
     "type": "string"
    },
    "ep_gain": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "lambda_tilt": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "note": {
     "default": "",
     "type": "string"
    },
    "sell_code": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "sell_name": {
     "default": "",
     "type": "string"
    }
   },
   "required": [
    "buy_code",
    "buy_name",
    "ep_gain",
    "lambda_tilt",
    "note",
    "sell_code",
    "sell_name"
   ],
   "title": "PlanMoveTrace",
   "type": "object"
  },
  "PlanSummary": {
   "properties": {
    "bench": {
     "description": "The bench, in order.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "buys": {
     "description": "Players bought into this plan.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "captain": {
     "$ref": "#/definitions/WirePlayerRef"
    },
    "expected_pts": {
     "description": "Raw expected points for ``gw`` alone, net of hits.",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek this plan is for.",
     "type": "integer"
    },
    "hits": {
     "description": "Hits taken to reach this plan.",
     "type": "integer"
    },
    "horizon_pts": {
     "description": "The same measure summed over the gameweeks the two plans share.",
     "type": "number"
    },
    "sells": {
     "description": "Players sold out of this plan.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "vice": {
     "$ref": "#/definitions/WirePlayerRef"
    },
    "xi": {
     "description": "The starting eleven.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    }
   },
   "required": [
    "bench",
    "buys",
    "captain",
    "expected_pts",
    "gw",
    "hits",
    "horizon_pts",
    "sells",
    "vice",
    "xi"
   ],
   "title": "PlanSummary",
   "type": "object"
  },
  "PlanWeekTrace": {
   "description": "One planned week's charges. Four of them are week-level on purpose:\na week with two transfers and one hit cannot attribute the hit to one of\nthem, and splitting it would be arithmetic dressed as a finding.\n\nThree of the objective's terms are **not** here \u2014 the XI, captain and vice\nweightings and the bench seats (``milp.py:813-835``, including\n``_decision_scales``' per-week autosub scales). They price the whole squad\nrather than a swap, so these numbers do not sum to ``expected_pts`` and\nare not meant to. The board's caption says so in the same words.",
   "properties": {
    "bank_value": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "ep_gain": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "ft_after": {
     "default": 0,
     "type": "integer"
    },
    "ft_basis": {
     "default": "flat",
     "enum": [
      "flat",
      "lambda"
     ],
     "type": "string"
    },
    "ft_shadow": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "ft_use_penalty": {
     "default": 0.0,
     "type": "number"
    },
    "ft_used": {
     "default": 0,
     "type": "integer"
    },
    "gw": {
     "type": "integer"
    },
    "hit_cost": {
     "default": 0.0,
     "type": "number"
    },
    "moves": {
     "items": {
      "$ref": "#/definitions/PlanMoveTrace"
     },
     "type": "array"
    },
    "note": {
     "default": "",
     "type": "string"
    },
    "price_charge": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "theta": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    }
   },
   "required": [
    "bank_value",
    "ep_gain",
    "ft_after",
    "ft_basis",
    "ft_shadow",
    "ft_use_penalty",
    "ft_used",
    "gw",
    "hit_cost",
    "moves",
    "note",
    "price_charge",
    "theta"
   ],
   "title": "PlanWeekTrace",
   "type": "object"
  },
  "PresserGradesData": {
   "description": "v12 \u00a73.2's readout, or its refusal.\n\n``recall_population`` is a field rather than a footnote: recall here is\nover the rows that carried a verdict, and the same word over every absent\nplayer in the gameweek would be a much harsher number about a much larger\npopulation.",
   "properties": {
    "absent_rows": {
     "default": 0,
     "description": "Players with no presser verdict at all, over the graded gameweeks.",
     "type": "integer"
    },
    "available": {
     "default": false,
     "description": "Whether enough verdicts have been graded to report.",
     "type": "boolean"
    },
    "by_source": {
     "default": [],
     "description": "How many verdicts came from each presser source.",
     "items": {
      "$ref": "#/definitions/SourceRows"
     },
     "type": "array"
    },
    "confusion": {
     "default": [],
     "description": "Started/not-started counts, one row per verdict.",
     "items": {
      "$ref": "#/definitions/VerdictRow"
     },
     "type": "array"
    },
    "git_sha": {
     "description": "The commit the readout was built from.",
     "type": "string"
    },
    "graded_gws": {
     "default": [],
     "description": "Gameweeks with at least one graded verdict.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why the report is empty, when it is."
    },
    "per_class": {
     "default": [],
     "description": "Precision and recall, one row per verdict.",
     "items": {
      "$ref": "#/definitions/VerdictScore"
     },
     "type": "array"
    },
    "recall_population": {
     "default": "verdict-carrying rows",
     "description": "What ``recall`` is measured over, stated so the number is not read\nagainst a larger population by mistake.",
     "type": "string"
    },
    "rows": {
     "default": 0,
     "description": "How many verdicts the tables below are built from.",
     "type": "integer"
    },
    "run_at": {
     "description": "When this readout was built.",
     "type": "string"
    },
    "verdicts_banked": {
     "default": 0,
     "description": "Total presser verdicts on record, graded or not.",
     "type": "integer"
    }
   },
   "required": [
    "absent_rows",
    "available",
    "by_source",
    "confusion",
    "git_sha",
    "graded_gws",
    "note",
    "per_class",
    "recall_population",
    "rows",
    "run_at",
    "verdicts_banked"
   ],
   "title": "PresserGradesData",
   "type": "object"
  },
  "PricePoint": {
   "properties": {
    "gw": {
     "description": "The gameweek this price was recorded at.",
     "type": "integer"
    },
    "price": {
     "description": "The player's price in \u00a3m.",
     "type": "number"
    }
   },
   "required": [
    "gw",
    "price"
   ],
   "title": "PricePoint",
   "type": "object"
  },
  "PriceSeries": {
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "points": {
     "description": "His price at every gameweek he was owned.",
     "items": {
      "$ref": "#/definitions/PricePoint"
     },
     "type": "array"
    }
   },
   "required": [
    "code",
    "name",
    "points"
   ],
   "title": "PriceSeries",
   "type": "object"
  },
  "PrivateLeagueRow": {
   "description": "One private mini-league the entry is in (v15 \u00a75.1).",
   "properties": {
    "entries": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``rank_count`` from the entry payload; ``None`` before the league has\na scored gameweek."
    },
    "gap": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Points to the entry immediately ahead or behind, in the direction\n``gap_kind`` names."
    },
    "gap_kind": {
     "anyOf": [
      {
       "enum": [
        "ahead",
        "behind"
       ],
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Whether the leader is ahead of the entry or the entry leads it."
    },
    "is_focus": {
     "description": "Whether this is the configured focus league.",
     "type": "boolean"
    },
    "last_rank": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Rank as of the previous gameweek."
    },
    "league_id": {
     "description": "The league's FPL id.",
     "type": "integer"
    },
    "name": {
     "description": "The league's name.",
     "type": "string"
    },
    "rank": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Current rank in this league."
    },
    "started": {
     "description": "Whether the league has a scored gameweek yet.",
     "type": "boolean"
    },
    "would": {
     "anyOf": [
      {
       "enum": [
        "chase",
        "defend"
       ],
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Gap-sign only \u2014 what the dial would lean to. The deadband and \u03bb\nappear when the league is opened (``/race?league_id=``)."
    }
   },
   "required": [
    "entries",
    "gap",
    "gap_kind",
    "is_focus",
    "last_rank",
    "league_id",
    "name",
    "rank",
    "started",
    "would"
   ],
   "title": "PrivateLeagueRow",
   "type": "object"
  },
  "PublicLeagueRow": {
   "description": "A public or system league: rank line only (v15 \u00a71.1).",
   "properties": {
    "entries": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Number of entries in the league."
    },
    "last_rank": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Rank as of the previous gameweek."
    },
    "league_id": {
     "description": "The league's FPL id.",
     "type": "integer"
    },
    "name": {
     "description": "The league's name.",
     "type": "string"
    },
    "rank": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Current rank in this league."
    }
   },
   "required": [
    "entries",
    "last_rank",
    "league_id",
    "name",
    "rank"
   ],
   "title": "PublicLeagueRow",
   "type": "object"
  },
  "QualityData": {
   "description": "Whichever modes have been run. Each is independent and may be absent.",
   "properties": {
    "benchmark": {
     "anyOf": [
      {
       "$ref": "#/definitions/BenchmarkEvaluation"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The newest benchmark against published references, or ``None``."
    },
    "current": {
     "anyOf": [
      {
       "$ref": "#/definitions/CurrentEvaluation"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The newest holdout evaluation, or ``None`` if none has been run."
    },
    "decomposition": {
     "anyOf": [
      {
       "$ref": "#/definitions/DecompositionData"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The newest forecast-vs-planning replay decomposition, or ``None``."
    },
    "flag_latency": {
     "anyOf": [
      {
       "$ref": "#/definitions/FlagLatencyData"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "v12 \u00a73.1's flag-latency readout, or ``None`` if never scored."
    },
    "news_shadow": {
     "anyOf": [
      {
       "$ref": "#/definitions/NewsShadowData"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Gate N2's standing readout, or ``None`` if never scored."
    },
    "presser_grades": {
     "anyOf": [
      {
       "$ref": "#/definitions/PresserGradesData"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "v12 \u00a73.2's presser-verdict readout, or ``None`` if never scored."
    }
   },
   "required": [
    "benchmark",
    "current",
    "decomposition",
    "flag_latency",
    "news_shadow",
    "presser_grades"
   ],
   "title": "QualityData",
   "type": "object"
  },
  "ReasonTally": {
   "description": "One reason code's row in the season tally: how many graded gameweeks\ncarried it, and the mean transfers-lane delta over them.",
   "properties": {
    "count": {
     "description": "How many graded gameweeks carried this reason.",
     "type": "integer"
    },
    "mean_delta_pts": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Mean transfers-lane points delta over those gameweeks."
    },
    "reason": {
     "description": "The deviation reason code.",
     "type": "string"
    }
   },
   "required": [
    "count",
    "mean_delta_pts",
    "reason"
   ],
   "title": "ReasonTally",
   "type": "object"
  },
  "ReferenceMetrics": {
   "description": "A published number: no row count, because we did not measure it.",
   "properties": {
    "mae": {
     "description": "The published mean absolute error.",
     "type": "number"
    },
    "rmse": {
     "description": "The published root-mean-square error.",
     "type": "number"
    }
   },
   "required": [
    "mae",
    "rmse"
   ],
   "title": "ReferenceMetrics",
   "type": "object"
  },
  "ReliabilityBin": {
   "properties": {
    "n": {
     "description": "How many predictions fell in this bin.",
     "type": "integer"
    },
    "obs": {
     "description": "Observed frequency in this bin \u2014 what a well-calibrated head would\nmatch to ``pred``.",
     "type": "number"
    },
    "pred": {
     "description": "Mean predicted probability in this bin.",
     "type": "number"
    }
   },
   "required": [
    "n",
    "obs",
    "pred"
   ],
   "title": "ReliabilityBin",
   "type": "object"
  },
  "ReviewAccuracyPoint": {
   "properties": {
    "accuracy": {
     "description": "That gameweek's ``ReviewGw.accuracy``.",
     "type": "integer"
    },
    "gw": {
     "description": "The gameweek this accuracy point is for.",
     "type": "integer"
    }
   },
   "required": [
    "accuracy",
    "gw"
   ],
   "title": "ReviewAccuracyPoint",
   "type": "object"
  },
  "ReviewGw": {
   "description": "One gameweek's banked grade. Every field but ``gw`` has a default, so\na ledger written by an older build still renders.",
   "properties": {
    "accuracy": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``my_points`` as a percentage of ``model_points``, capped at 100."
    },
    "chip": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The chip I played this gameweek, if any."
    },
    "decision": {
     "anyOf": [
      {
       "$ref": "#/definitions/DecisionRef"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why I did something other than what the advice said. ``None`` is \"no\nnote was written\", which is not the same as a note with no reason."
    },
    "gw": {
     "description": "The gameweek this grade is for.",
     "type": "integer"
    },
    "hindsight": {
     "$ref": "#/definitions/ReviewHindsight",
     "description": "The best legal eleven I could have picked from my actual fifteen."
    },
    "hits": {
     "default": 0,
     "description": "Hits I took this gameweek.",
     "type": "integer"
    },
    "lanes": {
     "description": "Every decision lane graded this gameweek.",
     "items": {
      "$ref": "#/definitions/ReviewLane"
     },
     "type": "array"
    },
    "misses": {
     "description": "Moves the model flagged that I did not make and that paid off anyway.",
     "items": {
      "$ref": "#/definitions/ReviewMiss"
     },
     "type": "array"
    },
    "model_chip": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The chip the model would have played this gameweek, if any."
    },
    "model_points": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Points the model's own plan would have scored this gameweek."
    },
    "my_points": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "My XI's points, captain doubled, no autosubs \u2014 the review's own\nreading, independent of FPL's official figure."
    },
    "no_advice": {
     "default": false,
     "description": "No advice existed for this gameweek to grade against.",
     "type": "boolean"
    },
    "notices": {
     "description": "Caveats on this gameweek's grade.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "official_gross": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "FPL's own points figure before hits are subtracted."
    },
    "official_points": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "FPL's own points figure for this gameweek, net of any hits."
    },
    "our_bench_points": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Points scored by the players the model's own plan would have benched."
    },
    "overall_rank": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "My overall FPL rank at the end of this gameweek.\n\n``None`` for two situations the reader must not see merged: a gameweek\nwhose entry history was never banked, and \u2014 for the whole of this season's\nexisting ledger \u2014 **a gameweek graded before the field existed.** Grades\nare banked and never re-derived (spec D2), so the trajectory begins empty\nand fills forward from the next graded week. A chart drawing this must\nshow a gap, never a zero and never a line through it: zero is the best\nrank in the game."
    },
    "points_on_bench": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Points scored by players left on my bench."
    },
    "post_deadline": {
     "default": false,
     "description": "Every banked run of this gameweek was written after its deadline.",
     "type": "boolean"
    },
    "projection_post_deadline": {
     "default": false,
     "description": "The snapshot named above cannot be trusted to predate the deadline.\n\n**Two causes, and the reader must not be told only the first.** Either\nevery snapshot for the gameweek was written after the deadline, or the\nrun did not record when the deadline was \u2014 which is the ordinary state of\na gameweek graded late, after ``ADVICE_HISTORY_KEEP`` pruned the payload\nthe deadline was carried on. In that second case an in-time snapshot may\nwell exist on disk; there is simply nothing left to compare its stamp\nagainst, and guessing would be worse than saying so.\n\nEither way the claim is the same and it is the weaker one: this table may\nhave seen team news nobody could act on. Related to ``post_deadline``\nabove \u2014 which is about the advice payload rather than the EP table \u2014 and\nthe two can disagree.",
     "type": "boolean"
    },
    "projection_snapshot": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The UTC stamp of the frozen EP table this grade was read against.\n\n``None`` for a gameweek graded before v12 W5 existed, and for one where no\nsnapshot was ever written. Grades are banked and never re-derived (spec\nD2), so every row already in the ledger keeps ``None`` for ever and the\ncolumn fills forward from the next graded week \u2014 drawn as absent, never as\na zero or a blank that reads like one."
    },
    "pwin_granularity_pp": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The smallest ``delta_pwin`` this simulation could resolve, in\npercentage points \u2014 ``100 / pwin_n``."
    },
    "pwin_n": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "How many Monte Carlo draws the lanes' ``delta_pwin`` figures were\nsimulated over."
    },
    "pwin_seed": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The RNG seed those draws used."
    },
    "reconciled": {
     "anyOf": [
      {
       "type": "boolean"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Whether ``my_points`` (net of hits) matches ``official_points``, or\n``None`` when the entry history was never banked."
    },
    "reviewed_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "ISO timestamp this grade was banked."
    }
   },
   "required": [
    "accuracy",
    "chip",
    "decision",
    "gw",
    "hindsight",
    "hits",
    "lanes",
    "misses",
    "model_chip",
    "model_points",
    "my_points",
    "no_advice",
    "notices",
    "official_gross",
    "official_points",
    "our_bench_points",
    "overall_rank",
    "points_on_bench",
    "post_deadline",
    "projection_post_deadline",
    "projection_snapshot",
    "pwin_granularity_pp",
    "pwin_n",
    "pwin_seed",
    "reconciled",
    "reviewed_at"
   ],
   "title": "ReviewGw",
   "type": "object"
  },
  "ReviewHindsight": {
   "description": "The best legal eleven out of the fifteen I owned, by actual points.\n\n``points`` and ``gap`` are ``None`` \u2014 never zero \u2014 when no legal eleven\ncould be built at all, which is what a fifteen the results frame does not\ncover looks like. A zero there would bank a *negative* gap.",
   "properties": {
    "captain": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "That eleven's best captain choice, by code."
    },
    "gap": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``points`` minus what my actual eleven scored."
    },
    "points": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Points the best legal eleven from my fifteen would have scored."
    },
    "xi": {
     "description": "That eleven's player codes.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    }
   },
   "required": [
    "captain",
    "gap",
    "points",
    "xi"
   ],
   "title": "ReviewHindsight",
   "type": "object"
  },
  "ReviewLane": {
   "description": "One graded decision lane (spec D5).\n\n``delta_pts`` and ``label`` are ``None`` \u2014 never zero \u2014 for a lane that\ncould not be built: the model's captain was not in my eleven, the model\nsold a player I never owned, either side played a wildcard. \"The model had\nno opinion I could have acted on\" and \"the model agreed with me\" are\ndifferent facts and the UI colours them differently.",
   "properties": {
    "aligned": {
     "default": false,
     "description": "Whether my choice and the model's agreed on this lane.",
     "type": "boolean"
    },
    "delta_pts": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "My choice's points minus the model's, on this lane."
    },
    "delta_pwin": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "My choice minus the model's, in percentage points of P(win the\nleague). ``0.0`` on the bench and chip lanes by construction \u2014 the\nsimulation normalises every squad to its eleven and one armband."
    },
    "label": {
     "anyOf": [
      {
       "enum": [
        "Brilliant",
        "Good",
        "Aligned",
        "Inaccuracy",
        "Blunder"
       ],
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The lane's verdict in one word, from the points delta's band."
    },
    "lane": {
     "description": "Which decision this lane grades.",
     "enum": [
      "transfers",
      "captaincy",
      "bench",
      "chip"
     ],
     "type": "string"
    },
    "mine": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What I did, in prose."
    },
    "model": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What the model would have done, in prose."
    },
    "note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why the lane could not be graded, when ``delta_pts`` is ``None``."
    }
   },
   "required": [
    "aligned",
    "delta_pts",
    "delta_pwin",
    "label",
    "lane",
    "mine",
    "model",
    "note"
   ],
   "title": "ReviewLane",
   "type": "object"
  },
  "ReviewLaneTotal": {
   "properties": {
    "graded": {
     "default": 0,
     "description": "How many gameweeks this lane was gradeable in. ``pts`` of zero over\n``graded`` of zero is \"never measured\", not \"never wrong\".",
     "type": "integer"
    },
    "losses": {
     "default": 0,
     "description": "Graded weeks this lane gained / lost points, counted strictly.\n\nA zero delta is neither, so ``wins + losses <= graded`` with slack \u2014 the\ndifference is the weeks I did exactly what the model did. A UI that\nrendered ``wins / (wins + losses)`` would silently drop those weeks; the\ndenominator is ``graded``.",
     "type": "integer"
    },
    "pts": {
     "default": 0.0,
     "description": "Total points delta on this lane, summed over graded gameweeks.",
     "type": "number"
    },
    "pwin": {
     "default": 0.0,
     "description": "Total P(win) delta on this lane, summed over graded gameweeks.",
     "type": "number"
    },
    "wins": {
     "default": 0,
     "description": "Graded weeks this lane gained points over the model.",
     "type": "integer"
    }
   },
   "required": [
    "graded",
    "losses",
    "pts",
    "pwin",
    "wins"
   ],
   "title": "ReviewLaneTotal",
   "type": "object"
  },
  "ReviewMiss": {
   "description": "A move the model flagged, I did not make, and that returned anyway.",
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "gain": {
     "description": "Points this move would have gained over what I actually did.",
     "type": "integer"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "over": {
     "description": "The player he was recommended over.",
     "type": "string"
    }
   },
   "required": [
    "code",
    "gain",
    "name",
    "over"
   ],
   "title": "ReviewMiss",
   "type": "object"
  },
  "RivalBeat": {
   "properties": {
    "entry": {
     "description": "FPL entry id.",
     "type": "integer"
    },
    "name": {
     "description": "The rival's team name.",
     "type": "string"
    },
    "p_beat": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``None`` when the entry's squad could not be read at all (private, or\njoined after the gameweek). Such an entry is listed but not simulated \u2014\nsee ``league_sim.is_readable`` \u2014 and the card renders a dash."
    }
   },
   "required": [
    "entry",
    "name",
    "p_beat"
   ],
   "title": "RivalBeat",
   "type": "object"
  },
  "RivalDetailData": {
   "properties": {
    "captain": {
     "anyOf": [
      {
       "$ref": "#/definitions/SquadPlayer"
      },
      {
       "type": "null"
      }
     ],
     "description": "The rival's captain in ``squad_gw``."
    },
    "chips_used": {
     "description": "Chips the rival has played this season.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "entry": {
     "description": "FPL entry id.",
     "type": "integer"
    },
    "live_points": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "description": "The rival's in-play points for the current gameweek, when one is\nlive; ``None`` otherwise."
    },
    "name": {
     "description": "The rival's team name.",
     "type": "string"
    },
    "player_name": {
     "description": "The rival manager's own name.",
     "type": "string"
    },
    "shared": {
     "description": "Players both the rival and the manager own.",
     "items": {
      "$ref": "#/definitions/SquadPlayer"
     },
     "type": "array"
    },
    "squad": {
     "description": "The rival's full squad for ``squad_gw``.",
     "items": {
      "$ref": "#/definitions/SquadPlayer"
     },
     "type": "array"
    },
    "squad_gw": {
     "description": "The gameweek the served squad was picked for.",
     "type": "integer"
    },
    "team_value": {
     "description": "The rival's squad value in \u00a3m.",
     "type": "number"
    },
    "their_differentials": {
     "description": "Players the rival owns that the manager does not.",
     "items": {
      "$ref": "#/definitions/SquadPlayer"
     },
     "type": "array"
    },
    "total": {
     "description": "The rival's total points to date.",
     "type": "integer"
    },
    "your_differentials": {
     "description": "Players the manager owns that the rival does not.",
     "items": {
      "$ref": "#/definitions/SquadPlayer"
     },
     "type": "array"
    }
   },
   "required": [
    "captain",
    "chips_used",
    "entry",
    "live_points",
    "name",
    "player_name",
    "shared",
    "squad",
    "squad_gw",
    "team_value",
    "their_differentials",
    "total",
    "your_differentials"
   ],
   "title": "RivalDetailData",
   "type": "object"
  },
  "RivalSummary": {
   "properties": {
    "differentials": {
     "description": "Players in the rival's squad the manager does not own.",
     "type": "integer"
    },
    "entry": {
     "description": "FPL entry id.",
     "type": "integer"
    },
    "event_total": {
     "description": "The rival's points in the newest gameweek.",
     "type": "integer"
    },
    "name": {
     "description": "The rival's team name.",
     "type": "string"
    },
    "overlap": {
     "description": "Players shared with the manager's own squad.",
     "type": "integer"
    },
    "player_name": {
     "description": "The rival manager's own name.",
     "type": "string"
    },
    "rank": {
     "description": "The rival's current league rank.",
     "type": "integer"
    },
    "total": {
     "description": "The rival's total points to date.",
     "type": "integer"
    }
   },
   "required": [
    "differentials",
    "entry",
    "event_total",
    "name",
    "overlap",
    "player_name",
    "rank",
    "total"
   ],
   "title": "RivalSummary",
   "type": "object"
  },
  "SensitivityMove": {
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "count": {
     "description": "How many of the sweep's re-solves made this move.",
     "type": "integer"
    },
    "frequency": {
     "description": "``count`` divided by the number of completed re-solves.",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek the move is made in.",
     "type": "integer"
    },
    "kind": {
     "description": "Which decision this move is: a buy, a sell or the captaincy.",
     "type": "string"
    },
    "label": {
     "description": "The move described in prose, for the frequency table.",
     "type": "string"
    },
    "name": {
     "default": "",
     "description": "Player name.",
     "type": "string"
    }
   },
   "required": [
    "code",
    "count",
    "frequency",
    "gw",
    "kind",
    "label",
    "name"
   ],
   "title": "SensitivityMove",
   "type": "object"
  },
  "SensitivityPlan": {
   "properties": {
    "buys": {
     "description": "Players this plan buys.",
     "items": {
      "$ref": "#/definitions/NamedPlayer"
     },
     "type": "array"
    },
    "captain": {
     "anyOf": [
      {
       "$ref": "#/definitions/NamedPlayer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "This plan's captain."
    },
    "count": {
     "description": "How many of the sweep's re-solves reached this exact plan.",
     "type": "integer"
    },
    "hits": {
     "default": 0,
     "description": "Hits this plan takes.",
     "type": "integer"
    },
    "sells": {
     "description": "Players this plan sells.",
     "items": {
      "$ref": "#/definitions/NamedPlayer"
     },
     "type": "array"
    },
    "value": {
     "default": 0.0,
     "description": "Horizon expected points on the **true** EP table, so two signatures are\ncompared on the board the manager faces rather than on their own draws.",
     "type": "number"
    }
   },
   "required": [
    "buys",
    "captain",
    "count",
    "hits",
    "sells",
    "value"
   ],
   "title": "SensitivityPlan",
   "type": "object"
  },
  "SensitivityReport": {
   "properties": {
    "available": {
     "default": false,
     "description": "Whether a sweep for this gameweek was found and is current.",
     "type": "boolean"
    },
    "completed": {
     "default": 0,
     "description": "How many re-solves actually finished.",
     "type": "integer"
    },
    "decision_sigma": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The scenario sweep's own *estimation* noise on the players that\nseparate the two plans, in quadrature (plan A6).\n\nNot the \u03c3 behind the EP bands, and the difference is the point. A band\nanswers \"what might he score\" and is dominated by football's own variance.\nThis answers \"how wrong might my forecast be\" \u2014 the only question a margin\nbetween two plans solved off the same board can be threatened by \u2014 and so\nit stays on ``optimize.scenarios``' calibrated table alone.\n\nComputed at serve time from the banked components frame rather than stored\nin the report, so a report swept before this field existed still gets the\nline. ``None`` when there is no runner-up, no components frame, or nothing\nin the symmetric difference \u2014 the card then prints its margin unqualified,\nwhich is what it did before."
    },
    "failures": {
     "default": 0,
     "description": "How many re-solves failed.",
     "type": "integer"
    },
    "frequencies": {
     "description": "The first horizon week's moves, ranked by how often the sweep made\nthem.",
     "items": {
      "$ref": "#/definitions/SensitivityMove"
     },
     "type": "array"
    },
    "generated_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "ISO timestamp the sweep was run."
    },
    "gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The gameweek the report is about."
    },
    "horizon": {
     "default": 0,
     "description": "How many gameweeks the sweep solved over.",
     "type": "integer"
    },
    "k": {
     "default": 0,
     "description": "How many re-solves the sweep asked for.",
     "type": "integer"
    },
    "margin": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``modal``'s value minus ``runner_up``'s, on the true EP table."
    },
    "modal": {
     "anyOf": [
      {
       "$ref": "#/definitions/SensitivityPlan"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The plan the sweep reached most often."
    },
    "notice": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "A caveat on the sweep, e.g. that expected minutes had to be guessed."
    },
    "runner_up": {
     "anyOf": [
      {
       "$ref": "#/definitions/SensitivityPlan"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The next most frequent *distinct* plan, or ``None`` when every\nre-solve agreed."
    },
    "seed": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The RNG seed the sweep's scenarios were drawn with."
    },
    "verdict": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The sweep's one-line summary of the modal plan and the runner-up."
    },
    "wall_s": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Seconds the sweep took."
    }
   },
   "required": [
    "available",
    "completed",
    "decision_sigma",
    "failures",
    "frequencies",
    "generated_at",
    "gw",
    "horizon",
    "k",
    "margin",
    "modal",
    "notice",
    "runner_up",
    "seed",
    "verdict",
    "wall_s"
   ],
   "title": "SensitivityReport",
   "type": "object"
  },
  "ServedAlternative": {
   "description": "A plan the solver ranked behind the recommended one (v12 W3 \u00a74.3),\nunder the advice's own ``alternative_plans`` key and shape.",
   "properties": {
    "gap": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "plan_by_gw": {
     "items": {
      "$ref": "#/definitions/ServedWeek"
     },
     "type": "array"
    }
   },
   "required": [
    "gap",
    "plan_by_gw"
   ],
   "title": "ServedAlternative",
   "type": "object"
  },
  "ServedMove": {
   "description": "A player on a move or in the XI, in ``advise._named``'s shape plus\nthe price the pool gives him (millions; ``None`` when it cannot) and\nthe decorations advise adds to a served buy or sell.",
   "properties": {
    "code": {
     "type": "integer"
    },
    "ep": {
     "default": 0.0,
     "type": "number"
    },
    "frequency": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "name": {
     "default": "",
     "type": "string"
    },
    "position": {
     "default": "",
     "type": "string"
    },
    "price": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "tag": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    }
   },
   "required": [
    "code",
    "ep",
    "name",
    "position",
    "price"
   ],
   "title": "ServedMove",
   "type": "object"
  },
  "ServedObjective": {
   "description": "The solver's own week one, kept beside the served plan (v16 \u00a74),\nand the same week priced, banked and traced under ``week`` so the\nboard's objective column is carried rather than rebuilt.",
   "properties": {
    "buys": {
     "items": {
      "$ref": "#/definitions/ServedMove"
     },
     "type": "array"
    },
    "expected_pts": {
     "default": 0.0,
     "type": "number"
    },
    "hits": {
     "default": 0,
     "type": "integer"
    },
    "line": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "sells": {
     "items": {
      "$ref": "#/definitions/ServedMove"
     },
     "type": "array"
    },
    "week": {
     "anyOf": [
      {
       "$ref": "#/definitions/ServedWeek"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    }
   },
   "required": [
    "buys",
    "expected_pts",
    "hits",
    "line",
    "sells",
    "week"
   ],
   "title": "ServedObjective",
   "type": "object"
  },
  "ServedPlan": {
   "description": "Field names are the advice JSON's own keys: ``model_dump`` is the\nwrite and ``model_validate(advice_dict)`` is the read.",
   "properties": {
    "alternative_plans": {
     "items": {
      "$ref": "#/definitions/ServedAlternative"
     },
     "type": "array"
    },
    "bank": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "bench": {
     "items": {
      "$ref": "#/definitions/ServedMove"
     },
     "type": "array"
    },
    "buys": {
     "items": {
      "$ref": "#/definitions/ServedMove"
     },
     "type": "array"
    },
    "captain": {
     "anyOf": [
      {
       "$ref": "#/definitions/ServedMove"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "captain_note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "expected_pts": {
     "default": 0.0,
     "type": "number"
    },
    "generated_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "gw": {
     "type": "integer"
    },
    "hits": {
     "default": 0,
     "type": "integer"
    },
    "objective": {
     "anyOf": [
      {
       "$ref": "#/definitions/ServedObjective"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "plan_by_gw": {
     "items": {
      "$ref": "#/definitions/ServedWeek"
     },
     "type": "array"
    },
    "restraint": {
     "anyOf": [
      {
       "$ref": "#/definitions/ServedRestraint"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "sells": {
     "items": {
      "$ref": "#/definitions/ServedMove"
     },
     "type": "array"
    },
    "vice": {
     "anyOf": [
      {
       "$ref": "#/definitions/ServedMove"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "xi": {
     "items": {
      "$ref": "#/definitions/ServedMove"
     },
     "type": "array"
    }
   },
   "required": [
    "alternative_plans",
    "bank",
    "bench",
    "buys",
    "captain",
    "captain_note",
    "expected_pts",
    "generated_at",
    "gw",
    "hits",
    "objective",
    "plan_by_gw",
    "restraint",
    "sells",
    "vice",
    "xi"
   ],
   "title": "ServedPlan",
   "type": "object"
  },
  "ServedRestraint": {
   "properties": {
    "agrees": {
     "default": true,
     "type": "boolean"
    },
    "bar": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "chosen": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "hit_cost": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "label": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "line": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "steps": {
     "items": {
      "$ref": "#/definitions/ServedStep"
     },
     "type": "array"
    }
   },
   "required": [
    "agrees",
    "bar",
    "chosen",
    "hit_cost",
    "label",
    "line",
    "note",
    "steps"
   ],
   "title": "ServedRestraint",
   "type": "object"
  },
  "ServedStep": {
   "description": "One step of the ladder's restraint walk (v16 \u00a73), as the ladder\nwrites it, with v17b's served sentence.",
   "properties": {
    "above": {
     "type": "string"
    },
    "below": {
     "type": "string"
    },
    "line": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "reason": {
     "default": "",
     "type": "string"
    },
    "reason_kind": {
     "default": "",
     "type": "string"
    },
    "share": {
     "type": "number"
    },
    "taken": {
     "type": "boolean"
    }
   },
   "required": [
    "above",
    "below",
    "line",
    "reason",
    "reason_kind",
    "share",
    "taken"
   ],
   "title": "ServedStep",
   "type": "object"
  },
  "ServedWeek": {
   "properties": {
    "bank": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "buys": {
     "items": {
      "$ref": "#/definitions/ServedMove"
     },
     "type": "array"
    },
    "chip": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    },
    "expected_pts": {
     "default": 0.0,
     "type": "number"
    },
    "gw": {
     "type": "integer"
    },
    "hit_cost": {
     "default": 0,
     "type": "integer"
    },
    "hits": {
     "default": 0,
     "type": "integer"
    },
    "sells": {
     "items": {
      "$ref": "#/definitions/ServedMove"
     },
     "type": "array"
    },
    "trace": {
     "anyOf": [
      {
       "$ref": "#/definitions/PlanWeekTrace"
      },
      {
       "type": "null"
      }
     ],
     "default": null
    }
   },
   "required": [
    "bank",
    "buys",
    "chip",
    "expected_pts",
    "gw",
    "hit_cost",
    "hits",
    "sells",
    "trace"
   ],
   "title": "ServedWeek",
   "type": "object"
  },
  "SettingOption": {
   "description": "One value a select offers and the word for it (v17e \u00a72.4).\n\nServed rather than hard-coded on the client so that the words a manager\npicks between are stated once, beside the bound they live inside.",
   "properties": {
    "label": {
     "description": "The word the select shows for it.",
     "type": "string"
    },
    "value": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "integer"
      }
     ],
     "description": "The value this option sets."
    }
   },
   "required": [
    "label",
    "value"
   ],
   "title": "SettingOption",
   "type": "object"
  },
  "SettingRow": {
   "description": "One editable setting, as the Settings tab receives it (v12 W5 \u00a76.2).",
   "properties": {
    "choices": {
     "description": "For ``kind == \"choice\"`` the allowed strings, in display order (v15\n\u00a74.2). Empty for every other kind.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "help": {
     "description": "A short explanation of what the setting does.",
     "type": "string"
    },
    "hi": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The upper bound the tab should enforce, when the setting has one."
    },
    "key": {
     "description": "The config field this row edits, dotted to its section.",
     "type": "string"
    },
    "kind": {
     "description": "What kind of control the tab should render for it.",
     "enum": [
      "int",
      "float",
      "bool",
      "floats3",
      "pool",
      "choice"
     ],
     "type": "string"
    },
    "label": {
     "description": "The setting's name in prose.",
     "type": "string"
    },
    "lo": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The lower bound the tab should enforce, when the setting has one."
    },
    "options": {
     "description": "What a select offers, in order, the saved value included when it is\nnot offered (v17e \u00a72.5). Empty for a row the tab types into.",
     "items": {
      "$ref": "#/definitions/SettingOption"
     },
     "type": "array"
    },
    "section": {
     "description": "The config section the field lives in, for grouping on the tab.",
     "type": "string"
    },
    "source": {
     "description": "Which file this value came from. ``local`` is ``config.local.toml``,\n``base`` is ``config.toml``, ``default`` is the dataclass \u2014 and the three\nare different facts: only a ``local`` value can be reset.",
     "enum": [
      "local",
      "base",
      "default"
     ],
     "type": "string"
    },
    "value": {
     "description": "Whatever the merged config holds. ``None`` only for ``bench_curve``,\nwhere it means \"no curve \u2014 one flat bench weight\", which is a real\nsetting and not an absent one.",
     "tsType": "unknown"
    }
   },
   "required": [
    "choices",
    "help",
    "hi",
    "key",
    "kind",
    "label",
    "lo",
    "options",
    "section",
    "source",
    "value"
   ],
   "title": "SettingRow",
   "type": "object"
  },
  "SettingWrite": {
   "properties": {
    "key": {
     "description": "The config field to write, dotted to its section.",
     "type": "string"
    },
    "value": {
     "default": null,
     "description": "``None`` removes the key from the overlay, so the value falls back to\n``config.toml`` or the dataclass default.",
     "tsType": "unknown"
    }
   },
   "required": [
    "key",
    "value"
   ],
   "title": "SettingWrite",
   "type": "object"
  },
  "SettingsPanel": {
   "properties": {
    "apply_note": {
     "description": "The standing note on when a saved change takes effect.",
     "type": "string"
    },
    "overlay_error": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why ``config.local.toml`` is being ignored, or ``None``. Also carries\nthe \"no config.toml at all\" case, which is the state a cold clone is in."
    },
    "rows": {
     "items": {
      "$ref": "#/definitions/SettingRow"
     },
     "type": "array"
    },
    "unavailable": {
     "description": "Whitelisted settings this build's ``Config`` does not have. Named\nrather than dropped: a form that is quietly shorter is a setting nobody\ncan find and nobody knows is missing.",
     "items": {
      "type": "string"
     },
     "type": "array"
    }
   },
   "required": [
    "apply_note",
    "overlay_error",
    "rows",
    "unavailable"
   ],
   "title": "SettingsPanel",
   "type": "object"
  },
  "SimPoint": {
   "description": "One banked gameweek of the headline, for the card's sparkline.",
   "properties": {
    "exp_finish": {
     "description": "Mean simulated final rank.",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek this simulated headline was run for.",
     "type": "integer"
    },
    "p_top3": {
     "description": "Modelled P(finishes top 3).",
     "type": "number"
    },
    "p_win": {
     "description": "Modelled P(finishes first), from the Monte Carlo league sim.",
     "type": "number"
    },
    "run_at": {
     "description": "ISO timestamp the simulation was run.",
     "type": "string"
    }
   },
   "required": [
    "exp_finish",
    "gw",
    "p_top3",
    "p_win",
    "run_at"
   ],
   "title": "SimPoint",
   "type": "object"
  },
  "SourceHealth": {
   "properties": {
    "age_hours": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "description": "Hours since ``modified_at``, or ``None`` when the source is absent."
    },
    "modified_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "description": "When it was last written, or ``None`` if it never was."
    },
    "path": {
     "description": "Where it lives under ``data/``, for ``_stat`` in `routers/meta.py`.",
     "type": "string"
    },
    "present": {
     "description": "Whether the file exists on disk.",
     "type": "boolean"
    },
    "source": {
     "description": "The ingested source's name, from ``DATA_SOURCES`` or the odds bank.",
     "type": "string"
    }
   },
   "required": [
    "age_hours",
    "modified_at",
    "path",
    "present",
    "source"
   ],
   "title": "SourceHealth",
   "type": "object"
  },
  "SourceRows": {
   "properties": {
    "rows": {
     "description": "How many verdicts came from this source.",
     "type": "integer"
    },
    "source": {
     "description": "The presser source's name.",
     "type": "string"
    }
   },
   "required": [
    "rows",
    "source"
   ],
   "title": "SourceRows",
   "type": "object"
  },
  "SquadDiff": {
   "description": "A candidate squad against the one you own, resolved server-side.",
   "properties": {
    "added": {
     "description": "Players the candidate squad brings in that were not owned.",
     "items": {
      "$ref": "#/definitions/SquadPlayerRef"
     },
     "type": "array"
    },
    "dropped": {
     "description": "Owned players the candidate squad does not keep, priced at sell value.",
     "items": {
      "$ref": "#/definitions/SquadPlayerRef"
     },
     "type": "array"
    },
    "gain_over_horizon": {
     "description": "Expected-points gain of the candidate squad over the owned one,\nsummed across the solve horizon.",
     "type": "number"
    },
    "kept": {
     "description": "Owned players the candidate squad keeps.",
     "items": {
      "$ref": "#/definitions/SquadPlayerRef"
     },
     "type": "array"
    },
    "recommend": {
     "description": "Whether ``gain_over_horizon`` clears ``threshold``.",
     "type": "boolean"
    },
    "threshold": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The bar ``recommend`` was decided against. Until v12 this was always\nthe flat 8.0 and was never served, so the card asserted a verdict and\nshowed nothing of the rule behind it (v12 W3 \u00a74.2)."
    },
    "threshold_source": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "See ``ChipWorkbenchRow.threshold_source``."
    }
   },
   "required": [
    "added",
    "dropped",
    "gain_over_horizon",
    "kept",
    "recommend",
    "threshold",
    "threshold_source"
   ],
   "title": "SquadDiff",
   "type": "object"
  },
  "SquadPlayer": {
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "element": {
     "description": "FPL's own element id.",
     "type": "integer"
    },
    "is_captain": {
     "description": "Whether he was captained (multiplier 2 or more).",
     "type": "boolean"
    },
    "multiplier": {
     "description": "FPL's own points multiplier for him: 0 unused, 1 normal, 2 captain,\n3 triple captain.",
     "type": "integer"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "position": {
     "description": "GKP, DEF, MID or FWD.",
     "type": "string"
    },
    "price": {
     "description": "His price in \u00a3m at the time the squad was picked.",
     "type": "number"
    }
   },
   "required": [
    "code",
    "element",
    "is_captain",
    "multiplier",
    "name",
    "position",
    "price"
   ],
   "title": "SquadPlayer",
   "type": "object"
  },
  "SquadPlayerRef": {
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "ep": {
     "description": "Expected points, from the saved pool's first horizon gameweek.",
     "type": "number"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "position": {
     "description": "GKP, DEF, MID or FWD.",
     "type": "string"
    },
    "price": {
     "description": "His price in \u00a3m \u2014 cost to buy or hold, sell price for a squad he is\nleaving (`routers/chips.py`'s ``_refs``).",
     "type": "number"
    }
   },
   "required": [
    "code",
    "ep",
    "name",
    "position",
    "price"
   ],
   "title": "SquadPlayerRef",
   "type": "object"
  },
  "Staleness": {
   "properties": {
    "advice_gw": {
     "description": "The gameweek the saved advice was solved for.",
     "type": "integer"
    },
    "current_gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "description": "``upcoming_gw()``'s answer now \u2014 the deadline the user is actually\nfacing, which can be later than ``advice_gw`` when the advice is old."
    },
    "data_through_gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The last gameweek ``ingested_through()`` finds fully scored in the\nbanked parquet, independent of when the advice itself was solved."
    },
    "data_warning": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``data_warning(current_gw, data_through_gw)``'s sentence when the\nmodel's ingested data trails the current gameweek, else ``None``."
    },
    "deadline": {
     "description": "ISO timestamp of ``advice_gw``'s deadline.",
     "type": "string"
    },
    "deadline_passed": {
     "description": "Whether ``deadline`` is before now, computed in ``staleness_for``\n(`routers/advice.py`) against ``pd.Timestamp.now(tz=\"UTC\")``.",
     "type": "boolean"
    },
    "generated_at": {
     "description": "ISO timestamp the advice was solved, from the saved solve state.",
     "type": "string"
    },
    "reason": {
     "description": "The one sentence ``staleness_for`` builds explaining ``stale``, or\nconfirming the advice is current.",
     "type": "string"
    },
    "stale": {
     "description": "``deadline_passed or current_gw > advice_gw``: the advice is behind\nthe gameweek the user needs, either way.",
     "type": "boolean"
    }
   },
   "required": [
    "advice_gw",
    "current_gw",
    "data_through_gw",
    "data_warning",
    "deadline",
    "deadline_passed",
    "generated_at",
    "reason",
    "stale"
   ],
   "title": "Staleness",
   "type": "object"
  },
  "StandingRow": {
   "properties": {
    "entry": {
     "description": "FPL entry id.",
     "type": "integer"
    },
    "event_total": {
     "description": "Points scored in the newest gameweek.",
     "type": "integer"
    },
    "is_you": {
     "description": "Whether this row is the configured entry.",
     "type": "boolean"
    },
    "name": {
     "description": "The entry's team name.",
     "type": "string"
    },
    "player_name": {
     "description": "The manager's own name.",
     "type": "string"
    },
    "rank": {
     "description": "Current league rank.",
     "type": "integer"
    },
    "total": {
     "description": "Total points to date.",
     "type": "integer"
    }
   },
   "required": [
    "entry",
    "event_total",
    "is_you",
    "name",
    "player_name",
    "rank",
    "total"
   ],
   "title": "StandingRow",
   "type": "object"
  },
  "TeamModelHealth": {
   "description": "The week's goals-conceded band, read off the banked components.\n\nv19g \u00a72.2: over GW4-6 the raw ``p_cs_model`` spanned 0.085-0.957 and every\nGW6 club-fixture carried ``odds_weight = 0``, so a third of the horizon\nwas priced on the unbounded team model with no market to blend against.\nThe clip itself is replay-gated (v20); naming the band and the zero-odds\ncount costs nothing and is what makes the drift visible in the meantime.",
   "properties": {
    "fixtures": {
     "description": "Club-fixtures after de-duplication, not player rows.",
     "type": "integer"
    },
    "gw": {
     "description": "The newest gameweek in the file, which is the week these numbers\ndescribe.",
     "type": "integer"
    },
    "max_p_cs_model": {
     "description": "The two ends the model actually reached this week. A minimum expected\ngoals-conceded near zero and a clean-sheet probability near one are the\nsame fixture seen twice.",
     "type": "number"
    },
    "min_e_gc_model": {
     "description": "The lowest expected-goals-conceded the team model reached this week.",
     "type": "number"
    },
    "zero_odds_fixtures": {
     "description": "How many of those were priced with no market at all \u2014 ``odds_weight``\nzero or absent.",
     "type": "integer"
    }
   },
   "required": [
    "fixtures",
    "gw",
    "max_p_cs_model",
    "min_e_gc_model",
    "zero_odds_fixtures"
   ],
   "title": "TeamModelHealth",
   "type": "object"
  },
  "TickerCell": {
   "properties": {
    "difficulty": {
     "description": "The 0-1 rating, from bookmaker odds when available, else Elo.",
     "type": "number"
    },
    "gw": {
     "description": "The gameweek this cell rates.",
     "type": "integer"
    },
    "home": {
     "description": "Whether the fixture is at home.",
     "type": "boolean"
    },
    "opponent": {
     "description": "The opponent's short name.",
     "type": "string"
    }
   },
   "required": [
    "difficulty",
    "gw",
    "home",
    "opponent"
   ],
   "title": "TickerCell",
   "type": "object"
  },
  "TickerData": {
   "properties": {
    "gws": {
     "description": "Every gameweek rated.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "source": {
     "description": "Whether the ratings came from bookmaker odds or the Elo fallback.",
     "enum": [
      "odds",
      "elo"
     ],
     "type": "string"
    },
    "teams": {
     "description": "Every club, sorted by mean difficulty.",
     "items": {
      "$ref": "#/definitions/TickerTeam"
     },
     "type": "array"
    }
   },
   "required": [
    "gws",
    "source",
    "teams"
   ],
   "title": "TickerData",
   "type": "object"
  },
  "TickerTeam": {
   "properties": {
    "cells": {
     "description": "One cell per rated gameweek.",
     "items": {
      "$ref": "#/definitions/TickerCell"
     },
     "type": "array"
    },
    "code": {
     "description": "The club's code.",
     "type": "integer"
    },
    "mean_difficulty": {
     "description": "Mean of ``cells``' difficulty, the figure teams are sorted by.",
     "type": "number"
    },
    "name": {
     "description": "The club's full name.",
     "type": "string"
    },
    "short_name": {
     "description": "The club's short name, as shown on ``cells``.",
     "type": "string"
    }
   },
   "required": [
    "cells",
    "code",
    "mean_difficulty",
    "name",
    "short_name"
   ],
   "title": "TickerTeam",
   "type": "object"
  },
  "Trajectory": {
   "properties": {
    "entry": {
     "description": "FPL entry id.",
     "type": "integer"
    },
    "name": {
     "description": "The entry's team name.",
     "type": "string"
    },
    "points": {
     "description": "The entry's points history, one entry per gameweek played.",
     "items": {
      "$ref": "#/definitions/GwPoint"
     },
     "type": "array"
    }
   },
   "required": [
    "entry",
    "name",
    "points"
   ],
   "title": "Trajectory",
   "type": "object"
  },
  "UpcomingFixture": {
   "description": "One of the next three league games, as the explain panel lists them.\n\nDeliberately *not* ``NextFixture`` (``:87``), whose name it shadowed until\nv12 W5: that one is the advised gameweek's single game, with an opponent\nshort name, a kickoff and a difficulty; this one is a gameweek number and\nan opponent's full name. Two response models under one name gave the\nschema generator two definitions it could only tell apart by mangling\nboth, and gave every ``from .schemas import NextFixture`` in the tree the\nsecond class rather than the first.",
   "properties": {
    "gw": {
     "description": "The gameweek of this upcoming fixture.",
     "type": "integer"
    },
    "home": {
     "description": "Whether the fixture is at home.",
     "type": "boolean"
    },
    "opponent": {
     "description": "The opponent's full name.",
     "type": "string"
    }
   },
   "required": [
    "gw",
    "home",
    "opponent"
   ],
   "title": "UpcomingFixture",
   "type": "object"
  },
  "VerdictRow": {
   "properties": {
    "n": {
     "description": "How many verdicts of this kind were graded.",
     "type": "integer"
    },
    "not_started": {
     "description": "Of those, how many players did not start.",
     "type": "integer"
    },
    "started": {
     "description": "Of those, how many players then started.",
     "type": "integer"
    },
    "verdict": {
     "description": "The presser verdict this row scores (e.g. \"doubtful\", \"OUT\").",
     "type": "string"
    }
   },
   "required": [
    "n",
    "not_started",
    "started",
    "verdict"
   ],
   "title": "VerdictRow",
   "type": "object"
  },
  "VerdictScore": {
   "properties": {
    "n": {
     "description": "How many verdicts of this kind were graded.",
     "type": "integer"
    },
    "precision": {
     "description": "P(did not start | this verdict). Absence is the event every class\nclaims, which is what makes the four numbers comparable.",
     "type": "number"
    },
    "recall": {
     "description": "P(this verdict | did not start), over the verdict-carrying rows.",
     "type": "number"
    },
    "verdict": {
     "description": "The presser verdict this row scores.",
     "type": "string"
    }
   },
   "required": [
    "n",
    "precision",
    "recall",
    "verdict"
   ],
   "title": "VerdictScore",
   "type": "object"
  },
  "WatchRequest": {
   "description": "A star, and optionally a sentence about why.",
   "properties": {
    "code": {
     "description": "FPL player code to star or update.",
     "type": "integer"
    },
    "note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Three requests, not two. ``None`` \u2014 the key omitted \u2014 is \"star him and\nsay nothing about the note\", which keeps whatever note and star date the\nrow already has; ``\"\"`` is \"clear the note\", which is what a cleared\ntextbox on the Watchlist tab sends; text sets it.\n\nThe first two used to be one value, so a star from the explorer destroyed\na note typed on the Watchlist tab. Only a caller that means to write the\nnote sends the key at all."
    }
   },
   "required": [
    "code"
   ],
   "title": "WatchRequest",
   "type": "object"
  },
  "WatchRow": {
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "name": {
     "description": "Player name, from the bootstrap snapshot.",
     "type": "string"
    },
    "note": {
     "description": "The manager's own note on why he is watched.",
     "type": "string"
    },
    "set_at": {
     "description": "ISO timestamp the note was last written.",
     "type": "string"
    },
    "starred_at": {
     "description": "When the star went on, carried unchanged through every later write, so\na row with no note can say how long it has been watched rather than when\nits note was last touched (v19h \u00a72.2).",
     "type": "string"
    }
   },
   "required": [
    "code",
    "name",
    "note",
    "set_at",
    "starred_at"
   ],
   "title": "WatchRow",
   "type": "object"
  },
  "WatchlistPanel": {
   "description": "Every starred player, name-resolved.\n\n``rows`` is empty on a fresh clone and on a broken store alike \u2014 the\ndistinction is a printed line on the server, not a field here, because a\nclient that rendered \"your watchlist may be corrupt\" would be showing the\nuser a problem they cannot act on.",
   "properties": {
    "rows": {
     "description": "Every starred player, sorted by code.",
     "items": {
      "$ref": "#/definitions/WatchRow"
     },
     "type": "array"
    }
   },
   "required": [
    "rows"
   ],
   "title": "WatchlistPanel",
   "type": "object"
  },
  "WhatIfRequest": {
   "properties": {
    "ban": {
     "description": "Player codes excluded from the candidate pool entirely, owned or not.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "chip": {
     "default": "none",
     "description": "The chip to play on the first horizon gameweek, if any.",
     "enum": [
      "none",
      "wc",
      "bb",
      "fh",
      "tc"
     ],
     "type": "string"
    },
    "force_in": {
     "description": "Unowned player codes the solve must buy.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "force_out": {
     "description": "Owned players the solve must sell in the first horizon gameweek.\n\nHe is then out of the squad **for the whole horizon**: ``milp`` pins squad\nmembership to 0 in every week, not only the first, so this is not a sale\nthe solver may reverse later. The bank is credited with his selling price.\n\nNot ``ban``: banning an owned player removes him from the candidate pool\nentirely, so he never enters the squad and \u2014 because he leaves the pool\nrather than the squad \u2014 the sale money never arrives. This says \"sell\nhim\", which is the instruction the planner board's handoff has been\napproximating with ``ban`` since v11.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "horizon": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "How many gameweeks to solve over; ``None`` takes the saved state's."
    },
    "lock": {
     "description": "Owned player codes the solve may not sell.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "max_hits": {
     "default": 0,
     "description": "The most hits the solve may take in the first horizon gameweek.",
     "type": "integer"
    },
    "max_transfers": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The most transfers the solve may make; ``None`` is no cap, 0 is bank."
    }
   },
   "required": [
    "ban",
    "chip",
    "force_in",
    "force_out",
    "horizon",
    "lock",
    "max_hits",
    "max_transfers"
   ],
   "title": "WhatIfRequest",
   "type": "object"
  },
  "WhatIfResult": {
   "properties": {
    "baseline": {
     "$ref": "#/definitions/PlanSummary",
     "description": "The served advice's own plan."
    },
    "captain_changed": {
     "description": "Whether the two plans captain different players.",
     "type": "boolean"
    },
    "delta_xpts": {
     "description": "``yours``' horizon points minus ``baseline``'s.",
     "type": "number"
    },
    "transfers_changed": {
     "description": "Whether the two plans buy different players.",
     "type": "boolean"
    },
    "verdict": {
     "description": "The lab's one-line summary of the comparison.",
     "type": "string"
    },
    "xi_in": {
     "description": "Players in ``yours``' eleven that were not in ``baseline``'s.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "xi_out": {
     "description": "Players in ``baseline``'s eleven that are not in ``yours``'.",
     "items": {
      "$ref": "#/definitions/WirePlayerRef"
     },
     "type": "array"
    },
    "yours": {
     "$ref": "#/definitions/PlanSummary",
     "description": "The plan solved under the what-if lab's request."
    }
   },
   "required": [
    "baseline",
    "captain_changed",
    "delta_xpts",
    "transfers_changed",
    "verdict",
    "xi_in",
    "xi_out",
    "yours"
   ],
   "title": "WhatIfResult",
   "type": "object"
  },
  "WinProb": {
   "properties": {
    "name": {
     "description": "The rival's team name.",
     "type": "string"
    },
    "p_win": {
     "description": "Modelled P(the manager finishes above this rival), from\n``win_probability``.",
     "type": "number"
    },
    "total": {
     "description": "The rival's total points to date.",
     "type": "integer"
    }
   },
   "required": [
    "name",
    "p_win",
    "total"
   ],
   "title": "WinProb",
   "type": "object"
  },
  "WireAdviceLatest": {
   "properties": {
    "advice": {
     "additionalProperties": true,
     "description": "The banked advice payload, enriched at serve time with position,\nidentity, the attacking haul rename and the field frame (`latest()` in\n`routers/advice.py`).",
     "type": "object"
    },
    "deadline": {
     "description": "ISO timestamp of ``gw``'s deadline, from the saved solve state.",
     "type": "string"
    },
    "gw": {
     "description": "The gameweek this advice was solved for, from ``latest_gw()``.",
     "type": "integer"
    },
    "mode": {
     "description": "The solve state's mode (e.g. ``\"normal\"``, a chip), as saved by\n``advise.py`` and read back by ``load_solve_state``.",
     "type": "string"
    },
    "staleness": {
     "$ref": "#/definitions/Staleness",
     "description": "Whether this advice is still current, from ``staleness_for``."
    }
   },
   "required": [
    "advice",
    "deadline",
    "gw",
    "mode",
    "staleness"
   ],
   "title": "WireAdviceLatest",
   "type": "object"
  },
  "WireCalibrationReport": {
   "description": "The banked report, or the honest empty one.\n\n``available`` is what the card branches on. The route answers 200 either\nway (spec \u00a74) because this card sits beside populated ones and a 422 there\nis indistinguishable from a broken endpoint.",
   "properties": {
    "available": {
     "default": false,
     "description": "Whether a calibration report is banked at all.",
     "type": "boolean"
    },
    "cumulative": {
     "additionalProperties": {
      "$ref": "#/definitions/CalibrationHead"
     },
     "default": {},
     "description": "Per-head calibration over the whole season to date.",
     "type": "object"
    },
    "excluded": {
     "default": [],
     "description": "Rows dropped from the report, whatever shape the scorer recorded.",
     "items": {
      "additionalProperties": true,
      "type": "object"
     },
     "type": "array"
    },
    "gameweeks": {
     "default": [],
     "description": "Per-gameweek calibration, for heads with enough rows to report weekly.",
     "items": {
      "$ref": "#/definitions/CalibrationGw"
     },
     "type": "array"
    },
    "git_sha": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The commit the report was built from."
    },
    "missing": {
     "default": [],
     "description": "Gameweeks with no calibration data at all.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "note": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Why the report is unavailable, when it is."
    },
    "omitted": {
     "additionalProperties": {
      "type": "string"
     },
     "default": {},
     "description": "Heads left out of the report entirely, with the reason.",
     "type": "object"
    },
    "per_gw_omitted": {
     "additionalProperties": {
      "type": "string"
     },
     "default": {},
     "description": "Heads reported cumulatively only, with the reason \u2014 see the comment\nabove.",
     "type": "object"
    },
    "run_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "When the report was built."
    },
    "season": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The season the report covers."
    }
   },
   "required": [
    "available",
    "cumulative",
    "excluded",
    "gameweeks",
    "git_sha",
    "missing",
    "note",
    "omitted",
    "per_gw_omitted",
    "run_at",
    "season"
   ],
   "title": "WireCalibrationReport",
   "type": "object"
  },
  "WireHealth": {
   "properties": {
    "artifacts": {
     "description": "Every file under ``reports/``, name and size.",
     "items": {
      "$ref": "#/definitions/ArtifactItem"
     },
     "type": "array"
    },
    "calibration": {
     "anyOf": [
      {
       "$ref": "#/definitions/CalibrationHealth"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The fitted EP calibration's deltas, or ``None`` when no calibration\nartifact is on disk (v19g \u00a72.1). Absent is a real state \u2014 a clone that has\nnever trained \u2014 and it is not the same news as a calibration fitted on\nthree positions, which is why the four-way ``missing`` list travels with\nthe values rather than being inferred from a short dict at the page."
    },
    "core_insights": {
     "anyOf": [
      {
       "$ref": "#/definitions/CoreInsightsHealth"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The core-insights collector's own state (v12 W4 \u00a75.1)."
    },
    "data": {
     "description": "One row per ingested source, from ``DATA_SOURCES`` plus the odds bank.",
     "items": {
      "$ref": "#/definitions/SourceHealth"
     },
     "type": "array"
    },
    "data_through_gw": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The last gameweek ``ingested_through()`` finds fully scored on disk."
    },
    "jobs": {
     "description": "Every installed plist and whether it has run lately (v19a \u00a72.3).\n\n``launchd`` above stays: it carries the advise log's *last line*, which\nis the one job whose output a reader wants quoted rather than dated.",
     "items": {
      "$ref": "#/definitions/JobHealth"
     },
     "type": "array"
    },
    "last_backup": {
     "anyOf": [
      {
       "$ref": "#/definitions/BackupHealth"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The newest ``gaffer-*.tar.gz`` in the configured backup directory.\n\n``None`` means *never*, and the tab renders it as \"never \u2014 run `gaffer\nbackup`\" rather than as a blank cell. A zero-byte dict would have been the\nother option and it is worse: it renders as a backup that happened and was\nempty, which is the one outcome this feature exists to prevent."
    },
    "launchd": {
     "$ref": "#/definitions/LaunchdHealth",
     "description": "The advise job's own log health, kept separate from ``jobs`` below."
    },
    "model_health": {
     "anyOf": [
      {
       "additionalProperties": true,
       "type": "object"
      },
      {
       "type": "null"
      }
     ],
     "description": "``reports/health.json``'s contents, whatever a fit run last wrote\nthere, or ``None`` when no such report exists."
    },
    "models": {
     "description": "Every fitted model's ``.meta.json`` under ``MODELS_DIR``.",
     "items": {
      "$ref": "#/definitions/WireModelHealth"
     },
     "type": "array"
    },
    "odds_key_present": {
     "description": "Whether ``config.toml``'s ``[odds] api_key`` is set \u2014 never the key\nitself.",
     "type": "boolean"
    },
    "season_config": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What ``config.toml`` says this season is."
    },
    "season_ingested": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What the last refresh actually banked, derived from the events' own\ndeadlines. Read off disk, never off the API: this endpoint is polled by a\ntab and must not depend on FPL being up."
    },
    "season_ok": {
     "anyOf": [
      {
       "type": "boolean"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Does the banked data's season match ``config.current_season``?\n\nThree states, not two. ``None`` is *cannot tell* \u2014 no events snapshot, or\ndeadlines that will not parse \u2014 and it is not an alarm: a cold clone has\nno data to disagree with. The banner draws on ``False`` alone."
    },
    "solver_top_n": {
     "anyOf": [
      {
       "additionalProperties": {
        "type": "integer"
       },
       "type": "object"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Players per position the solver may consider, on top of the ones you own.\n\nNamed for what it is on the wire \u2014 a solver pool \u2014 since a schema field\ncarries no TOML section with it. The value is\n``config_in_force().solver_top_n()``'s, so it is what an actual solve\nwould get rather than what the file says."
    },
    "team_model": {
     "anyOf": [
      {
       "$ref": "#/definitions/TeamModelHealth"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The newest banked components' goals-conceded band (v19g \u00a72.2), or\n``None`` when nothing has been banked or the file predates the model\ncolumns."
    }
   },
   "required": [
    "artifacts",
    "calibration",
    "core_insights",
    "data",
    "data_through_gw",
    "jobs",
    "last_backup",
    "launchd",
    "model_health",
    "models",
    "odds_key_present",
    "season_config",
    "season_ingested",
    "season_ok",
    "solver_top_n",
    "team_model"
   ],
   "title": "WireHealth",
   "type": "object"
  },
  "WireHistory": {
   "properties": {
    "backtests": {
     "description": "Banked backtest log rows, whatever shape the replay tooling wrote.",
     "items": {
      "additionalProperties": true,
      "type": "object"
     },
     "type": "array"
    },
    "prices": {
     "description": "Price history for every player ever owned.",
     "items": {
      "$ref": "#/definitions/PriceSeries"
     },
     "type": "array"
    },
    "runs": {
     "description": "Every advised gameweek, newest first.",
     "items": {
      "$ref": "#/definitions/HistoryRun"
     },
     "type": "array"
    }
   },
   "required": [
    "backtests",
    "prices",
    "runs"
   ],
   "title": "WireHistory",
   "type": "object"
  },
  "WireModelHealth": {
   "properties": {
    "metrics": {
     "additionalProperties": true,
     "description": "The rest of the sidecar's contents \u2014 whatever the trainer recorded.",
     "type": "object"
    },
    "name": {
     "description": "The model's filename stem, from its ``.meta.json`` sidecar.",
     "type": "string"
    },
    "saved_at": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "description": "When the model was fitted, from the sidecar's ``saved_at`` key."
    }
   },
   "required": [
    "metrics",
    "name",
    "saved_at"
   ],
   "title": "WireModelHealth",
   "type": "object"
  },
  "WirePlanTimeline": {
   "properties": {
    "alternatives": {
     "default": [],
     "description": "Empty on every artifact written before v12, and on any run with\n``[optimizer] alt_plan_max_gap = 0``. The board draws no tab strip for an\nempty list rather than a strip with one tab in it (v12 W3 \u00a74.3).",
     "items": {
      "$ref": "#/definitions/PlanAlternative"
     },
     "type": "array"
    },
    "bank": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "What is in the bank before the horizon's first move, in millions.\n\n``SolveState.bank`` in tenths, through the same conversion every price on\nthis payload takes. ``None`` means the solve state carried no usable\nfigure \u2014 never 0.0, which is \"fully invested\"."
    },
    "generated_at": {
     "description": "ISO timestamp the plan was generated.",
     "type": "string"
    },
    "gw": {
     "description": "The gameweek the plan was solved for.",
     "type": "integer"
    },
    "objective": {
     "anyOf": [
      {
       "$ref": "#/definitions/PlanGw"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "v16 \u00a74: the solver's own week one, traced, when the served plan is a\ndifferent rung of the ladder. ``None`` when they agree or when the\npayload predates the field."
    },
    "weeks": {
     "description": "The recommended plan, one entry per horizon gameweek.",
     "items": {
      "$ref": "#/definitions/PlanGw"
     },
     "type": "array"
    }
   },
   "required": [
    "alternatives",
    "bank",
    "generated_at",
    "gw",
    "objective",
    "weeks"
   ],
   "title": "WirePlanTimeline",
   "type": "object"
  },
  "WirePlayerExplain": {
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "ep_next": {
     "description": "Expected points for the first horizon gameweek, summed from\n``fixtures``.",
     "type": "number"
    },
    "fixtures": {
     "description": "The scoring breakdown for every horizon fixture found in the banked\ncomponents.",
     "items": {
      "$ref": "#/definitions/FixtureExplain"
     },
     "type": "array"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "next_fixtures": {
     "description": "His next three league games, off the fixtures snapshot.",
     "items": {
      "$ref": "#/definitions/UpcomingFixture"
     },
     "type": "array"
    },
    "position": {
     "description": "GKP, DEF, MID or FWD.",
     "type": "string"
    },
    "set_pieces": {
     "additionalProperties": {
      "anyOf": [
       {
        "type": "integer"
       },
       {
        "type": "null"
       }
      ]
     },
     "description": "``penalties`` / ``free_kicks`` / ``corners``, each the user's override\nfile's word where it has one and FPL's otherwise \u2014 the same numbers\n:class:`PlayerRow` serves, from the same loader.",
     "type": "object"
    },
    "set_pieces_manual": {
     "description": "Which of ``set_pieces``' three orders came from the user's override\nfile, a cleared one included. Additive and default-empty, so a client that\ndoes not read it is unaffected.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "team_name": {
     "description": "His club's name.",
     "type": "string"
    }
   },
   "required": [
    "code",
    "ep_next",
    "fixtures",
    "name",
    "next_fixtures",
    "position",
    "set_pieces",
    "set_pieces_manual",
    "team_name"
   ],
   "title": "WirePlayerExplain",
   "type": "object"
  },
  "WirePlayerRef": {
   "properties": {
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "ep": {
     "description": "Expected points, as the plan payload carries him.",
     "type": "number"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "next_fixture": {
     "anyOf": [
      {
       "$ref": "#/definitions/NextFixture"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "His next game, resolved at serve time by ``gaffer.web.identity``."
    },
    "position": {
     "description": "His position: GKP, DEF, MID or FWD.",
     "type": "string"
    },
    "team_code": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "His club's code, resolved at serve time by ``gaffer.web.identity``."
    },
    "team_short": {
     "anyOf": [
      {
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "His club's short name, resolved at serve time by\n``gaffer.web.identity``."
    }
   },
   "required": [
    "code",
    "ep",
    "name",
    "next_fixture",
    "position",
    "team_code",
    "team_short"
   ],
   "title": "WirePlayerRef",
   "type": "object"
  },
  "WirePlayerRow": {
   "properties": {
    "available": {
     "description": "Whether his status is outside :data:`UNAVAILABLE_STATUS`.",
     "type": "boolean"
    },
    "chance_of_playing": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "description": "FPL's own chance-of-playing percentage."
    },
    "code": {
     "description": "FPL player code.",
     "type": "integer"
    },
    "corners_order": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "description": "His club's corner-taking rank, from ``data/set_pieces.toml`` or FPL."
    },
    "element": {
     "description": "FPL's own element id, as the bootstrap snapshot carries him.",
     "type": "integer"
    },
    "ep_hi": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "p75 of ``ep_next``'s distribution, same contract as ``ep_lo``."
    },
    "ep_horizon": {
     "description": "Expected points summed over the solve horizon.",
     "type": "number"
    },
    "ep_lo": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "p25 of the noise model's distribution for ``ep_next`` \u2014 see\n:mod:`gaffer.uncertainty`. Deliberately **not** ``ep_next`` minus\nsomething: the calibrated path recentres, so the pair is quartiles rather\nthan a symmetric interval, and the UI labels it that way.\n\n``None`` \u2014 never ``ep_next`` \u2014 when the components frame carries no\nminutes model for him, or is absent altogether. A zero-width band on the\nleast-known player in the pool would read as certainty."
    },
    "ep_next": {
     "description": "Expected points for the next gameweek alone.",
     "type": "number"
    },
    "field_class": {
     "anyOf": [
      {
       "enum": [
        "shield",
        "sword",
        "threat"
       ],
       "type": "string"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``shield`` | ``sword`` | ``threat``, or ``None`` for the quadrant with\nnothing to say.\n\nA ``Literal`` and not a ``str``, since v12 W5: ``routers.players\n.field_class`` returns exactly these three and ``None``, the client has\nalways typed it as those three, and only the schema was saying ``str`` \u2014\nwhich the generated types then repeated, and the pitch's shirt colours\nstopped compiling against."
    },
    "field_eo": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Top-10k effective ownership from the latest banked scrape.\n\n``None`` means *unknown*, and it means it in two different situations\nthat the UI renders identically and correctly: no field log at all, or a\nlog that does not carry this player because no sampled entry started him.\nNeither is 0.0, which the reader would take as a measured differential."
    },
    "field_eo_deadline": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "Field EO projected forward one gameweek, in percent.\n\n``None`` means *no trend*, which is what one gameweek of samples buys \u2014\nand never 0.0, which is the different and stronger claim that nobody in\nthe top 10k starts him. Same contract as ``field_eo`` above."
    },
    "field_eo_delta": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The observed move between the last two sampled gameweeks, in points of\nEO. ``None`` when there is no earlier sample; ``0.0`` is a measurement \u2014\nthe field held steady."
    },
    "field_n": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "How many sampled entries the figure was measured over.\n\n\u00b12.8 from three hundred entries and \u00b12.8 from thirty are different claims\nand the page is entitled to say which one it is showing."
    },
    "field_se": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The standard error on ``field_eo``, in percentage points.\n\n``None`` for exactly the situations ``field_eo`` is ``None`` for, and \u2014 the\npart worth stating \u2014 **never 0.0**. Zero here would be a claim of perfect\nprecision drawn from a sample of a few hundred entries, which is a stronger\nstatement than any number on this row is entitled to make."
    },
    "free_kicks_order": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "description": "His club's free-kick-taking rank, from ``data/set_pieces.toml`` or FPL."
    },
    "in_squad": {
     "description": "Whether he is currently owned.",
     "type": "boolean"
    },
    "last4": {
     "description": "Points from the last four *finished* gameweeks, oldest first.\n\nEmpty when ``data/live/player_gw.parquet`` has not been written \u2014 the\nsparkline then renders an em dash rather than a flat line at zero.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "league_eo": {
     "description": "Effective ownership among the manager's rivals \u2014 captaincy weighted\ndouble \u2014 from the saved solve state's ``league_eo``.",
     "type": "number"
    },
    "name": {
     "description": "Player name.",
     "type": "string"
    },
    "news": {
     "description": "FPL's own news text for the player.",
     "type": "string"
    },
    "ownership": {
     "description": "FPL's own overall ownership percentage.",
     "type": "number"
    },
    "p_blank": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``P(points <= 2)`` under the same distribution."
    },
    "p_haul": {
     "anyOf": [
      {
       "type": "number"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "``P(points >= 10)`` under the same distribution. Crude by construction:\nit prices *forecast* error, not football's own variance.\n\nThis is ``uncertainty.Band.p_haul``, the whole-forecast tail \u2014 *not*\n``models.assemble.p_haul``, which is P(2+ attacking returns) under a\nPoisson and is served on the advice payload as ``p_attacking_haul``. Two\nquantities, one page, one name until v9c (spec D3)."
    },
    "penalties_order": {
     "anyOf": [
      {
       "type": "integer"
      },
      {
       "type": "null"
      }
     ],
     "description": "His club's penalty-taking rank, from ``data/set_pieces.toml`` or FPL."
    },
    "position": {
     "description": "GKP, DEF, MID or FWD.",
     "type": "string"
    },
    "price": {
     "description": "Current price in \u00a3m, from FPL's ``now_cost`` divided by ten.",
     "type": "number"
    },
    "set_piece_manual": {
     "description": "Kinds of set piece whose order above came from ``data/set_pieces.toml``\nrather than from FPL. Empty on every machine with no override file.\n\nIncludes a *cleared* order: a file that lists his club's queue and leaves\nhim out serves him ``None``, and that blank is the file's word as much as\na rank is. Only ``penalties`` reaches expected points; the other two move\nthe numbers on this row and nothing else.",
     "items": {
      "type": "string"
     },
     "type": "array"
    },
    "status": {
     "description": "FPL's own status code for the player.",
     "type": "string"
    },
    "team_code": {
     "description": "His club's code.",
     "type": "integer"
    },
    "team_name": {
     "description": "His club's name.",
     "type": "string"
    }
   },
   "required": [
    "available",
    "chance_of_playing",
    "code",
    "corners_order",
    "element",
    "ep_hi",
    "ep_horizon",
    "ep_lo",
    "ep_next",
    "field_class",
    "field_eo",
    "field_eo_deadline",
    "field_eo_delta",
    "field_n",
    "field_se",
    "free_kicks_order",
    "in_squad",
    "last4",
    "league_eo",
    "name",
    "news",
    "ownership",
    "p_blank",
    "p_haul",
    "penalties_order",
    "position",
    "price",
    "set_piece_manual",
    "status",
    "team_code",
    "team_name"
   ],
   "title": "WirePlayerRow",
   "type": "object"
  },
  "WireReview": {
   "properties": {
    "gws": {
     "description": "Every graded gameweek's own row.",
     "items": {
      "$ref": "#/definitions/ReviewGw"
     },
     "type": "array"
    },
    "summary": {
     "anyOf": [
      {
       "$ref": "#/definitions/WireReviewSummary"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The season totals over ``gws``, or ``None`` when nothing is graded."
    }
   },
   "required": [
    "gws",
    "summary"
   ],
   "title": "WireReview",
   "type": "object"
  },
  "WireReviewSummary": {
   "properties": {
    "accuracy": {
     "description": "The accuracy series across graded gameweeks.",
     "items": {
      "$ref": "#/definitions/ReviewAccuracyPoint"
     },
     "type": "array"
    },
    "best": {
     "anyOf": [
      {
       "additionalProperties": true,
       "type": "object"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The best-graded gameweek's own row, whatever shape the summariser\npicked."
    },
    "by_reason": {
     "description": "The deviation tally, ``REASONS`` order then ``none``. Codes with no\ngraded gameweek are absent \u2014 a nought would read as a measurement.",
     "items": {
      "$ref": "#/definitions/ReasonTally"
     },
     "type": "array"
    },
    "gws": {
     "description": "Every graded gameweek the summary covers.",
     "items": {
      "type": "integer"
     },
     "type": "array"
    },
    "hindsight_gap": {
     "default": 0,
     "description": "Season total of points left on the table versus each week's best\nlegal eleven.",
     "type": "integer"
    },
    "hindsight_gap_gws": {
     "default": 0,
     "description": "How many gameweeks that total covers.",
     "type": "integer"
    },
    "lanes": {
     "additionalProperties": {
      "$ref": "#/definitions/ReviewLaneTotal"
     },
     "description": "Season totals, one entry per decision lane.",
     "type": "object"
    },
    "points_on_bench": {
     "default": 0,
     "description": "Season total of points left on the bench.",
     "type": "integer"
    },
    "points_on_bench_gws": {
     "default": 0,
     "description": "How many gameweeks that total covers. A season of unbanked histories\nsums to zero over zero gameweeks, which is not an empty bench.",
     "type": "integer"
    },
    "reconciled_gws": {
     "default": 0,
     "description": "Graded gameweeks where the review's points matched FPL's official\nfigure.",
     "type": "integer"
    },
    "unreconciled_gws": {
     "default": 0,
     "description": "Graded gameweeks where they did not, or could not be checked.",
     "type": "integer"
    },
    "worst": {
     "anyOf": [
      {
       "additionalProperties": true,
       "type": "object"
      },
      {
       "type": "null"
      }
     ],
     "default": null,
     "description": "The worst-graded gameweek's own row, whatever shape the summariser\npicked."
    }
   },
   "required": [
    "accuracy",
    "best",
    "by_reason",
    "gws",
    "hindsight_gap",
    "hindsight_gap_gws",
    "lanes",
    "points_on_bench",
    "points_on_bench_gws",
    "reconciled_gws",
    "unreconciled_gws",
    "worst"
   ],
   "title": "WireReviewSummary",
   "type": "object"
  }
 }
}
